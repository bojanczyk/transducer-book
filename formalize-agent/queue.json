[
  {
    "id": "exercises-intro",
    "title": "Exercises: the introduction",
    "results": [],
    "lean_names": [],
    "prompt": "The exercises of the book are written as `\\exer{statement}{solution}` (see `\\newcommand{\\exer}` in `macros.sty`). The second argument is the author's own solution, and almost every exercise now has one: read it as the intended proof, not as a hint. The printed edition does not typeset solutions, so they have never been checked by anything.\n\nFormalise the exercises of the introduction: `intro.tex`, 11 exercises, all with solutions.\n\n* Put them in a new `RequestProject/Exercises/Intro.lean`, one section per chapter, in the order the book states them.\n* State each exercise faithfully. Some are not mathematical statements at all -- \"give an example of...\", \"is the generated subgroup necessarily finite?\" -- and for those, either formalise the concrete claim the author's solution actually establishes, or leave the exercise out and say which and why in your summary. Do not invent a statement to have something to prove.\n* An exercise is not a numbered result of the main text, so it does not belong in the `THEOREMS.md` tables. Record them in a new `EXERCISES.md`, in the same style: the exercise, the Lean name, the status.\n* Only some exercises carry a `\\label` in the sources. Use it where there is one. Where there is not, give the declaration a descriptive Lean name and record in `EXERCISES.md` which chapter file the exercise is in and its position within that chapter, and list in your closing summary every exercise you could not refer to by label -- the author can then add them.\n* Add the aliases to `RequestProject/Labels.lean` only for the exercises that do have a label, keeping that file's generated format and its `assert_no_sorry` convention.\n\nDo not touch the numbered results of the main text; if formalising an exercise needs a lemma that is really a result of the book, say so rather than restating it.",
    "depends_on": {},
    "max_continues": 4
  },
  {
    "id": "exercises-partA",
    "title": "Exercises: Part A (Mealy machines)",
    "results": [],
    "lean_names": [],
    "prompt": "The exercises of the book are written as `\\exer{statement}{solution}` (see `\\newcommand{\\exer}` in `macros.sty`). The second argument is the author's own solution, and almost every exercise now has one: read it as the intended proof, not as a hint. The printed edition does not typeset solutions, so they have never been checked by anything.\n\nFormalise the exercises of Part A: `mealy.tex` (7) and `krohn-rhodes.tex` (5), all with solutions.\n\n* Put them in a new `RequestProject/Exercises/PartA.lean`, one section per chapter, in the order the book states them.\n* State each exercise faithfully. Some are not mathematical statements at all -- \"give an example of...\", \"is the generated subgroup necessarily finite?\" -- and for those, either formalise the concrete claim the author's solution actually establishes, or leave the exercise out and say which and why in your summary. Do not invent a statement to have something to prove.\n* An exercise is not a numbered result of the main text, so it does not belong in the `THEOREMS.md` tables. Record them in a new `EXERCISES.md`, in the same style: the exercise, the Lean name, the status.\n* Only some exercises carry a `\\label` in the sources. Use it where there is one. Where there is not, give the declaration a descriptive Lean name and record in `EXERCISES.md` which chapter file the exercise is in and its position within that chapter, and list in your closing summary every exercise you could not refer to by label -- the author can then add them.\n* Add the aliases to `RequestProject/Labels.lean` only for the exercises that do have a label, keeping that file's generated format and its `assert_no_sorry` convention.\n\nDo not touch the numbered results of the main text; if formalising an exercise needs a lemma that is really a result of the book, say so rather than restating it.",
    "depends_on": {},
    "max_continues": 4
  },
  {
    "id": "exercises-partBC",
    "title": "Exercises: Parts B and C",
    "results": [],
    "lean_names": [],
    "prompt": "The exercises of the book are written as `\\exer{statement}{solution}` (see `\\newcommand{\\exer}` in `macros.sty`). The second argument is the author's own solution, and almost every exercise now has one: read it as the intended proof, not as a hint. The printed edition does not typeset solutions, so they have never been checked by anything.\n\nFormalise the exercises of Parts B and C: `rational-relations.tex` (1), `rational-functions.tex` (9), `regular-primes.tex` (1) and `logic.tex` (1).\n\n* Put them in a new `RequestProject/Exercises/PartBC.lean`, one section per chapter, in the order the book states them.\n* State each exercise faithfully. Some are not mathematical statements at all -- \"give an example of...\", \"is the generated subgroup necessarily finite?\" -- and for those, either formalise the concrete claim the author's solution actually establishes, or leave the exercise out and say which and why in your summary. Do not invent a statement to have something to prove.\n* An exercise is not a numbered result of the main text, so it does not belong in the `THEOREMS.md` tables. Record them in a new `EXERCISES.md`, in the same style: the exercise, the Lean name, the status.\n* Only some exercises carry a `\\label` in the sources. Use it where there is one. Where there is not, give the declaration a descriptive Lean name and record in `EXERCISES.md` which chapter file the exercise is in and its position within that chapter, and list in your closing summary every exercise you could not refer to by label -- the author can then add them.\n* Add the aliases to `RequestProject/Labels.lean` only for the exercises that do have a label, keeping that file's generated format and its `assert_no_sorry` convention.\n\nDo not touch the numbered results of the main text; if formalising an exercise needs a lemma that is really a result of the book, say so rather than restating it.",
    "depends_on": {},
    "max_continues": 4
  },
  {
    "id": "audit-C",
    "title": "Audit: verify everything claimed for Parts A, B and C, and the exercises",
    "results": [],
    "lean_names": [],
    "prompt": "This is an audit task, not a new-mathematics task. Do not start proving any new numbered result.\n\nGo through every numbered result of Parts A, B and C that `THEOREMS.md` records as proved, and verify the claim mechanically:\n\n1. `lake build` from scratch and report the result.\n2. For each such result, run `#print axioms` on its Lean name and check that the only axioms are `propext`, `Classical.choice`, `Quot.sound` -- with the exception of the results that are deliberately conditional (B.1.6 and the four effectivity results of Section B.3/B.4), for which you should instead confirm that the hypothesis appears as an explicit argument of the theorem and report what it is.\n3. Check that no numbered result has been silently weakened relative to the book: for each one, compare the Lean statement with the statement in the LaTeX sources (`../krohn-rhodes.tex`, `../rational-functions.tex`, `../weighted.tex`, `../2dfa.tex`, `../sst.tex`, `../logic.tex`, `../myhill-nerode.tex`) and flag every divergence you find, including the ones that are already documented.\n4. Check that `THEOREMS.md` is accurate: every result marked proved really is, every result marked open really is, and the file list matches what is on disk.\n\nThen correct `THEOREMS.md` wherever it is wrong, and report in your summary: the list of results whose status you had to change, and every divergence between a Lean statement and the book that you found. Do not fix the divergences in this task -- just report them, precisely.\n\nTwo corrections to `THEOREMS.md` have been made by hand and must be preserved; check both against the sources and keep whichever form is true.\n\n* **Lemma `lem:output-of-snake-graph-is-regular` is formalised** (the book numbers it C.2.12). It is recorded as unformalised in three places now -- the index table of `THEOREMS.md`, the accounting list at the end of `RequestProject/Labels.lean`, and `LABELS.md` -- so fix all three, and give it an alias with `assert_no_sorry` like any other proved result. Its index row used to read `| Lemmas C.2.3, C.2.4, C.2.12 | not formalised ... |`, which was written before the snake work and went stale. C.2.12 is the book's snake lemma -- the output of a snake graph is a regular function -- and it is `Transducers.boundedWidth_isRegular` in `SnakeReg.lean`, stated for the run of a two-way transducer rather than for an alphabet of snake letters. `#print axioms` on it reports only `propext`, `Classical.choice`, `Quot.sound`. It now has its own row; C.2.3 and C.2.4 remain genuinely not formalised.\n* **Definition B.1.3 is not formalised and was not recorded anywhere.** It is the definition of the rational and the recognisable subsets of a monoid. Nothing depends on it -- the book refers to it once, in the remark about the name \"Kleene Theorem\" -- so leaving it unformalised is a reasonable choice, but the index should say so rather than pass over it in silence. Add a row for it.\n\nMore generally: an index row that says a result is not formalised is exactly the kind of claim that rots, because it is written once and never revisited. Check every such row against the sources, not against the row's own history.\n\n**Replace the book's numbers with its `\\label` names.** Every place this formalisation names a result of the book by number -- the `**Theorem C.4.1.**` at the head of a docstring, the first column of every table in `THEOREMS.md`, and any mention in prose -- should name it by its LaTeX label instead. The reason is that numbers are not stable: inserting one result renumbers every later one, and a docstring that says `C.4.1` then quietly refers to something else, with nothing to catch it. Labels do not move.\n\nKeep the wording otherwise as it is: `**Theorem `thm:mso-logic-languages`.**` where it used to say `**Theorem C.4.1.**`.\n\nYou cannot read all of these out of your own copy of the book, which is older than the author's and is missing 28 labels, so here is the correspondence for every result that is formalised. It is generated from the author's current `main.aux`, and it is authoritative -- prefer it to anything in your sources.\n\n| Result, as the book currently numbers it | LaTeX label | primary Lean declaration |\n| --- | --- | --- |\n| Definition .0.1 | `def:continuity` | `Continuous` |\n| Definition A.1.1 | `def:mealy-machine` | `Mealy` |\n| Theorem A.1.2 | `thm:equivalence-decidable-mealy` | `mealy_equiv_iff_bounded` |\n| Theorem A.1.3 | `thm:composition-mealy` | `mealy_comp` |\n| Theorem A.1.4 | `thm:continuity-mealy` | `mealy_continuous` |\n| Definition A.2.1 | `def:prime-mealy-machines` | `Reversible` |\n| Theorem A.2.2 | `thm:krohn-rhodes` | `krohn_rhodes` |\n| Definition A.2.3 | `def:map-lifting` | `mapLift` |\n| Lemma A.2.4 | `lem:map-lifting-decomposition-mealy` | `mapLift_prime_decomposition` |\n| Lemma A.2.5 | `lem:Mealy-map-lifting` | `stateTransTransducer_prime_decomposition` |\n| Lemma A.2.6 | `lem:reversible-composition` | `reversible_comp` |\n| Definition A.2.7 | `def:aperiodic-mealy` | `Aperiodic` |\n| Theorem A.2.8 | `thm:aperiodic-mealy` | `aperiodic_iff_flipflop_composition` |\n| Claim A.2.9 | `claim:aperiodic-pumping` | `aperiodic_iff_pumping` |\n| Lemma A.2.10 | `lemma:derivatives` | `myhill_nerode_mealy` |\n| Lemma A.2.11 | `lem:aperiodicity-minimal-machine` | `aperiodic_iff_transStabilises` |\n| Definition B.1.1 | `def:nfa-with-output` | `NFAO` |\n| Definition B.1.2 | `def:rational-relation` | `IsRationalRel` |\n| Theorem B.1.4 | `thm:composition-rational-relations` | `rationalRel_comp` |\n| Theorem B.1.5 | `thm:continuity-rational-relations` | `rationalRel_continuous` |\n| Theorem B.1.6 | `thm:undecidable-equivalence-rational-relations` | `rationalRel_equivalence_undecidable` |\n| Claim B.1.7 | `claim:homomorphism-complement-rational` | `hom_complement_rational` |\n| Definition B.2.1 | `def:rational-function` | `IsRationalFun` |\n| Definition B.2.2 | `def:bimachine` | `Bimachine` |\n| Theorem B.2.3 | `thm:bimachines` | `rational_iff_unambiguous_iff_bimachine` |\n| Lemma B.2.4 | `lemma:eliminate-epsilon-transitions` | `epsilon_elimination` |\n| Lemma B.2.5 | `lem:uniformisation` | `uniformisation` |\n| Theorem B.2.6 | `thm:rational-primes` | `rational_iff_prime_composition` |\n| Theorem B.2.7 | `thm:rational-is-mealy-characterisation` | `rational_isMealy_iff` |\n| Definition B.3.1 | `def:semiring` | `Semiring` |\n| Definition B.3.2 | `def:weighted-automaton` | `wEval` |\n| Theorem B.3.3 | `thm:equivalence-weighted-automata` | `weighted_equivalence_decidable` |\n| Theorem B.3.4 | `thm:equivalence-rational-functions` | `rationalFun_equivalence_decidable` |\n| Lemma B.3.5 | `lem:closure-weighted-automata-precomposition` | `weighted_precomp_rational` |\n| Theorem B.3.6 | `thm:characterisation-rational-functions-weighted-automata` | `rational_iff_weighted_precomp` |\n| Theorem B.3.7 | `thm:zeroness-weighted-automata` | `weighted_zeroness_decidable` |\n| Theorem B.4.1 | `thm:mealy-machine-independent` | `isMealy_iff` |\n| Theorem B.4.2 | `thm:decide-if-mealy` | `rationalFun_isMealy_decidable` |\n| Lemma B.4.3 | `lem:decide-if-length-preserving` | `rationalFun_lengthPreserving_decidable` |\n| Claim B.4.4 | `claim:typing-length-preserving` | `lengthPreserving_iff_typing` |\n| Lemma B.4.5 | `lem:characterisation-length-preserving` | `lengthPreserving_rational_normal_form` |\n| Theorem B.4.6 | `thm:sequential-function-independent` | `isSequential_iff` |\n| Definition B.4.7 | `def:left-distance` | `leftDist` |\n| Theorem B.4.8 | `thm:subsequential-functions` | `isSubsequential_iff` |\n| Claim B.4.9 | `claim:bounded-extensions` | `delay_bound` |\n| Claim B.4.10 | `claim:computing-branching-part` | `key_drop` |\n| Claim B.4.11 | `claim:offsets-are-regular` | `incr_congr` |\n| Claim B.4.12 | `claim:eliminating-negative-letters` | `exists_deletion_bound` |\n| Theorem B.4.13 | `thm:machine-independent-rational-functions` | `isRationalFun_iff` |\n| Definition C.0.14 | `def:regular-functions` | `IsRegularFun` |\n| Theorem C.1.1 | `thm:regular-functions-are-continuous-and-closed-under-composition` | `regular_continuous` |\n| Lemma C.1.2 | `lem:reversal-duplication-continuous` | `reverse_duplicate_continuous` |\n| Lemma C.1.3 | `lem:map-lifting-continuous` | `mapLift_continuous` |\n| Theorem C.1.4 | `thm:decidable-equivalence-regular` | `regular_equivalence_decidable` |\n| Definition C.2.1 | `def:two-way-transducer` | `TwoWay` |\n| Theorem C.2.2 | `thm:continuity-2dfas` | `twoWay_continuous` |\n| Theorem C.2.5 | `thm:composition-of-two-way-transducers` | `twoWay_comp` |\n| Lemma C.2.6 | `lem:2dfa-precomposition-with-mealy` | `twoWay_precomp_mealy` |\n| Corollary C.2.7 | `cor:2dfa-closure-under-composition` | `twoWay_precomp_rational` |\n| Corollary C.2.8 | `cor:2dfa-computes-all-regular-functions` | `regularFun_isTwoWay` |\n| Theorem C.2.9 | `thm:2dfa-decomposition-into-primes` | `twoWay_iff_regular` |\n| Lemma C.2.10 | `lem:regular-closure-properties` | `regular_closure_properties` |\n| Claim C.2.11 | `claim:conditional` | `sum_of_regular` |\n| Lemma C.2.12 | `lem:output-of-snake-graph-is-regular` | `Transducers.boundedWidth_isRegular` |\n| Definition C.3.1 | `def:sst` | `SST` |\n| Theorem C.3.2 | `theorem:sst-two-way-equivalence` | `sst_iff_regular` |\n| Theorem C.4.1 | `thm:mso-logic-languages` | `regular_iff_msoDefinable` |\n| Lemma C.4.2 | `lem:mso-free-variables` | `mso_annotated_regular` |\n| Definition C.4.3 | `def:mso-relabeling` | `MSORelabelling` |\n| Theorem C.4.4 | `thm:logic-rational-functions` | `rational_iff_msoRelabelling` |\n| Claim C.4.5 | `claim:transition-formula` | `exists_form` |\n| Claim C.4.6 | `claim:mso-annotation-regular` | `msoRelabelling_annotation_regular` |\n| Definition C.4.7 | `def:mso-transduction` | `MSOTransduction` |\n| Theorem C.4.8 | `thm:logic-regular-functions` | `msoTransduction_iff_regular` |\n| Lemma C.4.9 | `lem:logic-reduction-to-type-n` | `exists_norm` |\n| Lemma C.4.10 | `lem:logic-precomputation` | `mso_formulas_via_rational` |\n| Theorem C.4.11 | `thm:logic-aperiodic` | `foDefinable_iff_aperiodic_dfa` |\n| Definition C.4.12 | `def:k-types` | `tp` |\n| Lemma C.4.13 | `lem:k-types-fo-equivalence` | `tp_eq_iff_fo_equiv` |\n| Claim C.4.14 | `claim:fo-composition-quantifier-rank` | `sat_iff_of_kEquiv` |\n| Lemma C.4.15 | `lem:k-types-properties` | `tp_properties` |\n| Theorem C.4.16 | `thm:fo-rational-functions` | `foRelabelling_iff_aperiodicBimachine` |\n| Theorem D.1.1 | `thm:for-transducers-are-polyregular` | `polyregular_iff_forTransducer` |\n| Definition D.1.2 | `def:prenex-normal-form-for-transducers` | `PrenexForm` |\n| Lemma D.1.3 | `lemma:prenex-normal-form` | `forTransducer_prenex` |\n| Lemma D.1.4 | `lem:for-closed-under-composition` | `forTransducer_comp` |\n| Theorem D.2.1 | `thm:pebble-are-continuous` | `pebble_continuous` |\n| Theorem D.2.4 | `thm:pebble-are-for` | `pebble_iff_forTransducer` |\n\nIf you meet a formalised result that is not in this table, leave its number alone and list it in your summary.\n\n**Labels that were guessed, and their real names.** The rename to labels was done against a copy of the book older than the author's, so where a result's `\\label` was missing you wrote a placeholder `nolabel:...`. That was the right thing to do rather than inventing a plausible name, but the labels do exist; here they are. Replace each one everywhere it occurs -- docstrings, `Labels.lean`, `LABELS.md`, `THEOREMS.md`, prose -- and keep the alias names in `Labels.lean` in step.\n\n| what the sources say now | the book's actual label |\n| --- | --- |\n| `item:fo-types-more-information` | `lem:k-types-properties` |\n| `nolabel:claim-aperiodic-pumping` | `claim:aperiodic-pumping` |\n| `nolabel:claim-complement-of-homomorphism` | `claim:homomorphism-complement-rational` |\n| `nolabel:claim-fo-type-of-a-tuple` | `claim:fo-composition-quantifier-rank` |\n| `nolabel:claim-formula-annotation-regular` | `claim:mso-annotation-regular` |\n| `nolabel:cor-two-way-implies-regular` | `cor:2dfa-computes-all-regular-functions` |\n| `nolabel:def-fo-types` | `def:k-types` |\n| `nolabel:def-left-distance` | `def:left-distance` |\n| `nolabel:def-nfa-with-output` | `def:nfa-with-output` |\n| `nolabel:def-prime-mealy-machines` | `def:prime-mealy-machines` |\n| `nolabel:def-rational-function` | `def:rational-function` |\n| `nolabel:def-semiring` | `def:semiring` |\n| `nolabel:def-two-way-transducer` | `def:two-way-transducer` |\n| `nolabel:def-weighted-automaton` | `def:weighted-automaton` |\n| `nolabel:lem-fo-types-characterisation` | `lem:k-types-fo-equivalence` |\n| `nolabel:lem-mso-to-automaton` | `lem:mso-free-variables` |\n| `nolabel:lem-reverse-and-duplicate-continuous` | `lem:reversal-duplication-continuous` |\n| `nolabel:thm-krohn-rhodes` | `thm:krohn-rhodes` |\n| `nolabel:thm-mealy-among-rational-functions` | `thm:rational-is-mealy-characterisation` |\n\nOne placeholder is correct and must stay: `nolabel:thm-fo-transduction-into-primes`. Theorem C.4.17 -- the one withdrawn at the author's request -- has no `\\label`, so there is nothing for it to be.\n\nThe conjecture had no label when you renamed, and now does: the author has given it `conj:regular-via-weighted-automata`. It is the first `conj:` label in the book, so expect the prefix.\n\nOne of these is not a missing label but a wrong one: `item:fo-types-more-information` is the label of an `\\item` *inside* the statement of the lemma, not the label of the lemma, which is `lem:k-types-properties`. When a result has no label of its own, do not reach for the nearest label in the surrounding text -- that is how a reference comes to point at an enumerate item. Say it has none.\n\n`Labels.lean` is a good piece of work and should stay: an alias per label with `assert_no_sorry` makes the compiler check both the correspondence and the status, which is exactly the sort of claim that otherwise rots. Keep it generated from the tables rather than edited by hand.\n\nThe exercises formalised by the three targets before this one are part of what you are auditing: check `EXERCISES.md` the same way you check `THEOREMS.md`, and confirm that every exercise it calls proved is free of `sorryAx`.\n\n`README.md`, `LABELS.md` and `THEOREMS.md` all refer to `tools/gen_labels.py`, `tools/tex_numbering.py` and `tools/relabel.py`, but there is no `tools/` directory anywhere in the sources. So `Labels.lean` cannot be regenerated as documented, and the last agent to touch it had to edit it by hand. Either write those scripts or stop the documentation promising them; say which you did.",
    "depends_on": {}
  },
  {
    "id": "D1-tools",
    "title": "Part D: prenex form and composition of for-transducers",
    "results": [
      "D.1.3",
      "D.1.4"
    ],
    "lean_names": [
      "forTransducer_prenex",
      "forTransducer_comp"
    ],
    "prompt": "Prove Lemma `lemma:prenex-normal-form` (`Transducers.forTransducer_prenex`: every for-transducer has an equivalent one in prenex form) and Lemma `lem:for-closed-under-composition` (`Transducers.forTransducer_comp`: the for-transducers are closed under composition).\n\nThese two are the tools the rest of Section D.1 is built from, which is why they come first and alone. Section D.1 of the book is in `../polyregular-for.tex`.\n\nPart D is where the least Lean exists: `RequestProject/PartD/` has only `Statements.lean` and `MarkedSquare.lean`. The definitions are all there and should be reused rather than rebuilt -- `ForProg`, `ForTest`, `Holds`, `forLoopRun`, `LoopFree`, `nestLoops`, `OutputsAtMostOne`, `PrenexForm`, `IsForTransducer`, and for the pebble side `Pebble`, `PebbleView`, `viewOf`, `PebbleAction`, `IsPebbleTransducer`. What is missing is the proofs.\n\nUnlike the earlier Part D targets, the whole of Parts A, B and C is now proved and available: in particular Theorem `thm:two-way-transducers-are-regular-functions` and Theorem `thm:sst-are-regular` are no longer blocked, so the regular-function theory is usable in full.",
    "depends_on": {},
    "max_continues": 6
  },
  {
    "id": "D1-equivalence",
    "title": "Part D: polyregular = for-transducers",
    "results": [
      "D.1.1"
    ],
    "lean_names": [
      "polyregular_iff_forTransducer"
    ],
    "prompt": "Prove Theorem `thm:for-transducers-are-polyregular` (`Transducers.polyregular_iff_forTransducer`): the polyregular functions are exactly the functions computed by for-transducers.\n\nThe two lemmas of the previous target, prenex form and closure under composition, should be available and are the main tools, together with marked squaring (`PartD/MarkedSquare.lean`) and the regular-function theory of Part C.\n\nPart D is where the least Lean exists: `RequestProject/PartD/` has only `Statements.lean` and `MarkedSquare.lean`. The definitions are all there and should be reused rather than rebuilt -- `ForProg`, `ForTest`, `Holds`, `forLoopRun`, `LoopFree`, `nestLoops`, `OutputsAtMostOne`, `PrenexForm`, `IsForTransducer`, and for the pebble side `Pebble`, `PebbleView`, `viewOf`, `PebbleAction`, `IsPebbleTransducer`. What is missing is the proofs.\n\nUnlike the earlier Part D targets, the whole of Parts A, B and C is now proved and available: in particular Theorem `thm:two-way-transducers-are-regular-functions` and Theorem `thm:sst-are-regular` are no longer blocked, so the regular-function theory is usable in full.",
    "depends_on": {
      "D.1.3": "forTransducer_prenex",
      "D.1.4": "forTransducer_comp"
    },
    "max_continues": 6
  },
  {
    "id": "D2-continuity",
    "title": "Part D: continuity of pebble transducers",
    "results": [
      "D.2.1"
    ],
    "lean_names": [
      "pebble_continuous"
    ],
    "prompt": "Prove Theorem `thm:pebble-are-continuous` (`Transducers.pebble_continuous`): pebble transducers compute continuous functions.\n\nIt stands on its own -- it does not need the equivalence of Section D.1 -- which is why it is a target of its own and can be attempted independently of the D.1 work. Section D.2 of the book is in `../polyregular-pebble.tex`; `PartD/MarkedSquare.lean` has the continuity machinery used for Theorem `thm:polyregular-functions-are-continuous`.\n\nPart D is where the least Lean exists: `RequestProject/PartD/` has only `Statements.lean` and `MarkedSquare.lean`. The definitions are all there and should be reused rather than rebuilt -- `ForProg`, `ForTest`, `Holds`, `forLoopRun`, `LoopFree`, `nestLoops`, `OutputsAtMostOne`, `PrenexForm`, `IsForTransducer`, and for the pebble side `Pebble`, `PebbleView`, `viewOf`, `PebbleAction`, `IsPebbleTransducer`. What is missing is the proofs.\n\nUnlike the earlier Part D targets, the whole of Parts A, B and C is now proved and available: in particular Theorem `thm:two-way-transducers-are-regular-functions` and Theorem `thm:sst-are-regular` are no longer blocked, so the regular-function theory is usable in full.",
    "depends_on": {},
    "max_continues": 6
  },
  {
    "id": "D2-pebble-equivalence",
    "title": "Part D: pebble transducers = for-transducers",
    "results": [
      "D.2.4"
    ],
    "lean_names": [
      "pebble_iff_forTransducer"
    ],
    "prompt": "Prove Theorem `thm:pebble-are-for` (`Transducers.pebble_iff_forTransducer`): the pebble transducers are equivalent to the for-transducers. This is the last open numbered result of the book.\n\nThe book proves it through the reachability analysis of a pebble automaton: Lemma `lem:reachability-pebble-automaton`, Claim `claim:reachability-basic-run`, Lemma `lem:children-of-configuration-in-pebble-run` and Claims `claim:from-configuration-to-child-configuration-graph` and `claim:from-child-configuration-graph-to-children` are its internal steps, deliberately not formalised as numbered results, so organise them however suits Lean. Theorem `thm:for-transducers-are-polyregular` should be available.\n\nPart D is where the least Lean exists: `RequestProject/PartD/` has only `Statements.lean` and `MarkedSquare.lean`. The definitions are all there and should be reused rather than rebuilt -- `ForProg`, `ForTest`, `Holds`, `forLoopRun`, `LoopFree`, `nestLoops`, `OutputsAtMostOne`, `PrenexForm`, `IsForTransducer`, and for the pebble side `Pebble`, `PebbleView`, `viewOf`, `PebbleAction`, `IsPebbleTransducer`. What is missing is the proofs.\n\nUnlike the earlier Part D targets, the whole of Parts A, B and C is now proved and available: in particular Theorem `thm:two-way-transducers-are-regular-functions` and Theorem `thm:sst-are-regular` are no longer blocked, so the regular-function theory is usable in full.",
    "depends_on": {
      "D.1.1": "polyregular_iff_forTransducer"
    },
    "max_continues": 6
  },
  {
    "id": "exercises-new",
    "title": "Exercises: everything solved since the first pass",
    "results": [],
    "lean_names": [],
    "prompt": "Formalise the exercises of the book whose solutions have been written since the earlier exercise targets ran.\n\nWork out which those are rather than trusting any list here, because the author writes solutions continuously and this description will be out of date by the time you read it. The gap is:\n\n* every `\\exer{statement}{solution}` in the book's `.tex` files whose second argument is non-empty,\n* minus every exercise already recorded in `EXERCISES.md`.\n\nWhen this target was written there were 78 exercises in the book, all 78 with a solution, and 42 of them recorded in `EXERCISES.md` -- so about 36 to do, spread over chapters that the first pass did not reach at all (`2dfa.tex`, `sst.tex`, `weighted.tex`, `myhill-nerode.tex`, `polyregular-for.tex`, `polyregular-pebble.tex`) as well as new ones in the chapters it did.\n\nGo in the order the book states them, so that if you run out of budget the continuation picks up cleanly where you stopped.\n\nThe conventions are the ones the first pass established, and `EXERCISES.md` documents them: a file per chapter under `RequestProject/Exercises/`, auxiliary facts in the matching `*Aux.lean`, an entry in `EXERCISES.md` identified by the exercise's `\\label`, and an alias in `RequestProject/Labels.lean` with `assert_no_sorry` for every exercise that has a label. Every exercise now has one.\n\nThe author's solution is the intended proof, not a hint. Where an exercise is not a mathematical statement -- \"give an example of...\", \"is it necessarily finite?\" -- formalise the concrete claim the solution actually establishes, or leave it out and say which and why. Do not invent a statement in order to have something to prove.\n\nDo not touch the numbered results of the main text.",
    "depends_on": {},
    "max_continues": 8
  },
  {
    "id": "encoding-config-graph",
    "title": "The string encoding of the configuration graph (C.2.3, C.2.4)",
    "results": [
      "C.2.3",
      "C.2.4"
    ],
    "lean_names": [],
    "prompt": "Introduce the book's string encoding of the reachable configuration graph of a two-way transducer, and prove the two lemmas of Section C.2 that are stated about it:\n\n* Lemma `lem:compute-configuration-graph` -- the function taking an input string to the string representation of its reachable configuration graph is rational.\n* Lemma `lem:check-if-output-string-of-configuration-graph-belongs-to-L` -- for a regular language `L`, the set of strings over `C` that represent a reachable configuration graph whose output string lies in `L` is a regular language.\n\nBoth are currently listed in `RequestProject/Labels.lean` as not formalised. They are the two halves of the book's proof that a two-way transducer can be pre-composed with a regular language, and the whole of Section C.2 is written in terms of them.\n\nDefine the alphabet `C` as the book does: bipartite graphs on two copies of the state set, edges directed and labelled by output strings, at most one outgoing edge per vertex going to the other copy, plus the special letter for the empty input. Prove that the strings representing reachable configuration graphs form a regular language -- the book calls this \"the main observation\" -- and build the two lemmas on it.\n\nThe two-way machinery already in `RequestProject/PartC/` (`TwoWay`, `TwoWayRun.lean`, `TwoWayVisit.lean`, `TwoWayAnnot.lean`) is what the encoding should be related to: prove that the encoding agrees with the run semantics already formalised, so that the two views are interchangeable and later targets can use whichever is convenient.\n\nBackground common to this group of targets. The book carries out several proofs by *encoding a run graph as a string over a finite alphabet* and then reasoning about the resulting language with the automata theory already available. Section C.2 says so explicitly (`../2dfa.tex`): to compute the reachable configuration graph \"we need to represent it as a string over a finite alphabet. The idea is to slice the graph into smaller graphs, one for each input letter\", and \"the alphabet for the string representation, call it `C`, consists of bipartite graphs, where the vertices are two copies of `Q`, edges are directed and labelled with output strings, each vertex has at most one outgoing edge, and this edge must go to the other copy of `Q`\", with a special letter for the empty input.\n\nThe formalisation never introduced that alphabet. It reasons about runs directly instead, which does prove the headline theorems but has two costs, and this group of targets is about paying them off:\n\n* every result of the book that is *about* the encoding was skipped. `RequestProject/Labels.lean` lists them at the end, each marked \"not formalised (the string encoding of the configuration graph / configuration encodings used inside proofs)\".\n* several exercises cannot be stated at all without it.\n\nSo: build the encoding the book describes, as a real object in Lean, and use it. Do not restate the existing theorems in terms of it and call that progress -- the point is to close the results that were skipped, keeping everything already proved proved.",
    "depends_on": {},
    "max_continues": 8
  },
  {
    "id": "encoding-snake-alphabet",
    "title": "State the snake lemma over the book's alphabet of snake letters",
    "results": [
      "C.2.12"
    ],
    "lean_names": [],
    "prompt": "Restate and prove Lemma `lem:output-of-snake-graph-is-regular` over the alphabet the book uses, and record the book's form as the formalised one.\n\nThe book states it as: let `C` be the alphabet used to represent snakes with state set `Q` and output alphabet `B`; for every `k` in `{1, …, |Q|}`, the function taking `w` in `C*` to the output of the snake graph it represents (and to the empty string when `w` represents no snake graph of width at most `k`) is regular.\n\nThe formalisation proves something equivalent but differently phrased -- `Transducers.boundedWidth_isRegular`, about the width-`k` output function `TwoWay.widthOut` of a two-way transducer, for every `k`. `THEOREMS.md` records the divergence in as many words: \"Stated for the width-`k` output function `TwoWay.widthOut` of a two-way transducer rather than for an alphabet of snake letters\".\n\nIntroduce the alphabet of snake letters, state the lemma as the book does, and prove it -- reusing `boundedWidth_isRegular` and the `Snake*` development rather than redoing the induction, by relating a snake graph over `C` to the run it encodes. Keep both statements: the book's form is what the label should name, and the existing one stays as the general form it is proved from. Say plainly in your summary whether the two are equivalent or whether the book's version is weaker, and update `THEOREMS.md` so the divergence note is either removed or replaced by an accurate one.\n\nBackground common to this group of targets. The book carries out several proofs by *encoding a run graph as a string over a finite alphabet* and then reasoning about the resulting language with the automata theory already available. Section C.2 says so explicitly (`../2dfa.tex`): to compute the reachable configuration graph \"we need to represent it as a string over a finite alphabet. The idea is to slice the graph into smaller graphs, one for each input letter\", and \"the alphabet for the string representation, call it `C`, consists of bipartite graphs, where the vertices are two copies of `Q`, edges are directed and labelled with output strings, each vertex has at most one outgoing edge, and this edge must go to the other copy of `Q`\", with a special letter for the empty input.\n\nThe formalisation never introduced that alphabet. It reasons about runs directly instead, which does prove the headline theorems but has two costs, and this group of targets is about paying them off:\n\n* every result of the book that is *about* the encoding was skipped. `RequestProject/Labels.lean` lists them at the end, each marked \"not formalised (the string encoding of the configuration graph / configuration encodings used inside proofs)\".\n* several exercises cannot be stated at all without it.\n\nSo: build the encoding the book describes, as a real object in Lean, and use it. Do not restate the existing theorems in terms of it and call that progress -- the point is to close the results that were skipped, keeping everything already proved proved.",
    "depends_on": {},
    "max_continues": 6
  },
  {
    "id": "encoding-pebble",
    "title": "Configuration encodings for pebble transducers (D.2.2, D.2.3, D.2.5, D.2.6, D.2.7)",
    "results": [
      "D.2.2",
      "D.2.3",
      "D.2.5",
      "D.2.6",
      "D.2.7"
    ],
    "lean_names": [],
    "prompt": "Prove the five results of Section D.2 that were skipped because they are about configuration encodings. `RequestProject/Labels.lean` lists each of them at the end as \"not formalised (configuration encodings used inside proofs)\":\n\n* Lemma `lem:reachability-pebble-automaton`\n* Claim `claim:reachability-basic-run`\n* Lemma `lem:children-of-configuration-in-pebble-run`\n* Claim `claim:from-configuration-to-child-configuration-graph`\n* Claim `claim:from-child-configuration-graph-to-children`\n\nSection D.2 is in `../polyregular-pebble.tex`. Theorem `thm:pebble-are-for` and Theorem `thm:pebble-are-continuous` are already proved, by an argument that goes around these lemmas rather than through them; the point here is to formalise the book's own route, so that the numbered results of Section D.2 exist and the two theorems can be seen to follow from them as the book says they do.\n\nIf proving one of them shows the book's statement to be inaccurate as printed, keep a faithful rendering, add the corrected version beside it, and document the discrepancy -- as was done for Claim `claim:sum-of-regular` and Definition `def:aperiodic`.\n\nBackground common to this group of targets. The book carries out several proofs by *encoding a run graph as a string over a finite alphabet* and then reasoning about the resulting language with the automata theory already available. Section C.2 says so explicitly (`../2dfa.tex`): to compute the reachable configuration graph \"we need to represent it as a string over a finite alphabet. The idea is to slice the graph into smaller graphs, one for each input letter\", and \"the alphabet for the string representation, call it `C`, consists of bipartite graphs, where the vertices are two copies of `Q`, edges are directed and labelled with output strings, each vertex has at most one outgoing edge, and this edge must go to the other copy of `Q`\", with a special letter for the empty input.\n\nThe formalisation never introduced that alphabet. It reasons about runs directly instead, which does prove the headline theorems but has two costs, and this group of targets is about paying them off:\n\n* every result of the book that is *about* the encoding was skipped. `RequestProject/Labels.lean` lists them at the end, each marked \"not formalised (the string encoding of the configuration graph / configuration encodings used inside proofs)\".\n* several exercises cannot be stated at all without it.\n\nSo: build the encoding the book describes, as a real object in Lean, and use it. Do not restate the existing theorems in terms of it and call that progress -- the point is to close the results that were skipped, keeping everything already proved proved.",
    "depends_on": {
      "C.2.3": ""
    },
    "max_continues": 8
  },
  {
    "id": "exercises-encoding",
    "title": "Exercises that needed the run-graph encodings",
    "results": [],
    "lean_names": [],
    "prompt": "Formalise the exercises that were left out because the run-graph encodings did not exist. `EXERCISES.md` marks each as not formalised; the ones this group of targets should have unblocked are\n\n* `exer:2dfa-unary-output` -- over a unary output alphabet, regular = rational\n* `exer:2dfa-complexity` -- the shortest accepted string can be exponential in the number of states\n* `exer:2dfa-loop-elimination-sipser` -- a polynomial-size two-way automaton\n* `exer:regular-compression` -- regular functions are compatible with compression\n\nRead `EXERCISES.md` rather than trusting that list: it records, for each unformalised exercise, why it was left out, and by the time you run this some may have been done and others may have become possible. Formalise every exercise whose obstacle was the missing encoding, and say in your summary which of the remaining ones are still blocked and on what.\n\n`exer:2dfa-complexity` carries a note in `EXERCISES.md` saying the book's construction does not prove the claim. Do not paper over that: if it is still true, restate it accurately, keep the author's construction beside it, and say so.\n\nThe exercise conventions are the ones `EXERCISES.md` documents; follow them rather than inventing new ones.\n\nBackground common to this group of targets. The book carries out several proofs by *encoding a run graph as a string over a finite alphabet* and then reasoning about the resulting language with the automata theory already available. Section C.2 says so explicitly (`../2dfa.tex`): to compute the reachable configuration graph \"we need to represent it as a string over a finite alphabet. The idea is to slice the graph into smaller graphs, one for each input letter\", and \"the alphabet for the string representation, call it `C`, consists of bipartite graphs, where the vertices are two copies of `Q`, edges are directed and labelled with output strings, each vertex has at most one outgoing edge, and this edge must go to the other copy of `Q`\", with a special letter for the empty input.\n\nThe formalisation never introduced that alphabet. It reasons about runs directly instead, which does prove the headline theorems but has two costs, and this group of targets is about paying them off:\n\n* every result of the book that is *about* the encoding was skipped. `RequestProject/Labels.lean` lists them at the end, each marked \"not formalised (the string encoding of the configuration graph / configuration encodings used inside proofs)\".\n* several exercises cannot be stated at all without it.\n\nSo: build the encoding the book describes, as a real object in Lean, and use it. Do not restate the existing theorems in terms of it and call that progress -- the point is to close the results that were skipped, keeping everything already proved proved.",
    "depends_on": {},
    "max_continues": 6
  },
  {
    "id": "audit-final",
    "title": "Final audit and write-up of the whole formalisation",
    "results": [],
    "lean_names": [],
    "prompt": "This is the closing task of the run. Do not start proving any new numbered result.\n\n1. `lake build` from scratch; report the result and every remaining warning.\n2. For every numbered result of the book that is formalised, run `#print axioms` and classify it as: proved outright (only `propext`, `Classical.choice`, `Quot.sound`); proved from an explicit hypothesis (say which); or still open (say what remains).\n3. Confirm that no `sorry` remains anywhere in the project except in declarations that `THEOREMS.md` explicitly lists as open, and that no `axiom` has been introduced beyond the conditional hypotheses that are documented as such.\n4. Compare each Lean statement against the book's LaTeX sources one final time and list every place where the formal statement diverges from the book -- corrections, added hypotheses, changed definitions -- with a one-line reason for each.\n5. Rewrite `THEOREMS.md` so that it is a complete and accurate final index: every numbered result, its Lean name, its file, its status, and the divergences from the book collected in one section.\n6. Write a short `FORMALISATION.md` aimed at a reader of the book: what has been formalised, what the conventions are, what was found to be wrong or imprecise in the book and had to be adjusted, and what is left open and why.\n\nReport all of this in your summary as well.\n\nThis is the last target, so it audits the exercises too, including the ones formalised after Part D was finished: check `EXERCISES.md` as you check `THEOREMS.md`, and confirm that every exercise it calls proved is free of `sorryAx`. Report any exercise of the book that has a written solution and no formalisation, so the author knows what is left rather than having to look for it.",
    "depends_on": {}
  },
  {
    "id": "hyp-forward-for",
    "title": "Discharge the three hypotheses of the forward for-transducers",
    "results": [],
    "lean_names": [],
    "depends_on": {},
    "prompt": "Discharge the three hypotheses of `RequestProject/Exercises/ForwardForTop.lean`, so that Exercise `exer:forward-for-transducer` -- forward for-transducers compute exactly the compositions of marked squaring and rational functions -- is proved outright:\n\n* `ForwardForClosedUnderComp` -- forward for-transducers are closed under composition.\n* `ForwardPrenexNormalForm` -- every forward for-program is equivalent to one in prenex form all of whose loops run first-to-last.\n* `ForwardStepRational` -- the one-step transducer `stepFun true A k` of the enumeration is a rational function.\n\nThese are the forward-direction versions of results the project already proves for general for-transducers: Lemma `lem:for-closed-under-composition` and Lemma `lemma:prenex-normal-form`. Start by reading those two proofs. The work should be to carry the direction of each loop through them: the existing constructions build the composed or the normalised program out of the loops of the originals, so what has to be checked is that they never introduce a last-to-first loop when they were given only first-to-last ones. If that turns out to be false for the construction as written, say so and give the counterexample -- that is a real finding about the book's proof, not a failure of the target.\n\n`ForwardStepRational` is a self-contained statement about a concrete function defined in the same file, and the project already has what is needed to show that an append-only sequential rewriting is rational.\n\n--- Background common to this group of targets. ---\n\nEvery numbered result and every exercise of the book is now formalised, the build\nis clean, and no declaration depends on `sorry`. What is left is a different kind\nof gap: a number of results are proved *from an explicit hypothesis* -- a\n`def ... : Prop` passed as an argument to the theorem and never proved.\n`EXERCISES.md` and `THEOREMS.md` say so in the status column, and the docstring of\neach hypothesis says why it was assumed rather than proved.\n\nThe author has now asked for these hypotheses to be discharged, so that the\nresults are proved outright. This group of targets does that, a file at a time.\n\nWhat \"discharge a hypothesis\" means here, precisely:\n\n* Prove the `Prop` as an ordinary theorem of the project.\n* Remove it from the argument list of every declaration that takes it, so those\n  declarations become unconditional. Their conclusions must stay exactly as they\n  are. Removing a hypothesis by weakening the conclusion, by restricting the\n  statement, or by introducing a different assumption somewhere else is not\n  discharging it. If you find you have to do one of those, stop and report it.\n* The `def ... : Prop` may then be deleted, or kept with the same statement as a\n  `theorem`. If you keep the name, make it a theorem, not a def.\n* Update `RequestProject/Labels.lean` (the alias docstring, and `assert_no_sorry`),\n  `EXERCISES.md` and `THEOREMS.md`: the status column must no longer say \"from the\n  hypothesis ...\".\n\nThree assumptions are deliberately out of scope for every target in this group and\nmust be left exactly as they are:\n\n* `Transducers.Exercises.EFSuccSeparation` (Exercise `exer:fo-suc`) -- the\n  Ehrenfeucht-Fraisse argument. The author will deal with this one himself.\n* `Transducers.Exercises.IteratesReduction` (Exercise\n  `exer:rational-composition-finiteness-undecidable`) -- the reduction from the\n  halting problem, which would need Turing machines formalised.\n* `not ComputablePred Transducers.PCP.Solvable` -- the undecidability of the Post\n  correspondence problem, which the book itself takes as given.\n\nIf you cannot discharge a hypothesis this target names, do not fake it. Leave it\nin place and say in your summary exactly what is missing and how large it looks. A\nprecise account of a real obstacle is a good outcome. A theorem that looks\nunconditional because its content moved into a new assumption is not.\n",
    "max_continues": 6,
    "discharges": [
      "ForwardForClosedUnderComp",
      "ForwardPrenexNormalForm",
      "ForwardStepRational"
    ]
  },
  {
    "id": "hyp-minimal-bimachine",
    "title": "Discharge the canonical suffix bimachine of exer:minimal-bimachine-lexicographic",
    "results": [],
    "lean_names": [],
    "depends_on": {},
    "prompt": "Discharge `CanonicalSuffixBimachineExists` in `RequestProject/Exercises/MinimalBimachine.lean`, so that Exercise `exer:minimal-bimachine-lexicographic` is proved outright.\n\nThe hypothesis says that for a rational function `f` there is a bimachine computing `f` whose suffix automaton is suffix-reachable and has exactly `Nat.card (ClassSet f)` states -- that is, that the lower bound `card_classSet_le`, which the file already proves, is attained.\n\nIts docstring records why it was assumed: the project proves Theorem `thm:machine-independent-rational-functions` only in the direction used elsewhere, and does not expose the two-step decomposition of its proof, from which the suffix automaton of `~`-classes can be read off. So the work is to expose it. Build the bimachine directly -- the suffix states are the `~`-classes, the prefix states are the states of the subsequential transducer after an annotated prefix -- and prove that it computes `f`, that it is suffix-reachable, and that it has the right number of suffix states. The docstring sketches why the prefix state is well defined: the annotations can be recovered from the class, `~` being a left congruence.\n\nDo not change `MinimalSuffixBimachine`, `SuffixReachable` or `ClassSet`. The uniqueness half of the exercise, which is the half the exercise actually asks about, is already proved against them.\n\n--- Background common to this group of targets. ---\n\nEvery numbered result and every exercise of the book is now formalised, the build\nis clean, and no declaration depends on `sorry`. What is left is a different kind\nof gap: a number of results are proved *from an explicit hypothesis* -- a\n`def ... : Prop` passed as an argument to the theorem and never proved.\n`EXERCISES.md` and `THEOREMS.md` say so in the status column, and the docstring of\neach hypothesis says why it was assumed rather than proved.\n\nThe author has now asked for these hypotheses to be discharged, so that the\nresults are proved outright. This group of targets does that, a file at a time.\n\nWhat \"discharge a hypothesis\" means here, precisely:\n\n* Prove the `Prop` as an ordinary theorem of the project.\n* Remove it from the argument list of every declaration that takes it, so those\n  declarations become unconditional. Their conclusions must stay exactly as they\n  are. Removing a hypothesis by weakening the conclusion, by restricting the\n  statement, or by introducing a different assumption somewhere else is not\n  discharging it. If you find you have to do one of those, stop and report it.\n* The `def ... : Prop` may then be deleted, or kept with the same statement as a\n  `theorem`. If you keep the name, make it a theorem, not a def.\n* Update `RequestProject/Labels.lean` (the alias docstring, and `assert_no_sorry`),\n  `EXERCISES.md` and `THEOREMS.md`: the status column must no longer say \"from the\n  hypothesis ...\".\n\nThree assumptions are deliberately out of scope for every target in this group and\nmust be left exactly as they are:\n\n* `Transducers.Exercises.EFSuccSeparation` (Exercise `exer:fo-suc`) -- the\n  Ehrenfeucht-Fraisse argument. The author will deal with this one himself.\n* `Transducers.Exercises.IteratesReduction` (Exercise\n  `exer:rational-composition-finiteness-undecidable`) -- the reduction from the\n  halting problem, which would need Turing machines formalised.\n* `not ComputablePred Transducers.PCP.Solvable` -- the undecidability of the Post\n  correspondence problem, which the book itself takes as given.\n\nIf you cannot discharge a hypothesis this target names, do not fake it. Leave it\nin place and say in your summary exactly what is missing and how large it looks. A\nprecise account of a real obstacle is a good outcome. A theorem that looks\nunconditional because its content moved into a new assumption is not.\n",
    "max_continues": 5,
    "discharges": [
      "CanonicalSuffixBimachineExists"
    ]
  },
  {
    "id": "hyp-even-parity",
    "title": "Discharge the two-state case analysis of exer:non-minimal-automaton",
    "results": [],
    "lean_names": [],
    "depends_on": {},
    "prompt": "Discharge `EvenParityNeedsThreeStates` in `RequestProject/Exercises/MinimalTransducer.lean`, so that Exercise `exer:non-minimal-automaton` is proved outright.\n\nThe hypothesis is `MinimalUnambiguousSize evenParity 3`: every unambiguous transducer computing `evenParity` has at least three states. The runs over the inputs `a^0` and `a^1` being disjoint is already proved; what the book leaves to the reader is the case analysis over unambiguous transducers with at most two states.\n\nNote that this family is not finite -- the transitions may read and write arbitrary strings -- so the argument has to be a proof about runs, not a finite check. `MinimalUnambiguousSize` quantifies over all finite state types, so what has to be proved is: for every finite `Q` with `Nat.card Q <= 2` there is no unambiguous `NFAO A B Q` whose relation is the graph of `evenParity`.\n\nEverything else in the exercise is already proved. Do not restate it.\n\n--- Background common to this group of targets. ---\n\nEvery numbered result and every exercise of the book is now formalised, the build\nis clean, and no declaration depends on `sorry`. What is left is a different kind\nof gap: a number of results are proved *from an explicit hypothesis* -- a\n`def ... : Prop` passed as an argument to the theorem and never proved.\n`EXERCISES.md` and `THEOREMS.md` say so in the status column, and the docstring of\neach hypothesis says why it was assumed rather than proved.\n\nThe author has now asked for these hypotheses to be discharged, so that the\nresults are proved outright. This group of targets does that, a file at a time.\n\nWhat \"discharge a hypothesis\" means here, precisely:\n\n* Prove the `Prop` as an ordinary theorem of the project.\n* Remove it from the argument list of every declaration that takes it, so those\n  declarations become unconditional. Their conclusions must stay exactly as they\n  are. Removing a hypothesis by weakening the conclusion, by restricting the\n  statement, or by introducing a different assumption somewhere else is not\n  discharging it. If you find you have to do one of those, stop and report it.\n* The `def ... : Prop` may then be deleted, or kept with the same statement as a\n  `theorem`. If you keep the name, make it a theorem, not a def.\n* Update `RequestProject/Labels.lean` (the alias docstring, and `assert_no_sorry`),\n  `EXERCISES.md` and `THEOREMS.md`: the status column must no longer say \"from the\n  hypothesis ...\".\n\nThree assumptions are deliberately out of scope for every target in this group and\nmust be left exactly as they are:\n\n* `Transducers.Exercises.EFSuccSeparation` (Exercise `exer:fo-suc`) -- the\n  Ehrenfeucht-Fraisse argument. The author will deal with this one himself.\n* `Transducers.Exercises.IteratesReduction` (Exercise\n  `exer:rational-composition-finiteness-undecidable`) -- the reduction from the\n  halting problem, which would need Turing machines formalised.\n* `not ComputablePred Transducers.PCP.Solvable` -- the undecidability of the Post\n  correspondence problem, which the book itself takes as given.\n\nIf you cannot discharge a hypothesis this target names, do not fake it. Leave it\nin place and say in your summary exactly what is missing and how large it looks. A\nprecise account of a real obstacle is a good outcome. A theorem that looks\nunconditional because its content moved into a new assumption is not.\n",
    "max_continues": 5,
    "discharges": [
      "EvenParityNeedsThreeStates"
    ]
  },
  {
    "id": "hyp-growth-dichotomy",
    "title": "The loop analysis: growth dichotomy and the full ideal",
    "results": [],
    "lean_names": [],
    "depends_on": {},
    "prompt": "Build the loop analysis of transducers that the four hypotheses of `RequestProject/Exercises/Ideals.lean` rest on, and discharge the first two:\n\n* `OutputsGrowthDichotomy` -- the number of outputs of a rational function on inputs of length at most `n` is either super-polynomial or `Theta(n^k)` for some `k`. This is the growth gap theorem for regular languages.\n* `IdentityFromSuperPolyOutputs` -- a rational function with super-polynomially many outputs has the identity of `{0,1}*` as a rational pre- and post-composition. Discharging it closes Exercise `exer:full-ideal`.\n\nThe author's solution, quoted in the docstrings, runs: the range of `f` is a regular language of super-polynomial growth, so an automaton for it has two loops with different output strings of the same length; that gives an injective encoding of `{0,1}*` inside the range; the Uniformisation Lemma turns the inverse of `f` into a rational function producing a preimage; and a rational function reads the encoded bits back.\n\nThe Uniformisation Lemma is already in the project as `Transducers.exists_rationalFun_of_total_rel`, and `lem:uniformisation` is proved. What is missing is the loop analysis itself: that a regular language whose growth is not polynomial has two loops of equal length with different labels, on a common state, reachable and co-reachable. Build that once, in a file of its own, in a form the next targets can reuse -- the same analysis is what `SortedFromOmegaOutputs`, `FactorThroughSortedOfOutputsPoly` and `RationalHasLinearRate` need, and those are the next two targets. Exercise `exer:polynomial-image-growth-decidable`, which the docstrings point at, is already formalised: read it first, some of this may already be there.\n\nLeave `SortedFromOmegaOutputs` and `FactorThroughSortedOfOutputsPoly` alone unless they fall out of the machinery; the next target is about them.\n\n--- Background common to this group of targets. ---\n\nEvery numbered result and every exercise of the book is now formalised, the build\nis clean, and no declaration depends on `sorry`. What is left is a different kind\nof gap: a number of results are proved *from an explicit hypothesis* -- a\n`def ... : Prop` passed as an argument to the theorem and never proved.\n`EXERCISES.md` and `THEOREMS.md` say so in the status column, and the docstring of\neach hypothesis says why it was assumed rather than proved.\n\nThe author has now asked for these hypotheses to be discharged, so that the\nresults are proved outright. This group of targets does that, a file at a time.\n\nWhat \"discharge a hypothesis\" means here, precisely:\n\n* Prove the `Prop` as an ordinary theorem of the project.\n* Remove it from the argument list of every declaration that takes it, so those\n  declarations become unconditional. Their conclusions must stay exactly as they\n  are. Removing a hypothesis by weakening the conclusion, by restricting the\n  statement, or by introducing a different assumption somewhere else is not\n  discharging it. If you find you have to do one of those, stop and report it.\n* The `def ... : Prop` may then be deleted, or kept with the same statement as a\n  `theorem`. If you keep the name, make it a theorem, not a def.\n* Update `RequestProject/Labels.lean` (the alias docstring, and `assert_no_sorry`),\n  `EXERCISES.md` and `THEOREMS.md`: the status column must no longer say \"from the\n  hypothesis ...\".\n\nThree assumptions are deliberately out of scope for every target in this group and\nmust be left exactly as they are:\n\n* `Transducers.Exercises.EFSuccSeparation` (Exercise `exer:fo-suc`) -- the\n  Ehrenfeucht-Fraisse argument. The author will deal with this one himself.\n* `Transducers.Exercises.IteratesReduction` (Exercise\n  `exer:rational-composition-finiteness-undecidable`) -- the reduction from the\n  halting problem, which would need Turing machines formalised.\n* `not ComputablePred Transducers.PCP.Solvable` -- the undecidability of the Post\n  correspondence problem, which the book itself takes as given.\n\nIf you cannot discharge a hypothesis this target names, do not fake it. Leave it\nin place and say in your summary exactly what is missing and how large it looks. A\nprecise account of a real obstacle is a good outcome. A theorem that looks\nunconditional because its content moved into a new assumption is not.\n",
    "max_continues": 8,
    "discharges": [
      "IdentityFromSuperPolyOutputs",
      "OutputsGrowthDichotomy"
    ]
  },
  {
    "id": "hyp-polynomial-ideals",
    "title": "Discharge the remaining two ideal hypotheses",
    "results": [],
    "lean_names": [],
    "depends_on": {},
    "prompt": "Discharge the remaining two hypotheses of `RequestProject/Exercises/Ideals.lean`, so that Exercises `exer:polynomial-ideals`, `exer:all-ideals` and `exer:decide-same-ideal` are proved outright:\n\n* `SortedFromOmegaOutputs` -- a rational function with `Omega(n^k)` outputs has `sortedFun k` as a rational pre- and post-composition.\n* `FactorThroughSortedOfOutputsPoly` -- a rational function with `O(n^k)` outputs factors as a rational pre- and post-composition of `sortedFun k`.\n\nThe first is the same argument as `IdentityFromSuperPolyOutputs`, with a `k`-pattern in the range in place of the two loops. The second is what the author describes as \"analysing the structure of strongly connected components in the automaton that computes a rational function whose range has growth `O(n^k)`\", and is the harder of the two.\n\nThe previous target, `hyp-growth-dichotomy`, was asked to leave the loop analysis in a reusable form. Read what it produced before you start, and reuse it; if it did not get that far, build what you need and say so in your summary.\n\nOnce all four hypotheses of the file are gone, `exer:all-ideals` and `exer:decide-same-ideal` become unconditional too, since they rest on nothing else. Check that, and update their rows.\n\n--- Background common to this group of targets. ---\n\nEvery numbered result and every exercise of the book is now formalised, the build\nis clean, and no declaration depends on `sorry`. What is left is a different kind\nof gap: a number of results are proved *from an explicit hypothesis* -- a\n`def ... : Prop` passed as an argument to the theorem and never proved.\n`EXERCISES.md` and `THEOREMS.md` say so in the status column, and the docstring of\neach hypothesis says why it was assumed rather than proved.\n\nThe author has now asked for these hypotheses to be discharged, so that the\nresults are proved outright. This group of targets does that, a file at a time.\n\nWhat \"discharge a hypothesis\" means here, precisely:\n\n* Prove the `Prop` as an ordinary theorem of the project.\n* Remove it from the argument list of every declaration that takes it, so those\n  declarations become unconditional. Their conclusions must stay exactly as they\n  are. Removing a hypothesis by weakening the conclusion, by restricting the\n  statement, or by introducing a different assumption somewhere else is not\n  discharging it. If you find you have to do one of those, stop and report it.\n* The `def ... : Prop` may then be deleted, or kept with the same statement as a\n  `theorem`. If you keep the name, make it a theorem, not a def.\n* Update `RequestProject/Labels.lean` (the alias docstring, and `assert_no_sorry`),\n  `EXERCISES.md` and `THEOREMS.md`: the status column must no longer say \"from the\n  hypothesis ...\".\n\nThree assumptions are deliberately out of scope for every target in this group and\nmust be left exactly as they are:\n\n* `Transducers.Exercises.EFSuccSeparation` (Exercise `exer:fo-suc`) -- the\n  Ehrenfeucht-Fraisse argument. The author will deal with this one himself.\n* `Transducers.Exercises.IteratesReduction` (Exercise\n  `exer:rational-composition-finiteness-undecidable`) -- the reduction from the\n  halting problem, which would need Turing machines formalised.\n* `not ComputablePred Transducers.PCP.Solvable` -- the undecidability of the Post\n  correspondence problem, which the book itself takes as given.\n\nIf you cannot discharge a hypothesis this target names, do not fake it. Leave it\nin place and say in your summary exactly what is missing and how large it looks. A\nprecise account of a real obstacle is a good outcome. A theorem that looks\nunconditional because its content moved into a new assumption is not.\n",
    "max_continues": 8,
    "discharges": [
      "SortedFromOmegaOutputs"
    ]
  },
  {
    "id": "hyp-linear-rate",
    "title": "Discharge the maximum cycle mean hypothesis",
    "results": [],
    "lean_names": [],
    "depends_on": {},
    "prompt": "Discharge `RationalHasLinearRate` in `RequestProject/Exercises/LinearOutput.lean`, so that Exercises `exer:rational-outpus-of-exactly-linear-size`, `exer:rational-outpus-of-exactly-linear-size-rational-number` and `exer:regular-outpus-of-exactly-linear-size` are proved outright.\n\nThe hypothesis says that every rational function `f` has a linear rate: there are `p q C` with `q > 0` such that `q * maxOutLen f n <= p * n + C` and `p * n <= q * maxOutLen f n + C` for every `n`. This is the maximum cycle mean of the transducer: weight each transition by the length of the string it writes, and the maximal output length on inputs of length at most `n` is `mu * n` up to an additive constant, where `mu` is the largest mean weight of a cycle.\n\nThe upper bound is the easy direction. The lower bound needs a cycle attaining the maximum to be both reachable and co-reachable, which is where the loop analysis left behind by `hyp-growth-dichotomy` should help -- read it before building anything of your own. `p` and `q` come out as the numerator and denominator of `mu`, which is exactly why the limit the exercise asks about is rational.\n\nThe file already proves the analytic half from the hypothesis, so the hypothesis itself is all that is needed.\n\n--- Background common to this group of targets. ---\n\nEvery numbered result and every exercise of the book is now formalised, the build\nis clean, and no declaration depends on `sorry`. What is left is a different kind\nof gap: a number of results are proved *from an explicit hypothesis* -- a\n`def ... : Prop` passed as an argument to the theorem and never proved.\n`EXERCISES.md` and `THEOREMS.md` say so in the status column, and the docstring of\neach hypothesis says why it was assumed rather than proved.\n\nThe author has now asked for these hypotheses to be discharged, so that the\nresults are proved outright. This group of targets does that, a file at a time.\n\nWhat \"discharge a hypothesis\" means here, precisely:\n\n* Prove the `Prop` as an ordinary theorem of the project.\n* Remove it from the argument list of every declaration that takes it, so those\n  declarations become unconditional. Their conclusions must stay exactly as they\n  are. Removing a hypothesis by weakening the conclusion, by restricting the\n  statement, or by introducing a different assumption somewhere else is not\n  discharging it. If you find you have to do one of those, stop and report it.\n* The `def ... : Prop` may then be deleted, or kept with the same statement as a\n  `theorem`. If you keep the name, make it a theorem, not a def.\n* Update `RequestProject/Labels.lean` (the alias docstring, and `assert_no_sorry`),\n  `EXERCISES.md` and `THEOREMS.md`: the status column must no longer say \"from the\n  hypothesis ...\".\n\nThree assumptions are deliberately out of scope for every target in this group and\nmust be left exactly as they are:\n\n* `Transducers.Exercises.EFSuccSeparation` (Exercise `exer:fo-suc`) -- the\n  Ehrenfeucht-Fraisse argument. The author will deal with this one himself.\n* `Transducers.Exercises.IteratesReduction` (Exercise\n  `exer:rational-composition-finiteness-undecidable`) -- the reduction from the\n  halting problem, which would need Turing machines formalised.\n* `not ComputablePred Transducers.PCP.Solvable` -- the undecidability of the Post\n  correspondence problem, which the book itself takes as given.\n\nIf you cannot discharge a hypothesis this target names, do not fake it. Leave it\nin place and say in your summary exactly what is missing and how large it looks. A\nprecise account of a real obstacle is a good outcome. A theorem that looks\nunconditional because its content moved into a new assumption is not.\n",
    "max_continues": 7,
    "discharges": [
      "RationalHasLinearRate"
    ]
  },
  {
    "id": "hyp-fo-non-elementary",
    "title": "Discharge the non-elementary succinctness construction",
    "results": [],
    "lean_names": [],
    "depends_on": {},
    "prompt": "Discharge `FirstStringOfOrderDefinable` in `RequestProject/Exercises/FONonElementary.lean`, so that Exercises `exer:fo-non-elementary` and `exer:for-transducer-continuity-nonelementary` are proved outright.\n\nThe hypothesis: there is a polynomially bounded `p` such that for every `n` there is a finite alphabet, a first-order sentence `phi` with `fsize phi <= p n`, and a string `w` of length `lenOrder n`, such that `w` is the unique model of `phi`.\n\nIts docstring records the construction the book sketches and leaves to the reader: an induction on `n` building a formula `phi_n (x1, x2, y1, y2)` saying that the two infixes marked by the two pairs of positions are strings of order `n` consecutive in the lexicographic order. The step uses `phi_{n-1}` on the sub-infixes of order `n-1` carrying the same index, and expresses that their bit sequences are consecutive binary numbers. The size stays polynomial by the standard trick: apply a single copy of `phi_{n-1}` to universally quantified arguments, so the size grows by a constant at each step.\n\nThis is the one hypothesis in scope where the book itself does not give the argument, and it is genuinely combinatorial. Set the induction up properly before proving anything: an alphabet indexed by `n`, `phi_n` as a formula with four free position variables, and the correctness statement carried along with the construction. What the exercise then derives from it -- that the unique string is at least `exp n` long -- is already proved in the file.\n\nIf the polynomial size bound turns out to need a different notion of formula size from the `fsize` the file uses, say so rather than adjusting `fsize` to fit. `fsize` is what makes the exercise's claim mean anything.\n\n--- Background common to this group of targets. ---\n\nEvery numbered result and every exercise of the book is now formalised, the build\nis clean, and no declaration depends on `sorry`. What is left is a different kind\nof gap: a number of results are proved *from an explicit hypothesis* -- a\n`def ... : Prop` passed as an argument to the theorem and never proved.\n`EXERCISES.md` and `THEOREMS.md` say so in the status column, and the docstring of\neach hypothesis says why it was assumed rather than proved.\n\nThe author has now asked for these hypotheses to be discharged, so that the\nresults are proved outright. This group of targets does that, a file at a time.\n\nWhat \"discharge a hypothesis\" means here, precisely:\n\n* Prove the `Prop` as an ordinary theorem of the project.\n* Remove it from the argument list of every declaration that takes it, so those\n  declarations become unconditional. Their conclusions must stay exactly as they\n  are. Removing a hypothesis by weakening the conclusion, by restricting the\n  statement, or by introducing a different assumption somewhere else is not\n  discharging it. If you find you have to do one of those, stop and report it.\n* The `def ... : Prop` may then be deleted, or kept with the same statement as a\n  `theorem`. If you keep the name, make it a theorem, not a def.\n* Update `RequestProject/Labels.lean` (the alias docstring, and `assert_no_sorry`),\n  `EXERCISES.md` and `THEOREMS.md`: the status column must no longer say \"from the\n  hypothesis ...\".\n\nThree assumptions are deliberately out of scope for every target in this group and\nmust be left exactly as they are:\n\n* `Transducers.Exercises.EFSuccSeparation` (Exercise `exer:fo-suc`) -- the\n  Ehrenfeucht-Fraisse argument. The author will deal with this one himself.\n* `Transducers.Exercises.IteratesReduction` (Exercise\n  `exer:rational-composition-finiteness-undecidable`) -- the reduction from the\n  halting problem, which would need Turing machines formalised.\n* `not ComputablePred Transducers.PCP.Solvable` -- the undecidability of the Post\n  correspondence problem, which the book itself takes as given.\n\nIf you cannot discharge a hypothesis this target names, do not fake it. Leave it\nin place and say in your summary exactly what is missing and how large it looks. A\nprecise account of a real obstacle is a good outcome. A theorem that looks\nunconditional because its content moved into a new assumption is not.\n",
    "max_continues": 8,
    "discharges": [
      "FirstStringOfOrderDefinable"
    ]
  },
  {
    "id": "hyp-effective-arithmetic",
    "title": "Remove the effectivity hypotheses blocked on computable arithmetic",
    "results": [],
    "lean_names": [],
    "depends_on": {},
    "prompt": "Remove the effectivity hypotheses that are blocked on Mathlib's `Primrec`/`Computable` API rather than on mathematics:\n\n* `Transducers.EffectiveWeightedEvalEq` (`RequestProject/PartB/Effective.lean`) -- a computable equality test for the values of two coded weighted automata over the rationals. Four numbered results rest on it: Theorems `thm:equivalence-weighted-automata`, `thm:equivalence-rational-functions`, `thm:zeroness-weighted-automata` and `thm:decide-if-mealy`.\n* `Transducers.EffectiveTwoWayEvalEq` and `Transducers.EffectiveTwoWayBound` (`RequestProject/PartC/EffectiveReg.lean`) -- the same for two-way transducers. Theorem `thm:decidable-equivalence-regular` rests on both. `RequestProject/PartC/RegCodeBound.lean` says the mathematical content of the bound is already there and that only the computability of the bound as a function of the codes is missing; check that, and if it is right this one is bookkeeping.\n* `Transducers.Exercises.ComputableDiagonalTest` (`RequestProject/Exercises/LengthCollision.lean`) -- that the concrete structurally recursive procedure `meetsDiag` is `Computable`.\n\nThe docstrings all give the same reason: Mathlib's `Computable` API has no arithmetic on the integers or the rationals, so a procedure that adds and compares rationals cannot be shown computable even though it plainly is. That is a gap in the library rather than in the mathematics, and it is the kind of gap that can be filled -- both types are `Encodable`, and the operations here are primitive recursive in the encodings.\n\nSo: build the missing `Primrec`/`Computable` lemmas for the arithmetic these procedures use -- addition, multiplication, and decidable equality on the integers and the rationals, plus whatever list and matrix operations sit on top of them -- and then discharge the hypotheses. Put the library part in a file of its own, clearly marked as general-purpose, since none of it is about transducers.\n\nCheck first whether Mathlib has grown these instances since the docstrings were written; if it has, use them rather than rebuilding them. If, after a serious attempt, some part genuinely cannot be expressed, leave that one hypothesis in place and replace its docstring with a precise account: which operation, which Mathlib declaration is missing, and what would have to exist for it to work. A sharp statement of the obstacle is worth a great deal more than a vague one.\n\n--- Background common to this group of targets. ---\n\nEvery numbered result and every exercise of the book is now formalised, the build\nis clean, and no declaration depends on `sorry`. What is left is a different kind\nof gap: a number of results are proved *from an explicit hypothesis* -- a\n`def ... : Prop` passed as an argument to the theorem and never proved.\n`EXERCISES.md` and `THEOREMS.md` say so in the status column, and the docstring of\neach hypothesis says why it was assumed rather than proved.\n\nThe author has now asked for these hypotheses to be discharged, so that the\nresults are proved outright. This group of targets does that, a file at a time.\n\nWhat \"discharge a hypothesis\" means here, precisely:\n\n* Prove the `Prop` as an ordinary theorem of the project.\n* Remove it from the argument list of every declaration that takes it, so those\n  declarations become unconditional. Their conclusions must stay exactly as they\n  are. Removing a hypothesis by weakening the conclusion, by restricting the\n  statement, or by introducing a different assumption somewhere else is not\n  discharging it. If you find you have to do one of those, stop and report it.\n* The `def ... : Prop` may then be deleted, or kept with the same statement as a\n  `theorem`. If you keep the name, make it a theorem, not a def.\n* Update `RequestProject/Labels.lean` (the alias docstring, and `assert_no_sorry`),\n  `EXERCISES.md` and `THEOREMS.md`: the status column must no longer say \"from the\n  hypothesis ...\".\n\nThree assumptions are deliberately out of scope for every target in this group and\nmust be left exactly as they are:\n\n* `Transducers.Exercises.EFSuccSeparation` (Exercise `exer:fo-suc`) -- the\n  Ehrenfeucht-Fraisse argument. The author will deal with this one himself.\n* `Transducers.Exercises.IteratesReduction` (Exercise\n  `exer:rational-composition-finiteness-undecidable`) -- the reduction from the\n  halting problem, which would need Turing machines formalised.\n* `not ComputablePred Transducers.PCP.Solvable` -- the undecidability of the Post\n  correspondence problem, which the book itself takes as given.\n\nIf you cannot discharge a hypothesis this target names, do not fake it. Leave it\nin place and say in your summary exactly what is missing and how large it looks. A\nprecise account of a real obstacle is a good outcome. A theorem that looks\nunconditional because its content moved into a new assumption is not.\n",
    "max_continues": 8,
    "discharges": [
      "EffectiveWeightedEvalEq",
      "EffectiveTwoWayEvalEq",
      "EffectiveTwoWayBound",
      "ComputableDiagonalTest"
    ]
  },
  {
    "id": "hyp-effective-section",
    "title": "The two remaining effectivity hypotheses: uniformisation on codes, and Parikh",
    "results": [],
    "lean_names": [],
    "depends_on": {},
    "prompt": "The last two effectivity hypotheses. Both are harder than the arithmetic of the previous target, and either may be more than one run's work.\n\n* `Transducers.Exercises.EffectiveRationalSection` (`RequestProject/Exercises/RatInjectiveDec.lean`) -- a computable operation on `RelCode`s implementing the Uniformisation Lemma: from a code describing a functional relation, a code for a section of it. Exercise `exer:rational-injectivity-decidable` rests on it. The project proves uniformisation semantically, as `Transducers.exists_rationalFun_of_total_rel`, but by a choice argument over the runs of the automaton, which is why it yields no construction on codes. Redoing that proof as an explicit construction on automata, and showing the construction primitive recursive on codes, is the work.\n* `Transducers.Exercises.EffectiveLengthPairsSemilinear` (`RequestProject/Exercises/LengthCollision.lean`) -- Parikh's theorem, effectively: a computable map from a pair of codes of rational functions to a semilinear description of the set of pairs of output lengths. Item (b) of Exercise `exer:decide-rational-colision` rests on it. Neither Parikh images nor semilinear sets exist in this project; check what Mathlib has before building either.\n\nTake them in that order. If you run out of budget, leave the repository building and say precisely where you got to -- the driver will continue you. If one of them is genuinely out of reach, say so with the same precision the previous target was asked for.\n\n--- Background common to this group of targets. ---\n\nEvery numbered result and every exercise of the book is now formalised, the build\nis clean, and no declaration depends on `sorry`. What is left is a different kind\nof gap: a number of results are proved *from an explicit hypothesis* -- a\n`def ... : Prop` passed as an argument to the theorem and never proved.\n`EXERCISES.md` and `THEOREMS.md` say so in the status column, and the docstring of\neach hypothesis says why it was assumed rather than proved.\n\nThe author has now asked for these hypotheses to be discharged, so that the\nresults are proved outright. This group of targets does that, a file at a time.\n\nWhat \"discharge a hypothesis\" means here, precisely:\n\n* Prove the `Prop` as an ordinary theorem of the project.\n* Remove it from the argument list of every declaration that takes it, so those\n  declarations become unconditional. Their conclusions must stay exactly as they\n  are. Removing a hypothesis by weakening the conclusion, by restricting the\n  statement, or by introducing a different assumption somewhere else is not\n  discharging it. If you find you have to do one of those, stop and report it.\n* The `def ... : Prop` may then be deleted, or kept with the same statement as a\n  `theorem`. If you keep the name, make it a theorem, not a def.\n* Update `RequestProject/Labels.lean` (the alias docstring, and `assert_no_sorry`),\n  `EXERCISES.md` and `THEOREMS.md`: the status column must no longer say \"from the\n  hypothesis ...\".\n\nThree assumptions are deliberately out of scope for every target in this group and\nmust be left exactly as they are:\n\n* `Transducers.Exercises.EFSuccSeparation` (Exercise `exer:fo-suc`) -- the\n  Ehrenfeucht-Fraisse argument. The author will deal with this one himself.\n* `Transducers.Exercises.IteratesReduction` (Exercise\n  `exer:rational-composition-finiteness-undecidable`) -- the reduction from the\n  halting problem, which would need Turing machines formalised.\n* `not ComputablePred Transducers.PCP.Solvable` -- the undecidability of the Post\n  correspondence problem, which the book itself takes as given.\n\nIf you cannot discharge a hypothesis this target names, do not fake it. Leave it\nin place and say in your summary exactly what is missing and how large it looks. A\nprecise account of a real obstacle is a good outcome. A theorem that looks\nunconditional because its content moved into a new assumption is not.\n",
    "max_continues": 8,
    "discharges": [
      "EffectiveRationalSection",
      "EffectiveLengthPairsSemilinear"
    ]
  },
  {
    "id": "audit-conditionals",
    "title": "Closing audit of the hypothesis-discharging group",
    "results": [],
    "lean_names": [],
    "depends_on": {},
    "prompt": "Closing audit for this group of targets. Do not start new mathematics.\n\n1. `lake build` from scratch. Report the job count, the errors and the warnings.\n2. Enumerate every `def ... : Prop` in `RequestProject/` that is used as a hypothesis of a result or an exercise, and every result or exercise that still takes one as an argument. For each, name the target that was supposed to discharge it and say what happened.\n3. The only assumptions that should remain are the three that were out of scope throughout -- `EFSuccSeparation`, `IteratesReduction`, and the undecidability of the Post correspondence problem -- together with any that a target reported as a real obstacle, with its account. Anything else still standing is a bookkeeping error: find it and say so.\n4. Run `#print axioms` through the aliases of `RequestProject/Labels.lean` on every formalised result and exercise, and confirm each depends only on `propext`, `Classical.choice` and `Quot.sound`. `tools/print_axioms.sh` does exactly this.\n5. Bring `THEOREMS.md` and `EXERCISES.md` up to date. No status column may say \"from the hypothesis ...\" for a hypothesis that is now proved, and the counts in the `## Status` sections must agree with the tables.\n6. Update `FORMALISATION.md`, in particular its account of what the formalisation assumes, which should now be much shorter than it was.\n\nReport the final tally: how many numbered results and how many exercises are proved outright, how many remain conditional, and on exactly what.\n\n--- Background common to this group of targets. ---\n\nEvery numbered result and every exercise of the book is now formalised, the build\nis clean, and no declaration depends on `sorry`. What is left is a different kind\nof gap: a number of results are proved *from an explicit hypothesis* -- a\n`def ... : Prop` passed as an argument to the theorem and never proved.\n`EXERCISES.md` and `THEOREMS.md` say so in the status column, and the docstring of\neach hypothesis says why it was assumed rather than proved.\n\nThe author has now asked for these hypotheses to be discharged, so that the\nresults are proved outright. This group of targets does that, a file at a time.\n\nWhat \"discharge a hypothesis\" means here, precisely:\n\n* Prove the `Prop` as an ordinary theorem of the project.\n* Remove it from the argument list of every declaration that takes it, so those\n  declarations become unconditional. Their conclusions must stay exactly as they\n  are. Removing a hypothesis by weakening the conclusion, by restricting the\n  statement, or by introducing a different assumption somewhere else is not\n  discharging it. If you find you have to do one of those, stop and report it.\n* The `def ... : Prop` may then be deleted, or kept with the same statement as a\n  `theorem`. If you keep the name, make it a theorem, not a def.\n* Update `RequestProject/Labels.lean` (the alias docstring, and `assert_no_sorry`),\n  `EXERCISES.md` and `THEOREMS.md`: the status column must no longer say \"from the\n  hypothesis ...\".\n\nThree assumptions are deliberately out of scope for every target in this group and\nmust be left exactly as they are:\n\n* `Transducers.Exercises.EFSuccSeparation` (Exercise `exer:fo-suc`) -- the\n  Ehrenfeucht-Fraisse argument. The author will deal with this one himself.\n* `Transducers.Exercises.IteratesReduction` (Exercise\n  `exer:rational-composition-finiteness-undecidable`) -- the reduction from the\n  halting problem, which would need Turing machines formalised.\n* `not ComputablePred Transducers.PCP.Solvable` -- the undecidability of the Post\n  correspondence problem, which the book itself takes as given.\n\nIf you cannot discharge a hypothesis this target names, do not fake it. Leave it\nin place and say in your summary exactly what is missing and how large it looks. A\nprecise account of a real obstacle is a good outcome. A theorem that looks\nunconditional because its content moved into a new assumption is not.\n"
  },
  {
    "id": "hyp-two-way-bound",
    "title": "The computable equivalence bound for two-way transducers",
    "results": [],
    "lean_names": [],
    "depends_on": {},
    "discharges": [
      "EffectiveTwoWayBound"
    ],
    "max_continues": 12,
    "prompt": "Discharge `Transducers.EffectiveTwoWayBound`\n(`RequestProject/PartC/EffectiveReg.lean`). It is the *last* hypothesis anywhere in the\nbook's numbered results: Theorem `thm:decidable-equivalence-regular` (equivalence is\ndecidable for regular functions) is the only numbered result that is not unconditional,\nand this is what it rests on. Everything else in Parts A, B, C and D is proved outright.\n\nThe mathematics is already in the project and is NOT what is missing. That such a bound\nexists is proved: `Transducers.exists_twoWayCode_bound`\n(`RequestProject/PartC/RegCodeBound.lean`), as is the semantic decision procedure\n`Transducers.regularFun_eq_of_short` (`RequestProject/PartC/WeightedRegClosure.lean`).\nWhat is missing is only that the bound is a *computable function of the two codes*.\n\nWhy it is missing: the bound comes out of a chain of three existentials over abstract\nfinite types, none of which carries size information -- `isRegularFun_of_isTwoWay`\n(`PartC/SnakeReg.lean`), then `isWeighted_comp_regular` and `exists_injective_weighted`\n(`PartC/WeightedRegClosure.lean`), then `weighted_eq_of_short`\n(`PartB/WeightedZero.lean`), which gives the bound as `d1 + d2` for dimensions that exist\nonly as abstract types. Read the long docstring at the top of\n`RequestProject/PartC/EffectiveReg.lean`; it sets all of this out.\n\nThe route is already isolated and machine-checked for you.\n`RequestProject/PartC/RegBoundGap.lean` states the missing construction as\n`Transducers.EffectiveTwoWayWeighted` -- a `Computable₂` map\n`TwoWayCode → TwoWayCode → WCode × WCode` producing valid weighted codes over the\nrationals whose values agree on an input exactly when the two coded two-way transducers\ndo -- and proves `Transducers.effectiveTwoWayBound_of_effectiveTwoWayWeighted`. So it is\nenough to prove `EffectiveTwoWayWeighted`; the bound is then `Transducers.wcodeBound` of\nthe two images, exactly as `Transducers.effectiveWeightedBound`\n(`PartB/WeightedBound.lean`) already does for weighted automata, where the same chain is\neffective because a `WCode` *is* a linear representation up to normalisation.\n\nThe work is to redo, in a size-explicit code-to-code form, the prime decomposition\n(Theorem `thm:2dfa-decomposition-into-primes`, the snake construction of\n`RequestProject/PartC/Snake*.lean`) and the weighted closure properties of\n`WeightedRegClosure.lean`. This is a large piece of engineering rather than new\nmathematics, and it is the only thing between this hypothesis and a theorem.\n\nMachinery that did not exist when this hypothesis was written, and which you should use:\n`RequestProject/Common/PrimrecArith.lean` and `RequestProject/Common/PrimrecList.lean`\ngive primitive recursive arithmetic on the integers and rationals and the list operations\nthat go with them; `RequestProject/PartC/TwoWaySim.lean` and `TwoWaySimPrimrec.lean` give\nthe fuel-bounded simulation of a coded two-way transducer, which is how the sibling\nhypothesis `EffectiveTwoWayEvalEq` was discharged.\n\nYou may instead prove `EffectiveTwoWayBound` directly by another route if you find one --\n`EffectiveTwoWayWeighted` is a signpost, not a requirement.\n\nACCEPTANCE TEST. `Transducers.EffectiveTwoWayBound` is taken as an argument by no\ndeclaration, and `#check @Transducers.Book.«thm:decidable-equivalence-regular»` shows a\ntype with no hypotheses, with `#print axioms` reporting only `propext`,\n`Classical.choice`, `Quot.sound`.\n\nCONTEXT — what discharging a hypothesis means here.\n\nTo discharge a hypothesis is to *prove* the `Prop` and remove it from the argument list of\nevery declaration that takes it, leaving all conclusions unchanged. It is not discharging\nto weaken a conclusion, to add a side condition that was not in the book, or to replace the\nassumption by another that is no weaker: introducing a fresh `def ... : Prop` and taking\n*that* as an argument is precisely the move this task exists to eliminate. No `sorry`, no\n`axiom`, no `native_decide`. Keep the project building, and keep `THEOREMS.md`,\n`EXERCISES.md` and `RequestProject/Labels.lean` truthful about what changed.\n\nIf the work defeats you, stop and report precisely which step and why, leaving the project\nbuilding and its conclusions unchanged. A precise account of an obstacle is an acceptable\noutcome. A result that looks unconditional but rests on a fresh assumption is not.\n\nOut of scope, do not touch: `Transducers.Exercises.EFSuccSeparation`,\n`Transducers.Exercises.EffectiveLengthPairsSemilinear`, and\n`Transducers.Exercises.CanonicalSuffixBimachineExists` (which is false and refuted in the\nproject)."
  },
  {
    "id": "hyp-iterates-reduction",
    "title": "The halting-problem reduction for finiteness of the iterates",
    "results": [],
    "lean_names": [],
    "depends_on": {},
    "discharges": [
      "IteratesReduction"
    ],
    "max_continues": 10,
    "prompt": "Discharge `Transducers.Exercises.IteratesReduction`\n(`RequestProject/Exercises/IterateFiniteness.lean`), which Exercise\n`exer:rational-composition-finiteness-undecidable` rests on. It asks for an undecidable\nset and a computable reduction to the problem of the exercise:\n\n    ∃ (H : Set ℕ) (red : ℕ → RelCode),\n      (¬ ∃ D : ℕ → Bool, Computable D ∧ ∀ e, (D e = true ↔ e ∈ H)) ∧\n      Computable red ∧ ∀ e, CodeSelfMap (red e) ∧ (CodeIteratesFinite (red e) ↔ e ∈ H)\n\nThe mathematical step of the exercise is already proved: `iterates_finite_iff`, that the\nset of iterates is finite exactly when two iterates coincide. Only the reduction is\nmissing.\n\nThis was out of scope until now for a concrete reason: the project had no unconditionally\nundecidable set at all. It now has one. `RequestProject/Acceptance/Basic.lean` gives\n`Acceptance.ATM : Set ℕ`, the acceptance problem for partial recursive programs, with\n`Acceptance.atm_not_turingDecidable : ¬ TuringDecidable ATM` proved outright by\ndiagonalisation, and `Acceptance.turingDecidable_iff_computablePred` relating that notion\nto Mathlib's `ComputablePred`. `RequestProject/Sim/` additionally provides a counter\nmachine, a compiler from `Nat.Partrec'` into it, and `PCP.Sim.exists_tape_machine`. All of\nthis arrived with the merge of the Post correspondence problem and is unconditional.\n\nSo take `H` to be `ATM`, or its complement if the reduction comes out the other way round\n(the complement of an undecidable set is undecidable, and `H` is existentially quantified\nhere, so either direction is admissible). The work is the computable map `red` from a\nnatural number to a code of a rational function that is a self-map on the strings over its\nalphabet, whose iterates are finite exactly when the corresponding instance is in `H`.\nNote the first conjunct of the target is stated with a decider of type `ℕ → Bool` rather\nthan with `ComputablePred`; relate the two rather than restating anything.\n\nACCEPTANCE TEST. `Transducers.Exercises.IteratesReduction` is taken as an argument by no\ndeclaration, and `#check @Transducers.Book.«exer:rational-composition-finiteness-undecidable»`\nshows a type with no hypotheses, with `#print axioms` reporting only `propext`,\n`Classical.choice`, `Quot.sound`.\n\nCONTEXT — what discharging a hypothesis means here.\n\nTo discharge a hypothesis is to *prove* the `Prop` and remove it from the argument list of\nevery declaration that takes it, leaving all conclusions unchanged. It is not discharging\nto weaken a conclusion, to add a side condition that was not in the book, or to replace the\nassumption by another that is no weaker: introducing a fresh `def ... : Prop` and taking\n*that* as an argument is precisely the move this task exists to eliminate. No `sorry`, no\n`axiom`, no `native_decide`. Keep the project building, and keep `THEOREMS.md`,\n`EXERCISES.md` and `RequestProject/Labels.lean` truthful about what changed.\n\nIf the work defeats you, stop and report precisely which step and why, leaving the project\nbuilding and its conclusions unchanged. A precise account of an obstacle is an acceptable\noutcome. A result that looks unconditional but rests on a fresh assumption is not.\n\nOut of scope, do not touch: `Transducers.Exercises.EFSuccSeparation`,\n`Transducers.Exercises.EffectiveLengthPairsSemilinear`, and\n`Transducers.Exercises.CanonicalSuffixBimachineExists` (which is false and refuted in the\nproject)."
  },
  {
    "id": "C5-types-and-terms",
    "title": "Part C.5: types, regular terms, and the easy direction",
    "results": [
      "C.5.1",
      "C.5.2",
      "C.5.3"
    ],
    "lean_names": [],
    "depends_on": {},
    "max_continues": 8,
    "prompt": "Section C.5 *Combinators* is new in the book and nothing of it is formalised yet.\nThis target is its definitions and the easy half of its main theorem.\n\n**Definitions.**  Definition `def:types` (C.5.1): types built from the unit type by product,\nco-product and list.  Definition `def:regular-functions-on-types-under-string-representation`\n(C.5.2): what it means for a type-to-type function to be regular *under string\nrepresentation*, i.e. after encoding the elements of a type as strings.  Definition\n`def:regular-terms` (C.5.3): a regular term is built from the atomic terms — identity,\nprojections, co-projections, distributivity, cons, uncons, reverse, concat, split, and group\nprefix multiplication for a finite group — by composition, pairing, co-pairing and map.\n\n**The easy direction of Theorem `thm:regular-terms` (C.5.4)**: every function defined by a\nregular term is regular under string representation.  The book calls this a straightforward\ninduction on the term, whose basis is that each atomic term is regular under string\nrepresentation; the only delicate part is parsing the string representation of the input, and\nthe book works `distr` through as its example.  Lemma `lem:terms-define-string-homomorphisms`\n(C.5.7) belongs with this material.\n\nState the theorem in both directions but prove only this one for now, leaving the converse for\nthe next target — do NOT state the converse as a hypothesis of anything.  It is better to have\n`regularTerm_isRegular` proved and the equivalence not yet stated than an equivalence that\nrests on an assumption.\n\n**What to build on.**  `Transducers.IsRegularFun` (`RequestProject/PartC/RegularDef.lean`) is\n`CompClosure RegularFam`, where `RegularFam` is the rational functions together with\n`mapReverse` and `mapDuplicate`.  The whole of Parts A, B and C is proved, so the regular\nfunction theory is available in full and should be reused rather than rebuilt.  The string\nrepresentation of a type is the part with no counterpart in the project yet, and is the real\ndesign decision here: choose it so that the induction of the easy direction goes through, and\nsay in the file's header docstring why you chose it.\n\nThe section is `partCRegular/combinators.tex`, and it is in the workspace you were given.\n(Until now the driver shipped only the book files that sit beside `main.tex`; since the book\nwas reorganised into per-part directories that meant no chapters at all reached you. That is\nfixed, and every chapter `main.tex` includes is now in the workspace.)\n\nFollow the conventions of the project: every numbered result gets an alias in\n`RequestProject/Labels.lean` whose Lean name is its LaTeX label, followed by `assert_no_sorry`,\nand an entry in `THEOREMS.md`.  No `sorry`, no `axiom`, no `native_decide`.  Do not introduce a\n`def ... : Prop` and take it as an argument in order to close a goal.  If a step defeats you,\nstop and report which one and why, leaving the project building; a precise account of an\nobstacle is an acceptable outcome, a result that merely looks proved is not."
  },
  {
    "id": "C5-terms-converse",
    "title": "Part C.5: every regular function is a regular term",
    "results": [
      "C.5.4",
      "C.5.7",
      "C.5.10",
      "C.5.12",
      "C.5.13",
      "C.5.14",
      "C.5.15"
    ],
    "lean_names": [],
    "depends_on": {},
    "max_continues": 10,
    "prompt": "Finish Section C.5 by proving the converse direction of Theorem `thm:regular-terms`\n(C.5.4) — every function that is regular under string representation is defined by a regular\nterm — and closing the equivalence.\n\nThe definitions, the easy direction and the string representation come from the target\n`C5-types-and-terms`, which ran before this one.  Build on what it left; do not restate it.\n\nThe route is the book's, and it goes through the prime decomposition, which this project has\nalready proved.  The lemmas to establish are:\n\n* `lem:terms-define-append-hash` (C.5.10), with `claim:finite-type-bijection-disjoint-units`\n  (C.5.8), `claim:finite-domain-regular-list-function` (C.5.9) and `claim:bang-definable`\n  (C.5.11);\n* `lem:terms-define-map-reverse-duplicate` (C.5.12) — terms define the two non-rational primes\n  of `Transducers.RegularFam`, `Transducers.mapReverse` and `Transducers.mapDuplicate`;\n* `lem:terms-define-flip-flop` (C.5.13) and `lem:terms-define-reversible` (C.5.14) — terms\n  define the Mealy primes, `Transducers.IsFlipFlopMealy` and `Transducers.IsReversibleMealy`\n  (`RequestProject/PartA/MealyBasic.lean`), which with Krohn-Rhodes gives the rational ones;\n* `lem:terms-define-string-representation` (C.5.15) and `claim:head` (C.5.16).\n\nSince `Transducers.IsRegularFun` is by definition `CompClosure RegularFam`, and terms are\nclosed under composition, the converse reduces to: terms define every member of `RegularFam`.\nThe rational case is where Krohn-Rhodes and the Mealy primes come in.  Theorem\n`thm:2dfa-decomposition-into-primes` and Theorem `thm:krohn-rhodes` are both proved in the\nproject.\n\nWhen both directions are in place, state `thm:regular-terms` as the equivalence it is, give it\nits alias, and make sure `#print axioms` on that alias reports only `propext`,\n`Classical.choice`, `Quot.sound`.\n\nThe section is `partCRegular/combinators.tex`, and it is in the workspace you were given.\n(Until now the driver shipped only the book files that sit beside `main.tex`; since the book\nwas reorganised into per-part directories that meant no chapters at all reached you. That is\nfixed, and every chapter `main.tex` includes is now in the workspace.)\n\nFollow the conventions of the project: every numbered result gets an alias in\n`RequestProject/Labels.lean` whose Lean name is its LaTeX label, followed by `assert_no_sorry`,\nand an entry in `THEOREMS.md`.  No `sorry`, no `axiom`, no `native_decide`.  Do not introduce a\n`def ... : Prop` and take it as an argument in order to close a goal.  If a step defeats you,\nstop and report which one and why, leaving the project building; a precise account of an\nobstacle is an acceptable outcome, a result that merely looks proved is not."
  },
  {
    "id": "exercises-two-new",
    "title": "The two new exercises of Part B",
    "results": [],
    "depends_on": {},
    "max_continues": 6,
    "lean_names": [
      "Transducers.Exercises.not_isRationalFun_duplicate",
      "Transducers.Exercises.rational_primes_no_reverse_reversible"
    ],
    "prompt": "Two exercises have been added to Part B and neither is formalised.\n\n**`exer:duplication-not-rational` (B.4.1, `partBRational/myhill-nerode.tex`)** — string\nduplication is not rational. The author's solution is the one already used for string reversal:\nthe relation `∼` of Theorem `thm:machine-independent-rational-functions` has infinite index, and\nin fact all strings are pairwise inequivalent. The project has that argument written out for\nreversal in `RequestProject/Exercises/ReverseNotRational.lean`\n(`not_boundedVarRel_reverse`, then `not_isRationalFun_reverse` via `isRationalFun_iff`); follow\nit. Name the result `Transducers.Exercises.not_isRationalFun_duplicate`.\n\n**`exer:no-reverse-reversible` (B.2.9, `partBRational/rational-functions.tex`)** — in the list\nof atomic functions of Theorem `thm:rational-primes` (B.2.6) the reversal of a reversible Mealy\nmachine is not needed. The author's solution: to implement a right-to-left reversible Mealy\nmachine with states `Q`, first run the ordinary left-to-right reversible machine to label every\nposition with the state transformation of the prefix up to it; then a right-to-left flip-flop\nmachine to label every position with the transformation of the whole input; then, by cancellation\nin the group of state permutations, obtain for every position the transformation of the suffix\nafter it, from which the wanted output is recovered. Name the result\n`Transducers.Exercises.rational_primes_no_reverse_reversible`.\n\nThese are independent of each other and of Section C.5.\n\nThe book changed under you, so read `partCRegular/combinators.tex` and the Part B files as they\nnow stand rather than trusting anything written about them earlier.\n\nConventions: every numbered result gets an alias in `RequestProject/Labels.lean` whose Lean name\nis its LaTeX label, followed by `assert_no_sorry`, plus an entry in `THEOREMS.md` (or\n`EXERCISES.md` for an exercise). No `sorry`, no `axiom`, no `native_decide`; do not introduce a\n`def ... : Prop` and take it as an argument to close a goal. `tools/gen_labels.py --check`,\n`tools/decl_files.py --check` and `tools/tex_numbering.py --check` must all pass when you are\ndone — the last two fail right now because Section C.5 was renumbered, and fixing that is part\nof the job. If a step defeats you, stop and say which and why, leaving the project building."
  },
  {
    "id": "C5-restructure",
    "title": "Part C.5 restructured: functoriality combinators, pairing derived",
    "results": [
      "C.5.3",
      "C.5.4",
      "C.5.8"
    ],
    "depends_on": {},
    "max_continues": 10,
    "lean_names": [
      "Transducers.regular_iff_regularTerm"
    ],
    "prompt": "Section C.5 has been restructured and the Lean no longer matches the book.\n\n**What changed.** Definition `def:regular-terms` (C.5.3) used to take pairing, co-pairing and map\nas combinators. It now takes composition together with *functoriality* combinators that lift\nfunctions along the type constructors, and the diagonal is atomic. Pairing and co-pairing are no\nlonger primitive: they are *derived*, and that is the new Claim `claim:pairing-copairing`\n(C.5.8) — pairing is the diagonal followed by `f₁ × f₂`, co-pairing is `f₁ + f₂` followed by the\nco-diagonal.\n\n**What the project has.** `Transducers.RegTerm` in `RequestProject/PartC/CombTerms.lean` is the\nold definition: it has `pair` and `copair` as constructors. That must change to the book's new\nlist, and `claim:pairing-copairing` must then be *proved* rather than assumed.\n\n**What must survive.** Theorem `thm:regular-terms` (C.5.4) is unchanged as a statement and must\nstill be proved, as `Transducers.regular_iff_regularTerm`, in both directions. Everything the\nexisting Section C.5 files prove about the old term language has to be carried across; where a\nproof used `pair` or `copair` directly it should now use the derived forms from\n`claim:pairing-copairing`, which is exactly why the book introduces that claim first.\n\nDo not leave the old and new term languages side by side: the book has one definition of a\nregular term, and so should the project.\n\nThe book changed under you, so read `partCRegular/combinators.tex` and the Part B files as they\nnow stand rather than trusting anything written about them earlier.\n\nConventions: every numbered result gets an alias in `RequestProject/Labels.lean` whose Lean name\nis its LaTeX label, followed by `assert_no_sorry`, plus an entry in `THEOREMS.md` (or\n`EXERCISES.md` for an exercise). No `sorry`, no `axiom`, no `native_decide`; do not introduce a\n`def ... : Prop` and take it as an argument to close a goal. `tools/gen_labels.py --check`,\n`tools/decl_files.py --check` and `tools/tex_numbering.py --check` must all pass when you are\ndone — the last two fail right now because Section C.5 was renumbered, and fixing that is part\nof the job. If a step defeats you, stop and say which and why, leaving the project building."
  },
  {
    "id": "C5-rational-terms",
    "title": "Part C.5: the term characterisation of the rational functions",
    "results": [
      "C.5.18"
    ],
    "depends_on": {},
    "max_continues": 10,
    "lean_names": [
      "Transducers.rational_iff_rationalTerm"
    ],
    "prompt": "Prove the new Theorem `thm:rational-terms` (C.5.18): a type-to-type function is\n**rational** under string representation if and only if it is defined by a variant of the regular\nterms.\n\nThe variant keeps the atomic functions and combinators of `def:regular-terms` as restructured by\nthe target `C5-restructure`, except that reverse and the diagonal are removed — reverse because\nstring reversal is not rational (Example `ex:string-reversal-not-rational`), the diagonal because\nit gives string duplication, which is not rational (Exercise `exer:duplication-not-rational`,\nformalised by the target `exercises-two-new`). Removing the diagonal also removes pairing, so\nthree atomic functions are added back:\n\n* adjoining units, `A → A × 1` and `A → 1 × A`;\n* left distributivity, `(A + B) × C → (A × C) + (B × C)`;\n* the list deconstructor on the right, `A* → 1 + A* × A`.\n\nName the theorem `Transducers.rational_iff_rationalTerm`.\n\nThis depends on both other targets, which run before it. The rational side already exists in the\nproject as `Transducers.IsRationalFun`, and the string representation and the regular case come\nfrom Section C.5 as it now stands — reuse them rather than rebuilding.\n\nThe book changed under you, so read `partCRegular/combinators.tex` and the Part B files as they\nnow stand rather than trusting anything written about them earlier.\n\nConventions: every numbered result gets an alias in `RequestProject/Labels.lean` whose Lean name\nis its LaTeX label, followed by `assert_no_sorry`, plus an entry in `THEOREMS.md` (or\n`EXERCISES.md` for an exercise). No `sorry`, no `axiom`, no `native_decide`; do not introduce a\n`def ... : Prop` and take it as an argument to close a goal. `tools/gen_labels.py --check`,\n`tools/decl_files.py --check` and `tools/tex_numbering.py --check` must all pass when you are\ndone — the last two fail right now because Section C.5 was renumbered, and fixing that is part\nof the job. If a step defeats you, stop and say which and why, leaving the project building."
  },
  {
    "id": "C5-exercises-terms",
    "title": "Part C.5 exercises: constructions in the term language",
    "results": [],
    "depends_on": {},
    "max_continues": 8,
    "lean_names": [
      "Transducers.Exercises.regTerm_ite",
      "Transducers.Exercises.regTerm_id_replaceable",
      "Transducers.Exercises.pref_bijective",
      "Transducers.Exercises.regTerm_windows_two"
    ],
    "prompt": "Four new exercises of Section C.5, all of them constructions in the term language\nrather than anything about naturality. Formalise them, giving each the Lean name in brackets.\n\n* **`exer:if-then-else`** [`Transducers.Exercises.regTerm_ite`] -- with `1 + 1` read as the\n  Booleans, the functions definable by regular terms are closed under the conditional that maps\n  `a` to `f₁ a` when `f a` is true and to `f₂ a` when it is false. State it for regular terms and,\n  if it costs little, for the rational fragment too, since the book says \"regular (or rational)\".\n\n* **`exer:identity-function-replacement`** [`Transducers.Exercises.regTerm_id_replaceable`] -- the\n  general identity `A → A` can be replaced by its special case at `A = 1` without changing what\n  the terms define; and in fact even that special case is not needed. Both halves are wanted.\n\n* **`exer:group-prefix-multiplication-isomorphism`** [`Transducers.Exercises.pref_bijective`] --\n  group prefix multiplication is an isomorphism. The author's solution: if the output is\n  `[g₁,…,gₙ]` then the `i`-th input letter is `g_{i-1}⁻¹ gᵢ`, taking `g₀` to be the unit, and that\n  inverse is itself definable by a regular term. Prove both the bijectivity and that the inverse\n  is term-definable.\n\n* **`exer:windows-of-size-two`** [`Transducers.Exercises.regTerm_windows_two`] -- exhibit a\n  regular term of type `A* → (A × A)*` sending a list to its windows of size two, so that\n  `[1,2,3,4] ↦ [(1,2),(2,3),(3,4)]`. The book sketches a route through a function\n  `A → (A+A)*`; follow it or find a shorter one, but the term must be exhibited and proved to\n  compute exactly that function.\n\nSection C.5 is `partCRegular/combinators.tex`; read it as it now stands. The term language and\nits semantics are already in the project, in `RequestProject/PartC/Comb*.lean` -- `Transducers.Ty`,\n`Transducers.RegTerm` with the functoriality combinators and the diagonal atomic, the string\nrepresentation, `Transducers.regular_iff_regularTerm` and `Transducers.rational_iff_rationalTerm`.\nReuse all of it; do not restate the term language.\n\nConventions: each exercise gets an alias in `RequestProject/Labels.lean` named for its LaTeX\nlabel, followed by `assert_no_sorry`, and a row in `EXERCISES.md`. No `sorry`, no `axiom`, no\n`native_decide`; do not introduce a `def ... : Prop` and take it as an argument to close a goal.\n`tools/gen_labels.py --check`, `tools/decl_files.py --check` and `tools/tex_numbering.py --check`\nmust pass. If an exercise defeats you, leave it out, say which and why, and finish the others --\na precise obstacle is worth more than a strained proof."
  },
  {
    "id": "C5-exercises-natural",
    "title": "Part C.5 exercises: natural transformations",
    "results": [],
    "depends_on": {},
    "max_continues": 10,
    "lean_names": [
      "Transducers.Exercises.natural_id_eq_id",
      "Transducers.Exercises.no_natural_head"
    ],
    "prompt": "Six new exercises of Section C.5 about *natural transformations*. The project has no\nnotion of naturality yet, so the first job is to define one: a family of functions indexed by\ntypes, natural in the sense that the square commutes for every `g : A → B`, as spelled out in\n`exer:natural-identity`. Define it once, over the type functors of `Transducers.Ty`, and use it\nthroughout. Say in the file's header docstring what you chose and why.\n\n* **`exer:natural-identity`** [`Transducers.Exercises.natural_id_eq_id`] -- a natural family\n  `f_A : A → A` is the identity at every `A`. This is the exercise that fixes the definition of\n  naturality, so do it first.\n\n* **`exer:head-operation-natural`** [`Transducers.Exercises.no_natural_head`] -- there is no\n  natural transformation `A* → A`; in particular the `head` of `claim:head` is not one.\n\n* **`exer:natural-list-function`** -- there are uncountably many natural transformations\n  `A* → A*`.\n\n* **`exer:natural-transformations-from-A`** -- every natural transformation `A → A*` is regular,\n  at each type.\n\n* **`exer:natural-transformations-from-regular-terms`** -- naturality extends to functors in\n  several type variables, and the atomic functions of `def:regular-terms`, group prefix\n  multiplication aside, are natural transformations.\n\n* **`exer:natural-transformations-list-free`** -- a natural transformation whose *domain* functor\n  does not use the list constructor is definable by a regular term.\n\nThe first two are the ones I expect to land; the last four are harder and the last in particular\nis a real theorem. Do them in the order above, and if the later ones defeat you say so precisely\nrather than weakening them -- an honest account of where the argument stops is the right outcome\nfor those.\n\nSection C.5 is `partCRegular/combinators.tex`; read it as it now stands. The term language and\nits semantics are already in the project, in `RequestProject/PartC/Comb*.lean` -- `Transducers.Ty`,\n`Transducers.RegTerm` with the functoriality combinators and the diagonal atomic, the string\nrepresentation, `Transducers.regular_iff_regularTerm` and `Transducers.rational_iff_rationalTerm`.\nReuse all of it; do not restate the term language.\n\nConventions: each exercise gets an alias in `RequestProject/Labels.lean` named for its LaTeX\nlabel, followed by `assert_no_sorry`, and a row in `EXERCISES.md`. No `sorry`, no `axiom`, no\n`native_decide`; do not introduce a `def ... : Prop` and take it as an argument to close a goal.\n`tools/gen_labels.py --check`, `tools/decl_files.py --check` and `tools/tex_numbering.py --check`\nmust pass. If an exercise defeats you, leave it out, say which and why, and finish the others --\na precise obstacle is worth more than a strained proof."
  }
]
