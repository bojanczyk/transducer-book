# encoding-snake-alphabet — COMPLETE_WITH_ERRORS

task: 8a76b61a-cf79-4ecb-a1de-5da367aae515
created: 2026-08-28 03:14:55.994353

The task is complete and the repository builds clean.

**What is in place**

* `RequestProject/PartC/SnakeAlph.lean` introduces the book's alphabet of snake letters, `Transducers.SnakeLetter Q B` — a letter is a bipartite graph on two copies of the state set `Q`, each vertex having at most one outgoing edge, which goes to the other copy and is labelled by an element of `B + 1` — together with the snake graph a string over it describes (`SnakeGraph.Edge`), the book's "all edges lie on a single directed path" condition (`SnakeGraph.IsSnakePath`, `RepresentsSnake`), the output along the path (`pathOut`, `SnakeOutIs`, unique by `snakeOutIs_unique`), the width (`colVisits`, `SnakeWidthLe`) and the function of the lemma, `SnakeGraph.snakeOut k`.
* `RequestProject/PartC/SnakeAlphReg.lean` states and proves the lemma in the book's form: `Transducers.SnakeGraph.snakeOut_isRegular` — for every `k`, the function taking `w ∈ C*` to the output of the snake graph it represents, and to the empty string when `w` represents no snake graph of width at most `k`, is a regular function.
* The proof does not redo the induction on the width. It reuses `Transducers.boundedWidth_isRegular` together with the `Snake*` development: the strings representing a snake graph of width `≤ k` are shown to form a regular language (out- and in-degree at most one, at most one source, no directed cycle, bounded column visits — `SnakeAlphChar.lean`, `SnakeAlphLoc.lean`, `SnakeAlphLocLang.lean`, `SnakeAlphCyc.lean`), and on those strings the output is computed by a two-way transducer that walks along the snake (`SnakeAlphRun.lean`); the two are combined by the conditional of Lemma `lem:regular-closure-properties`.
* Both statements are kept. The label `lem:output-of-snake-graph-is-regular` in `RequestProject/Labels.lean` names the book's form `SnakeGraph.snakeOut_isRegular`; the general form `Transducers.boundedWidth_isRegular` is recorded under the same label as `#2`. Both aliases carry `assert_no_sorry`, checked at build time.

**Equivalence.** The two forms have the same mathematical content, but as Lean statements the book's form is the weaker one: it is one instance of the general statement, over the fixed alphabet `SnakeLetter Q B`, and it is *derived* from it. The converse derivation is not formalised; it holds mathematically because the reachable part of a run of width at most `k` is a snake graph of width at most `k` whose slice encoding is obtained letter by letter, so `widthOut M k` factors as a letter-to-letter map followed by `snakeOut k`. This is stated plainly in `THEOREMS.md`.

**Status of the numbered result.** Lemma `lem:output-of-snake-graph-is-regular` is **proved outright**, in both forms, with no hypothesis and no `sorry` anywhere in its dependencies. `#print axioms` reports exactly `propext`, `Classical.choice`, `Quot.sound` for `Transducers.SnakeGraph.snakeOut_isRegular` and for `Transducers.boundedWidth_isRegular`. Both are proved for every `k : ℕ` rather than only for `k ∈ {1, …, |Q|}`, which is more general; the book's special letter for the empty input is not needed, since the empty string already represents the one-column graph with empty output. Both deviations are documented in the docstrings and in `THEOREMS.md`.

**Documentation.** `THEOREMS.md` no longer carries the old divergence note "stated for the width-`k` output function … rather than for an alphabet of snake letters"; it has been replaced by an accurate account (the alphabet, the two forms, which is derived from which, the two remaining deviations, and the axiom check), and the file list and status table rows are up to date. The only change made in this session was to refresh a stale header comment in `RequestProject/PartC/Statements.lean` that still described the lemma as formalised only in the transducer form; it now points at both forms.

`lake build` succeeds with no errors (8347 jobs) and the work is committed and pushed.
