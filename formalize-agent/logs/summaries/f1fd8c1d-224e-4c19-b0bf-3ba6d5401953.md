# hyp-polynomial-ideals — COMPLETE_WITH_ERRORS

task: f1fd8c1d-224e-4c19-b0bf-3ba6d5401953
created: 2026-08-31 13:49:55.839699

**State found, and what this run did.** The two hypotheses this target names — `SortedFromOmegaOutputs` and `FactorThroughSortedOfOutputsPoly` — were already discharged in `RequestProject/Exercises/Ideals.lean` when I resumed, together with the supporting loop analysis left in reusable form by the earlier `hyp-growth-dichotomy` work. So this run added no mathematics; it verified that state end to end, from a full rebuild, and repaired one stale index entry. Nothing was left half-done.

**Verification carried out.**

* A **full `lake build` of `transducer-lean/` succeeds: 8414 jobs, no errors and no warnings.**
* `tools/print_axioms.sh --all` runs `#print axioms` on **all 213 aliases** of `RequestProject/Labels.lean` and reports only `propext`, `Classical.choice`, `Quot.sound` for every one of them — including `exer:full-ideal`, `exer:polynomial-ideals`, `exer:all-ideals` and `exer:decide-same-ideal`.
* `RequestProject/` contains no `sorry` outside block comments, no `axiom`, no `@[implemented_by]` and no `native_decide`. The only `sorry` occurrences are inside `/- … -/` blocks that preserve statements the project deliberately does not make.
* I read the two key files rather than trusting the status column: `Exercises/IdealsOmega.lean` (`exists_rational_sorted_of_omega`) and `Exercises/IdealsPoly.lean` (`exists_pattern_cover_range`, `exists_rational_factor_through_sorted`) carry real arguments — the chain-of-loops pattern plus the Uniformisation Lemma in the first case, the pattern cover of an automaton with no ambiguous cycle and no chain of `k+1` loops in the second.

**Status of the results.**

* `SortedFromOmegaOutputs` and `FactorThroughSortedOfOutputsPolyPos` are **theorems**, with exactly the statements they had as assumptions (the second with the extra `1 ≤ k`); no declaration of `Exercises/Ideals.lean` takes a hypothesis argument any more.
* Exercise `exer:polynomial-ideals` (`polynomial_ideals`) is **proved**, with the extra hypothesis `1 ≤ k`. That hypothesis is not in the book and cannot be dropped: `polynomial_ideals_false_at_zero` refutes the `k = 0` case, and `not_factorThroughSortedOfOutputsPoly` refutes the factorisation step as the author literally states it. Both faithful statements are kept — one as a `def … : Prop` next to its refutation, one commented out — and the divergence is recorded in `EXERCISES.md`, in `THEOREMS.md` and in the docstrings.
* Exercises **`exer:all-ideals` (`all_ideals`) and `exer:decide-same-ideal` (`sameIdeal_iff`, with `sameIdeal_decidable`) are proved outright and unconditionally**: I checked their signatures and they rest on nothing beyond `IsIdeal`; their rows in `EXERCISES.md` and their alias docstrings in `Labels.lean` say so, and each carries `assert_no_sorry`.
* `exer:full-ideal` remains proved outright, as before.

**One repair.** `tools/tex_numbering.py --check` flagged three numbers of the label dictionary of `LABELS.md` (`def:regular-functions`, `def:polyregular-functions`, `thm:polyregular-functions-are-continuous`) as out of date against `main.aux`; I corrected the number column only. All three bookkeeping checks (`gen_labels.py --check`, `tex_numbering.py --check`, `decl_files.py --check`) now pass. No LaTeX source was touched.

**Documentation.** A *Verification of this state* section was appended to `THEOREMS.md`, and a corresponding entry was added at the top of `ARISTOTLE_SUMMARY.md`. `EXERCISES.md` was left as found — its ideals section already records the discharge, the divergence and the revised tally (69 of 81 exercises proved outright, 12 from an explicit hypothesis). All work is committed and pushed.

**Still assumed elsewhere in the project** (out of scope here, unchanged): `EFSuccSeparation`, `IteratesReduction`, the undecidability of PCP, and the remaining hypotheses tabulated in `EXERCISES.md`/`THEOREMS.md`.
