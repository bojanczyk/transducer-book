# C5-restructure — COMPLETE_WITH_ERRORS

task: a99576a5-5598-4f81-8ac3-591bb4a90a19
created: 2026-09-07 09:56:29.555279

The Lean formalization is done, builds, and is committed and pushed. It lives in `transducer-lean/`, and the work is in these files.

**1. Definition `def:regular-terms` rewritten to the book's new list** — `RequestProject/PartC/CombTerms.lean`. The inductive syntax `Transducers.RegTerm` now has as atomic terms: identity, the two projections, the two co-projections, the **diagonal** `A → A × A`, the **co-diagonal** `A + A → A`, distributivity, the list constructor and deconstructor, reverse, concatenation, split, and group prefix multiplication; and as combinators: composition together with the three **functoriality** combinators `f₁ × f₂` (`prodMap`), `f₁ + f₂` (`sumMap`) and `f*` (`map`). The old `pair` and `copair` constructors are gone, and the semantics `RegTerm.eval` was extended and adjusted to match. A search of the sources confirms no `RegTerm.pair`/`RegTerm.copair` remains: there is one term language in the project, not two.

**2. Claim `claim:pairing-copairing` is now proved rather than built in** — `RequestProject/PartC/CombFinite.lean`. `Transducers.IsRegularTermFun.pair` is the diagonal followed by `f₁ × f₂`; `Transducers.IsRegularTermFun.copair` is `f₁ + f₂` followed by the co-diagonal — exactly the book's two-line argument. The primitives they rest on are `IsRegularTermFun.prodMap`, `IsRegularTermFun.sumMap`, `tfun_diag`, `tfun_codiag`. Because the earlier Section C.5 files were all written against `IsRegularTermFun.pair`/`.copair`, every proof about the old term language carries across unchanged and now routes through the derived forms, which is why the book introduces this claim first. `tfun_prodMap`/`tfun_sumMap` in `CombDerived.lean` keep their statements and are now primitive instead of derived.

**3. Theorem `thm:regular-terms` still proved, in both directions, statement unchanged** — `Transducers.regular_iff_regularTerm` (`RequestProject/PartC/CombRepr.lean`), as the equivalence `IsRegularUnderRepr f ↔ IsRegularTermFun f`, from `regularTerm_isRegular` and `regularTerm_of_isRegular`. The easy direction (`RequestProject/PartC/CombStatements.lean`) gained the four new cases, proved from the pairing/co-pairing closure of `CombCombinators.lean` and the projections/co-projections of `CombAtomProj.lean`: `isRegularUnderRepr_diag`, `isRegularUnderRepr_codiag`, `IsRegularUnderRepr.prodMap`, `IsRegularUnderRepr.sumMap`.

**Verification just run.**
* `lake build` — "Build completed successfully (8499 jobs)", no errors.
* `#print axioms`: `regular_iff_regularTerm`, `regularTerm_isRegular`, `regularTerm_of_isRegular` each depend only on `propext`, `Classical.choice`, `Quot.sound`; `IsRegularTermFun.pair` and `IsRegularTermFun.copair` only on `Quot.sound`. `tools/print_axioms.sh` reports all 240 aliases within the allowed three axioms, and the `assert_no_sorry` checks in `Labels.lean` all pass.
* The claim's statement was re-elaborated independently against the book's wording (pairing `A → B₁ × B₂` from `A → B₁`, `A → B₂`; co-pairing `A₁ + A₂ → B` from `A₁ → B`, `A₂ → B`) and both type-check from the proved lemmas.
* `tools/gen_labels.py --check`, `tools/decl_files.py --check` and `tools/tex_numbering.py --check LABELS.md` all pass. Section C.5's renumbering, including C.5.8 for `claim:pairing-copairing`, was already recorded in `LABELS.md` on the incoming state, so all three checkers passed before as well as after my edits; I re-ran them after every change.
* No `sorry`, no `axiom`, no `native_decide` was introduced; the only `sorry` occurrences anywhere in the sources are inside pre-existing commented-out blocks documenting results the book leaves unproved.

**Status of the numbered results touched.** Theorem `thm:regular-terms` — proved outright, both directions, unconditionally. Claim `claim:pairing-copairing` — proved outright (previously true by construction of the syntax, now a genuine derivation). Definition `def:regular-terms` — restated to the book's new list. Nothing here is proved from an added hypothesis, and nothing in Section C.5 is left open.

**Documentation updated and committed.** `THEOREMS.md`: the file rows for `CombTerms.lean`, `CombFinite.lean`, `CombCombinators.lean` and `CombStatements.lean`, the index row for `claim:pairing-copairing`, the `## Status` section, and removal of the *Divergences from the book* bullet that recorded the old primitive/derived inversion — that divergence no longer exists. Also the two `claim:pairing-copairing` docstrings in `RequestProject/Labels.lean` and the stale paragraph of `ARISTOTLE_SUMMARY.md`. The working tree is clean and both commits are pushed.
