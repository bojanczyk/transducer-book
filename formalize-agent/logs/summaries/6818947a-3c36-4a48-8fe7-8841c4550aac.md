# C2-regular-equivalence — COMPLETE_WITH_ERRORS

task: 6818947a-3c36-4a48-8fe7-8841c4550aac
created: 2026-08-22 07:38:08.230436

## Where the three target results stand

**Proved outright (no `sorry` anywhere in their dependencies):**

* **Lemma C.2.10** — `Transducers.regular_closure_properties` (`RequestProject/PartC/RegClosure.lean` and the `MapLift*`/`RegMapLift.lean` files): regular functions are closed under the map combinator, under concatenation and under case distinction over a regular language.
* **Claim C.2.11** — `Transducers.sum_of_regular` (`RequestProject/PartC/RegSum.lean`), in a corrected form. The claim exactly as printed is **false on the empty input**; the faithful rendering is kept, commented out, next to the corrected one, the counterexample is formalised as `Transducers.not_sum_of_regular_nil`, and the discrepancy is documented in the docstring and in `THEOREMS.md` (*An error in Claim C.2.11*).
* **Corollary C.2.8** — `Transducers.regularFun_isTwoWay` (regular ⊆ two-way). The book prints this corollary as the *opposite* inclusion, which is a typo; that too is documented.

**Still open: Theorem C.2.9** — `Transducers.twoWay_iff_regular`. It must **not** be described as proved. Its right-to-left half is Corollary C.2.8 and is proved; its left-to-right half (`Transducers.twoWay_isRegular`) is reduced, sorry-free, to the book's snake lemma `Transducers.boundedWidth_isRegular`. That lemma is proved by induction on the width; the base cases `k = 0` and `k = 1` are proved, and the **single remaining `sorry`** of Section C.2 is the induction step `Transducers.boundedWidth_isRegular_step` (`RequestProject/PartC/SnakeReg.lean`).

## What this run added (all sorry-free and verified)

* `RequestProject/PartC/SnakeBase.lean` — the base cases `k ≤ 1` of the snake lemma: a halting run of width one never moves left, hence is a single left-to-right pass, recognised by a deterministic automaton and with its output produced by a bimachine (`TwoWay.widthOut_zero_isRegular`, `TwoWay.widthOut_one_isRegular`). `SnakeWidth.lean` was trimmed and the snake lemma moved to the new `SnakeReg.lean`.
* `RequestProject/PartC/SnakeConfine.lean` — the **confinement** of the pieces of the record-breaker decomposition: after the last visit to a record-breaking column the walk stays strictly to its right (`Walk.recSeq_lt_of_recLast_lt`), up to the first visit to one it stays weakly to its left (`Walk.le_recSeq_of_le_recFirst`); hence the loop and the progress parts of the `i`-th record-breaker lie in the columns `x(i−1) < · ≤ x(i+1)` (`Walk.loop_confined`, `Walk.progress_confined`, plus their versions for the trajectory of a run). This is the book's step 4 and is what makes the required rational function have linear growth.
* `RequestProject/PartC/SnakeMirror.lean` — **mirroring a two-way transducer** (`TwoWay.mirror`), an involution that swaps the two letters adjacent to the head and the two directions; it turns every run into the mirrored run on the reversed input with the same output (`TwoWay.stepCfg_mirror`, `TwoWay.reaches_mirror_iff`, `TwoWay.reaches_mirror_reverse`). This realises the book's "otherwise reverse the snake" without introducing an alphabet of snake letters.

## What remains for C.2.9

Only the machine-theoretic half of the induction step: a version of `TwoWay.widthOut` in which the source and the target vertex of the run are marked in the input (the pieces of a run start and end in the middle of it), and the four rational/regular stages of the book's proof — marking the record-breakers, forming the blocks `wᵢ₋₁ # wᵢ`, duplicating them, and gluing with the map combinator. The combinatorics, the confinement and the reversal are all now in place; the closure properties needed for the gluing are Lemma C.2.10 and Claim C.2.11, which are proved.

## Housekeeping

`lake build` succeeds with no errors over the whole project. No new `sorry`, no new `axiom`, no `native_decide`: the total `sorry` count is unchanged. `#print axioms` was run on every result finished here (`TwoWay.widthOut_zero_isRegular`, `TwoWay.widthOut_one_isRegular`, `TwoWay.widthOut_one_eq`, `Walk.loop_confined`, `Walk.progress_confined`, `TwoWay.run_loop_confined`, `TwoWay.run_progress_confined`, `Walk.recSeq_lt_of_recLast_lt`, `Walk.le_recSeq_of_le_recFirst`, `TwoWay.stepCfg_mirror`, `TwoWay.reaches_mirror_iff`, `TwoWay.reaches_mirror_reverse`, `TwoWay.mirror_mirror`) and reports only `propext`, `Classical.choice`, `Quot.sound` (the mirroring results use only `propext` and `Quot.sound`). `THEOREMS.md` has been updated — file table, the C.2.9 status row, the section *What is missing in Theorem C.2.9*, and the `## Status` section — and everything is committed and pushed. No LaTeX source was modified and `ARISTOTLE_SUMMARY.md` was left untouched.
