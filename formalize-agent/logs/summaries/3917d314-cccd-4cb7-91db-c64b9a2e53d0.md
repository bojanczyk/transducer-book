# external — COMPLETE

task: 3917d314-cccd-4cb7-91db-c64b9a2e53d0
created: 2026-08-26 01:41:04.225375

All 11 exercises of the introduction (`intro.tex`) are formalised and proved outright — none is conditional on an extra hypothesis, none is left open, and none of the exercise files contains a `sorry`.

**Files added / changed (all under `transducer-lean/`)**
- `RequestProject/Exercises/Intro.lean` (new) — namespace `Transducers.Exercises`, one section per chapter, exercises in book order.
- `RequestProject/Exercises/IntroAux.lean` (new) — the reusable automata/regularity helpers the exercises needed (transporting a dfa along an equivalence of state sets, complementation, the dfa accepting a single string, regularity of singletons and of the empty language, finite unions of regular languages, and two stabilisation lemmas about iterating a map on a finite set).
- `RequestProject/Exercises.lean` (new roll-up), imported from `RequestProject.lean`.
- `RequestProject/Labels.lean` — the 14 label aliases for the exercise declarations, in the file's generated format, each followed by `assert_no_sorry`.
- `EXERCISES.md` (new) — the index of the exercises in the style of `THEOREMS.md`: the statement, the Lean name, the label, and the status.
- `THEOREMS.md` — pointer to `EXERCISES.md`, the two new files added to the layout and file table, and a closing note recording that no numbered result of the main text was changed.

**Labels.** Every one of the 11 exercises carries a `\label` in the sources available to me, so all of them are referred to by label and **there is no exercise the author needs to add a label for**. The labels used are `exer:reverse-continuous`, `exer:duplication-continuous`, `exer:squaring-continuous`, `exer:factorial-power-continuous`, `exer:factorial-continuous`, `ex:continuity-for-finite-images`, `exer:finite-images-assumption-necessary`, `exer:middle-letter-not-continuous`, `ex:distance`, `exer:all-functions-continuous-for-metric`, `exer:continuous-iff-uniformly-continuous`.

**Choices worth flagging (each also documented in the docstring and in `EXERCISES.md`)**
- `exer:reverse-continuous` and `exer:duplication-continuous` are exactly the two halves of a lemma of the main text (the one I refer to as `nolabel:lem-reverse-and-duplicate-continuous`, for which I could find no `\label` in my copy of the sources). Rather than restating that lemma I deduce the two exercises from the project's existing proof of it.
- `exer:finite-images-assumption-necessary` asks to show that an assumption cannot be dropped, which is not itself a mathematical statement; it is formalised as the concrete claim the author's solution establishes, namely the existence of an injective (hence all-fibres-regular) string-to-string function that is not continuous, over an alphabet with two distinct letters.
- `exer:continuous-iff-uniformly-continuous` is stated with both alphabets finite; the equivalence is proved under those hypotheses.
- `ex:distance` is packaged both as the five metric axioms (`strDist_isMetric`) and as a `MetricSpace` instance (`strMetric`); the stronger ultrametric inequality, which the author's solution proves, is also recorded as `strDist_ultrametric`. This is why that one exercise has four aliases in `Labels.lean`.
- **Error found in an author's solution.** For `exer:factorial-continuous` the solution builds a three-component automaton whose update rule for the third component is wrong as written (it does not maintain the intended invariant). The exercise is nevertheless true; I prove the more general statement that `w ↦ w^(g |w|)` is continuous for every monotone `g : ℕ → ℕ`, and obtain the exercise at `g = id`. The discrepancy is recorded in the docstring and in `EXERCISES.md`.

**Verification.** `lake build` succeeds with no errors (8267 jobs). `#print axioms` on each of the 12 exercise theorems reports only `propext`, `Classical.choice`, `Quot.sound`. The only remaining `sorry`s in the repository are the pre-existing ones in Part D of the main text, which I did not touch. All work is committed and pushed.
