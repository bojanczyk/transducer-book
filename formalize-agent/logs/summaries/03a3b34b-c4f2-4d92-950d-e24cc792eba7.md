# C2-regular-equivalence — COMPLETE_WITH_ERRORS

task: 03a3b34b-c4f2-4d92-950d-e24cc792eba7
created: 2026-08-22 05:13:25.358752

## What I did

I continued the formalisation of §C.2 of the book in `transducer-lean/`, working on Theorem C.2.9 and the two closure results it uses.

### Status of the three targeted results

* **Lemma C.2.10 (`Transducers.regular_closure_properties`) — proved outright.** Sorry-free; `#print axioms` reports only `propext`, `Classical.choice`, `Quot.sound`.
* **Claim C.2.11 (`Transducers.sum_of_regular`) — proved outright**, in the corrected form (the claim as printed is contradictory on the empty input; the original statement is kept commented out next to it with the counterexample `Transducers.not_sum_of_regular_nil`, and the discrepancy is documented in the docstring and in `THEOREMS.md`). Axioms: only the three standard ones.
* **Theorem C.2.9 (`Transducers.twoWay_iff_regular`) — still open.** Its right-to-left half (regular ⊆ two-way, Corollary C.2.8) is proved. Its left-to-right half (`Transducers.twoWay_isRegular`) is *derived without any gap of its own* from a single open statement, the book's snake lemma `Transducers.boundedWidth_isRegular` in `RequestProject/PartC/SnakeWidth.lean`, which is the only `sorry` in the project concerning §C.2. So C.2.9 must **not** be described as proved.

### New work this session (all sorry-free)

I formalised the entire *combinatorial* content of the book's induction on the width of a snake, which is the heart of the missing lemma:

* `RequestProject/PartC/SnakeRec.lean` — the greedy sequence of **record-breaking columns** of a walk, its stabilisation after finitely many stages, the increasing chain of times formed by the first and last visits to them, and the resulting factorisation of the output of a run into the outputs of the **loop parts** and the **progress parts** (`TwoWay.outRange_eq_loopProgOut`, `TwoWay.runOutput_eq_loopProgOut`); plus the bound showing each progress part has width ≤ k − 1.
* `RequestProject/PartC/SnakeLoop.lean` — splitting a **loop part** into pieces of width ≤ k − 1 (first at the intermediate visits to its base column, then at the first visit to the furthest column of each resulting one-sided loop; the loops lying to the left of their base column are reduced to those on the right by reflecting the walk), the bound for the last progress part, and the capstone results `Walk.walk_splitsInto_pred`, `TwoWay.run_splitsInto_pred` and `TwoWay.runOutput_splits`: **a halting run of width ≤ k (k ≥ 2) splits into finitely many consecutive pieces of width ≤ k − 1, and its output is the concatenation of their outputs.**

All of these depend only on `propext`, `Classical.choice`, `Quot.sound` (checked with `#print axioms`). I also cleaned up the linter warnings in `SnakeWalk.lean`/`SnakeWidth.lean` by removing unused hypotheses.

### What remains for C.2.9

Only the machine-theoretic half of the induction step: presenting each piece of a run as the run of a *width-(k−1) snake with an arbitrary source and target* on a factor of the input cut out by a rational function, and gluing the pieces back with Lemma C.2.10. This is documented precisely in the docstrings and in the new section *What is missing in Theorem C.2.9* of `THEOREMS.md`.

### Housekeeping

`lake build` succeeds with no errors; the total `sorry` count is unchanged at 18 (none added). `THEOREMS.md` is updated: the C.2.9 status row, the file table (new rows for `SnakeWidth.lean`, `SnakeWalk.lean`, `SnakeRec.lean`, `SnakeLoop.lean`), the `## Status` section and the axiom check. `PartC.lean` now imports the new files. Everything is committed and pushed; the LaTeX sources were not touched.
