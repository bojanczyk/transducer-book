# encoding-snake-alphabet — COMPLETE_WITH_ERRORS

task: 7f6b66ff-9221-430a-a32a-c699cc874415
created: 2026-08-28 02:15:19.466956

Lemma `lem:output-of-snake-graph-is-regular` is now formalised in the book's own form, over the book's alphabet, and that form is what the label names.

**What is in place**

* The alphabet of snake letters is a real object in Lean: `Transducers.SnakeLetter Q B` (`RequestProject/PartC/SnakeAlph.lean`) — a letter is a bipartite graph on two copies of `Q` in which each vertex has at most one outgoing edge, going to the other copy and labelled by a letter of `B` or by nothing. A string over it glues into a graph on the pairs (state, column); `SnakeGraph.RepresentsSnake`, `SnakeGraph.pathOut`, `SnakeGraph.SnakeWidthLe` and the lemma's function `SnakeGraph.snakeOut k` are all defined there.
* The book's statement is `Transducers.SnakeGraph.snakeOut_isRegular` (`RequestProject/PartC/SnakeAlphReg.lean`): for every `k`, the function taking `w ∈ C*` to the output of the snake graph it represents, and to the empty string when `w` represents no snake graph of width at most `k`, is regular. It is **proved outright** — `#print axioms` on the label alias reports only `propext`, `Classical.choice`, `Quot.sound`.
* It is proved by *reusing* `Transducers.boundedWidth_isRegular` and the `Snake*` development, not by redoing the induction: the strings representing a snake graph of width at most `k` are shown regular (degree, source-uniqueness, width and acyclicity conditions, in `SnakeAlphChar.lean`, `SnakeAlphLocLang.lean`, `SnakeAlphCyc.lean`), and on them the output is the output of the halting run of the two-way transducer that walks along the snake (`SnakeAlphRun.lean`); the two are combined by the conditional of Lemma `lem:regular-closure-properties`.
* Both statements are kept. The label `lem:output-of-snake-graph-is-regular` names the book's form; the general form is kept alongside as `lem:output-of-snake-graph-is-regular#2` = `Transducers.boundedWidth_isRegular`, also proved outright with the three standard axioms.

**Are the two equivalent?** They have the same mathematical content, but only one direction is formalised. As Lean statements the book's form is the weaker one: it is the general form specialised to the fixed alphabet `SnakeLetter Q B`, and it is derived from it. The converse derivation (encode a transducer's run slice by slice, a letter-to-letter map, and compose with `snakeOut k`) is true but not formalised. That is now stated exactly this way in `THEOREMS.md` and in the docstring of `boundedWidth_isRegular`.

**Divergence note.** The old note "stated for the width-`k` output function `TwoWay.widthOut` … rather than for an alphabet of snake letters" is gone. What remains in `THEOREMS.md` under *Divergences from the book* for this lemma is accurate and small: it is proved for every `k : ℕ` rather than only for `k ∈ {1, …, |Q|}` (more general, and immaterial since a column has at most `|Q|` vertices), and the book's special letter for the empty input is not needed, since the empty string already represents the one-column graph with no edge. I also replaced a sentence in `THEOREMS.md` that claimed the two forms are "equivalent in strength" with the precise account above, and rewrote the now-stale "Divergence from the book" paragraph in the docstring of `boundedWidth_isRegular` to say that it is the general form and that the book's form is `SnakeGraph.snakeOut_isRegular`.

**State of the repository.** `lake build` succeeds with no errors (8347 jobs). No `sorry` occurs in any file this result depends on — the only occurrences of `sorry` outside Part D's five open statements are inside block comments. Nothing was weakened or deleted, no axiom was added, and everything is committed and pushed.

