# external — COMPLETE

task: 3a0b9170-15b5-49fd-bd83-4432b046ae60
created: 2026-08-25 23:16:48.802555

**What was done**

Definition `def:rational-recognisable-subsets` (rational and recognisable subsets of a monoid, from the paragraph *Why the name rational?*) is now formalised, so every numbered result of Parts A, B and C is either formalised or accounted for as an internal step.

* `transducer-lean/RequestProject/PartB/RationalStatements.lean` — a new section *Rational and recognisable subsets of a monoid*, placed where the book puts it (after Definition `def:rational-relation`, before *Composition and continuity*):
  * `Transducers.IsRecognisableSubset L` — there is a congruence `c : Con M` of finite index (`Finite c.Quotient`) and a finite set `S` of classes with `L` the union of the classes in `S` (i.e. the preimage of `S` under the quotient map). Mathlib's `Con M` is exactly the book's "equivalence relation compatible with multiplication"; the finiteness of `S` follows from finite index but is required as in the book.
  * `Transducers.IsRationalSubset` — an inductive predicate with constructors: finite sets, union, product, and Kleene star `⋃ₙ Lⁿ`. Being inductive, it *is* the least such class, and its recursor is the corresponding induction principle.
  * Three sanity results, proved sorry-free (axioms: `propext`, `Classical.choice`, `Quot.sound`): `iUnion_pow_eq_submonoidClosure` (the star is the generated submonoid), `isRecognisableSubset_iff_exists_hom` (recognisable ⇔ preimage of a subset of a finite monoid under a homomorphism, the usual formulation), and `isRecognisableSubset_of_finite` / `isRationalSubset_of_finite` (in a finite monoid every subset is both).
* `RequestProject/Labels.lean` — the two aliases `«def:rational-recognisable-subsets»` and `«def:rational-recognisable-subsets#2»`, each with `assert_no_sorry`, in book order; the entry was removed from the accounting comment at the end of the file.
* `THEOREMS.md` — a row in the Part B table, plus a paragraph describing the new declarations.
* `LABELS.md` — no change was needed: the dictionary of labels already carries the row `| B.1.3 | definition | \`def:rational-recognisable-subsets\` | rational-relations.tex |` (that table lists every label of the book, formalised or not), and it is correct as it stands.

`lake build` in `transducer-lean/` completes successfully (8264 jobs); the only `sorry` warnings are the pre-existing Part C/D statements recorded in `THEOREMS.md`, and the new declarations were checked to be free of `sorryAx`.

**Things in the sources that looked out of date or off**

* `sst (conflicted).tex` at the top level is a stale sync-conflict copy of `sst.tex` (206 vs 265 lines, older wording, missing the section's opening paragraph and several `\myunderbrace` annotations, and a differing register update `Y#X` vs `YX#`). It is not `\input` by `main.tex`, so it only risks confusion; it looks safe to delete.
* `README.md` and `LABELS.md` refer to helper scripts `tools/gen_labels.py`, `tools/tex_numbering.py` and `tools/relabel.py`, but there is no `tools/` directory in the sources I received, so `Labels.lean` cannot currently be regenerated as documented (I edited it by hand, keeping the generated format exactly).
* The top-level `lakefile.toml` (the one beside the `.tex` files, not the one in `transducer-lean/`) is not a valid Lake configuration: its library glob `"transducer-lean.+"` is rejected, so `lake` fails to configure a workspace at the top level. The real Lean project in `transducer-lean/` is fine and is where I built. Its checkout also arrived without a `.lake` directory, so I pointed it at the pre-built Mathlib (the `.lake` directory is gitignored and is not part of the delivered sources).
* Build artefacts `main.aux` and `main.bbl` are tracked in the repository.
* Otherwise the sources looked consistent: I re-extracted every theorem-like environment from the `.tex` files (103 of them) and each one's label appears in the `LABELS.md` dictionary; the only two unlabelled ones are a claim inside an exercise solution in `intro.tex` and a commented-out claim in `logic.tex`, neither of which is a numbered result of the main text.

