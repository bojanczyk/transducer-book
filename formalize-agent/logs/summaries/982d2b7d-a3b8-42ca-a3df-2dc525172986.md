# hyp-effective-arithmetic — COMPLETE

task: 982d2b7d-a3b8-42ca-a3df-2dc525172986
created: 2026-09-01 12:13:51.077603

## Where the task stands

**Three of the four named effectivity hypotheses are discharged and are theorems; the fourth is not, and I did not fake it.**

* `Transducers.EffectiveWeightedEvalEq` — **theorem** (`RequestProject/PartB/WCodePrimrec.lean`). The four numbered results that rested on it — `thm:equivalence-weighted-automata`, `thm:equivalence-rational-functions`, `thm:zeroness-weighted-automata`, `thm:decide-if-mealy` — are **proved outright**, with their conclusions unchanged and no hypothesis argument.
* `Transducers.Exercises.ComputableDiagonalTest` — **theorem** (`RequestProject/Exercises/LengthCollision.lean`).
* `Transducers.EffectiveTwoWayEvalEq` — **theorem** (`RequestProject/PartC/TwoWaySimPrimrec.lean`), from the fuel-bounded simulation of a coded two-way transducer.
* `Transducers.EffectiveTwoWayBound` — **still a hypothesis**, and Theorem `thm:decidable-equivalence-regular` (`Transducers.regular_equivalence_decidable`) still takes it as an explicit argument. So that result is *not* proved outright.

The general-purpose library the first three rest on is `RequestProject/Common/PrimrecArith.lean` (arithmetic of ℤ and ℚ in Mathlib's `Primrec` API, which the pinned Mathlib does not supply — it supplies only the `Primcodable` instances and `Primrec.eq`) and `RequestProject/Common/PrimrecList.lean` (the list operations on top of it). Neither mentions transducers.

## What this session added

I re-checked the remaining hypothesis rather than accepting the earlier account, and confirmed it: `RegCodeBound.lean` really does prove the *existence* of the bound (`Transducers.exists_twoWayCode_bound`), and what is missing is only its computability as a function of the codes — but that is *not* bookkeeping and not a Mathlib gap. The chain producing the bound (`isRegularFun_of_isTwoWay` → `isWeighted_comp_regular`/`exists_injective_weighted` → `weighted_eq_of_short`) passes through existentials over abstract finite types (`IsRegularFun`, `IsWeighted f = ∃ (Q : Type) (_ : Finite Q), …`) carrying no size information, so no `Primrec` lemma can bridge it.

To make that obstacle sharp rather than vague, I added `RequestProject/PartC/RegBoundGap.lean`:

* `Transducers.EffectiveTwoWayWeighted` — the effective form of the book's reduction: a computable map from two codes of two-way transducers to two *valid* codes of weighted automata over ℚ whose values on an input agree exactly when the two coded transducers produce the same output.
* `Transducers.effectiveTwoWayBound_of_effectiveTwoWayWeighted` — **proved**: that single statement implies `EffectiveTwoWayBound`, the bound being the sum of the two Schützenberger bounds `Transducers.wcodeBound` of the images, which is primitive recursive in the codes.

So the gap is exactly one construction — the size-explicit, code-to-code form of the prime decomposition (the snake lemma) and of the weighted-automata closure properties — and everything downstream of it is already proved. Filling it means redoing those two developments as constructions on codes with correctness proofs; that is a large piece of work, comparable in size to the existing `Snake*.lean` development, and I did not attempt it.

`EffectiveTwoWayWeighted` is **assumed nowhere**: no theorem takes it, and the argument list and conclusion of Theorem `thm:decidable-equivalence-regular` are unchanged. No new assumption was traded in.

## Verification

`lake build` succeeds: 8453 jobs, no errors, no warnings. `tools/print_axioms.sh` reports only `propext`, `Classical.choice`, `Quot.sound` for all 213 labelled aliases, and the same three for the new theorem. `tools/gen_labels.py --check`, `tools/decl_files.py --check` and `tools/tex_numbering.py --check` pass. `RequestProject/` contains no `sorry` outside block comments (the 13 occurrences are all inside `/- … -/` documentation of superseded statements), no `axiom`, no `@[implemented_by]`, no `native_decide`. `THEOREMS.md` (files table, the section *The conditional result of Part C*, the *Status* section and its job count), `FORMALISATION.md` and the docstring of `EffectiveTwoWayBound` were updated; `EXERCISES.md` and `Labels.lean` needed no change, since the exercise statuses and aliases were already correct. Everything is committed and pushed.

