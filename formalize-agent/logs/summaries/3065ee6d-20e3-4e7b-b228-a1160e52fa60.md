# C5-terms-converse — COMPLETE

task: 3065ee6d-20e3-4e7b-b228-a1160e52fa60
created: 2026-09-04 08:16:40.169623

Section C.5 of the book is now complete: the converse direction of Theorem `thm:regular-terms` is proved and the theorem is stated as the equivalence it is in the book.

**Proved outright (no hypothesis of any kind; `#print axioms` on each alias in `RequestProject/Labels.lean` reports only `propext`, `Classical.choice`, `Quot.sound`):**

| label | Lean name | file |
| --- | --- | --- |
| `thm:regular-terms` | `Transducers.regular_iff_regularTerm` (`IsRegularUnderRepr f ↔ IsRegularTermFun f`) | `RequestProject/PartC/CombRepr.lean` |
| `thm:regular-terms` (converse) | `Transducers.regularTerm_of_isRegular` | `RequestProject/PartC/CombRepr.lean` |
| `lem:terms-define-string-representation` | `Transducers.terms_define_string_representation` | `RequestProject/PartC/CombRepr.lean` |
| `lem:terms-define-flip-flop` | `Transducers.terms_define_flip_flop` | `RequestProject/PartC/CombMealyFF.lean` |
| `lem:terms-define-reversible` | `Transducers.terms_define_reversible` | `RequestProject/PartC/CombMealyRev.lean` |
| `lem:terms-define-map-reverse-duplicate` | `Transducers.terms_define_map_reverse`, `Transducers.terms_define_map_duplicate` | `RequestProject/PartC/CombMapRev.lean` |
| `lem:terms-define-append-hash` | `Transducers.terms_define_append_hash` | `RequestProject/PartC/CombStrFam.lean` |
| `claim:bang-definable`, `claim:head`, `claim:finite-type-bijection-disjoint-units` | `Transducers.bang_definable`, `Transducers.claim_head`, `Transducers.finite_type_bijection_disjoint_units` | `RequestProject/PartC/CombDerived.lean` |
| `claim:finite-domain-regular-list-function`, `lem:terms-define-string-homomorphisms` | `Transducers.finite_domain_regular_list_function`, `Transducers.terms_define_string_homomorphisms` | `RequestProject/PartC/CombFinite.lean` (from the earlier target; labelled and indexed here) |

Nothing is proved from an explicit hypothesis, no `def ... : Prop` is taken as an argument, and nothing is left open in the section. Every result above has its alias in `RequestProject/Labels.lean` followed by `assert_no_sorry`, and a row in `THEOREMS.md`.

**Route.** `IsRegularFun` is by definition `CompClosure RegularFam` and terms are closed under composition, so the converse reduces to the primes. `Transducers.TermStrFun` (`CombStrFam.lean`) is the interface between the primes, whose alphabets are arbitrary finite types, and the terms, which speak about `Ty`s; one naming of the alphabets suffices because a change of naming is a letter-to-letter renaming. The rational primes come from Theorem `thm:rational-primes` (behind it Theorem `thm:2dfa-decomposition-into-primes` and Theorem `thm:krohn-rhodes`) together with the two Mealy lemmas; the two non-rational primes are `lem:terms-define-map-reverse-duplicate`. This gives `Transducers.termStrFun_of_isRegularFun` (`CombComplete.lean`), and `lem:terms-define-string-representation` then supplies, for each type, a term for its representation and a term for a one-sided inverse; the converse is the composition of the three.

**Three route divergences (statements unchanged), documented in the docstrings, in `THEOREMS.md` under *Divergences from the book*, and in the summary file:** `claim:finite-type-bijection-disjoint-units` is deduced *from* `claim:finite-domain-regular-list-function` (the reverse of the book's order; both are proved); in `lem:terms-define-reversible` the input alphabet is embedded in `Equiv.Perm (Option A)` by `a ↦ swap none (some a)` instead of carrying an arbitrary cyclic group structure; in the product case of `lem:terms-define-string-representation` the projections supplied by the easy direction replace the book's marking machine, so `claim:head` — proved all the same — is not needed there.

**Documentation and checks.** `THEOREMS.md` now has the new file rows, the rewritten `thm:regular-terms` row, index rows for the ten C.5 results and for the one unformalised claim `nolabel:claim-representations-are-a-regular-language`, refreshed counts (of the 115 environments of the dictionary: 23 definitions and 89 results formalised, none from a hypothesis, 3 not formalised), and a closing `## Status` section. `ARISTOTLE_SUMMARY.md` was rewritten for this pass. `python3 tools/gen_labels.py --check` passes, `lake build` succeeds (8497 jobs, no errors), no declaration uses `sorry`, `axiom` or `native_decide` (the only `sorry` occurrences anywhere are inside pre-existing commented-out records of withdrawn or corrected statements), and no LaTeX source was touched. All work is committed and pushed.
