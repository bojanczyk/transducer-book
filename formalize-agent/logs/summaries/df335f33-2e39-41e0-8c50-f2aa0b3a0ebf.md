# encoding-pebble — COMPLETE

task: df335f33-2e39-41e0-8c50-f2aa0b3a0ebf
created: 2026-08-28 12:34:18.048496

All five results of Section D.2 of the book that had been skipped as "configuration encodings used inside proofs" are now formalised, proved outright, and part of the index. Nothing that was proved before was weakened, and no `sorry`, `axiom` or `native_decide` was used.

**The encoding, as a real object in Lean**
* `Transducers.PebEnc.PairLetter` / `PebEnc.pairEnc` (`RequestProject/PartD/PebEnc.lean`) — the string representation of a *pair* of configurations of a pebble transducer: one letter per gap of the input, carrying the two states, the input letter that follows the gap, and the pebbles of each stack sitting in that gap. `Pebble.RestrReaches` / `Pebble.BalancedRun` render the book's balanced runs.
* `Transducers.CG.ConfLetter` / `CG.confEnc` — the representation of a single configuration; `Transducers.CG.CGLetter` with the graph a string over it describes (`RequestProject/PartD/ChildGraph.lean`, `ChildPath.lean`) — the book's *child configuration graph*, "a string over a fixed finite alphabet, whose length is the same as the input string".

**Proved outright** (each verified by `#print axioms`: only `propext`, `Classical.choice`, `Quot.sound`; no dependency contains a `sorry`)
* Lemma `lem:reachability-pebble-automaton` — `Transducers.reachability_pebble_automaton` (`PartD/PebReach.lean`)
* Claim `claim:reachability-basic-run` — `Transducers.reachability_basic_run` (same file)
* Claim `claim:from-child-configuration-graph-to-children` — `Transducers.from_child_configuration_graph_to_children` (`PartD/ChildGraphFor.lean`)
* Claim `claim:from-configuration-to-child-configuration-graph` — `Transducers.from_configuration_to_child_configuration_graph` (`PartD/CGFor.lean`)
* Lemma `lem:children-of-configuration-in-pebble-run` — `Transducers.children_of_configuration_in_pebble_run` (`PartD/CGFor.lean`), obtained by composing the two claims, exactly as the book concludes.

None is proved from an extra assumption supplied as a hypothesis of the surrounding development; the only hypotheses in the statements are the ones documented below. Nothing is left open.

**Documented divergences** (recorded in the docstrings, in `THEOREMS.md` under *Divergences from the book*, and in `FORMALISATION.md`)
* The book asks for an mso formula `φ(s, t)` with two free variables of the type of configurations. Since mso quantification is over positions, the Lean statements encode the pair of configurations into the input string and conclude that the set of encodings of reachable pairs is a *regular language* — equivalent by Theorem `thm:mso-logic-languages`, which is already in the project. Because a formula is only evaluated on a genuine structure, the language is only required to be correct on genuine encodings.
* The two results about children carry the hypothesis that the stack has fewer than `k` pebbles; the book does not state it, but its proof begins with exactly that case distinction ("If ℓ = k, then there is nothing to do").
* Claim `claim:from-child-configuration-graph-to-children` is proved from a two-pebble transducer via Theorem `thm:pebble-are-for`, not by the book's induction on the width of the graph; the statement obtained is the book's. Consequently the five results sit *after* the two headline theorems in the import order, and those two theorems keep the proofs they already had rather than being re-derived through the five. This is stated plainly in `PartD/Statements.lean` and `THEOREMS.md`; nothing is circular.

**Non-vacuity check.** `RequestProject/PartD/ChildExample.lean` exhibits a one-pebble machine whose initial configuration has exactly one child, and shows the for-transducer of the lemma maps that configuration's representation to its child's — so the `IsChildSeq` hypothesis of the two results about children is demonstrably satisfiable.

**Housekeeping.** `lake build` succeeds with no errors (8368 jobs). Linter warnings introduced by the new files were fixed at the root (three unused `simp` arguments, one unused hypothesis, one unused instance binder); the warning inventory in `THEOREMS.md` is updated. `THEOREMS.md`, `FORMALISATION.md`, `README.md`, `LABELS`-related aliases in `RequestProject/Labels.lean` (five new aliases, each with `assert_no_sorry`), the file tables, the counts (now 71 results proved outright, 3 environments not formalised, none of them in Part D) and `ARISTOTLE_SUMMARY.md` are all updated, and `tools/gen_labels.py --check`, `tools/decl_files.py --check`, `tools/tex_numbering.py --check` and `tools/relabel.py` all report no problem. Everything is committed and pushed.
