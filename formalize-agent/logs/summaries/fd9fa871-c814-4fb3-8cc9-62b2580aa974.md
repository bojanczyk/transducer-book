# exercises-partA — COMPLETE

task: fd9fa871-c814-4fb3-8cc9-62b2580aa974
created: 2026-08-26 03:53:53.796570

All twelve exercises of Part A are formalised and proved, and the run leaves `lake build` succeeding with no errors.

**Where the work is**

* `transducer-lean/RequestProject/Exercises/PartA.lean` — the exercises of `mealy.tex` (7) and `krohn-rhodes.tex` (5), one section per chapter, in the order the book states them, with the machinery each one needs. Every proof follows the author's own solution; deviations are stated in the docstrings.
* `transducer-lean/RequestProject/Exercises.lean` — roll-up file, imported by `RequestProject.lean`.
* `transducer-lean/EXERCISES.md` — new index in the style of `THEOREMS.md`: the exercise, its Lean name, its status, plus a section explaining, exercise by exercise, exactly what was formalised where the exercise is not a mathematical statement as it stands.
* `transducer-lean/RequestProject/Labels.lean` — a new *Exercises* section with one alias per exercise, named after its LaTeX label, each followed by `assert_no_sorry`, keeping the file's generated format.
* `THEOREMS.md` and `README.md` — updated only to record that the exercises live in `Exercises/` and are indexed in `EXERCISES.md`. No numbered result of the main text was touched, restated or weakened.

**Labels.** Every exercise of Part A carries a `\label` in the sources, so all twelve are referred to by label and all twelve get an alias; there is no exercise I had to refer to by position. Note that the last one is labelled `ex:map-lifting-continuous` (with the `ex:` prefix rather than `exer:`), which is how it appears in the sources.

**Exercises that are not statements.** None was left out; for each, what is formalised is the concrete claim the author's solution establishes: for "give an example of…" (`exer:letter-to-letter-not-mealy`, `exer:regular-complete-mealy`) that the author's example has the required properties; for the two yes/no questions (`exer:invertible-mealy-group`, `exer:invertible-mealy-is-reversible`) the author's answer, witnessed by the author's machines; for the three "give an algorithm" exercises (`exer:invertible`, `exer:polynomial-image-growth-decidable`, `exer:regular-complete-mealy-2`) the author's criterion together with the resulting decision procedure. The polynomial running time claimed in the solution to `exer:regular-complete-mealy-2` is not formalised — the project has no model of running time — and this is recorded in `EXERCISES.md`.

**Other choices, all documented in the docstrings.** `exer:composition-needs-many-states` is proved with a binary counter instead of the author's sieve on the first *n* primes (in the author's version the *i*-th machine has *pᵢ* states, so the bound is exponential in the number of machines but not obviously in the number of their states); the author's lower-bound argument is kept. `exer:flip-flop-from-sequential-composition` uses one two-state machine per state rather than per bit of a binary encoding, which the exercise does not require. `exer:delay-not-flip-flop-composition` is about reversible machines, as its text says although its label mentions flip-flops, and it needs a nonempty input alphabet. `ex:map-lifting-continuous` is exactly Lemma `lem:map-lifting-continuous` of the main text, so it is derived from the project's existing proof rather than proved again — this is the one place where an exercise coincides with a result of the book. Likewise the Mealy machine of a dfa used by `exer:regular-complete-mealy` is reused from the main text's `PartC/FOMealy.lean` instead of being redefined.

**Verification.** The full project builds (`lake build`, no errors). `RequestProject/Exercises/` contains no `sorry`, no new axiom and no `native_decide`; `#print axioms` on all fifteen exercise declarations reports only `propext`, `Classical.choice`, `Quot.sound`, and the `assert_no_sorry` lines in `Labels.lean` re-check this at every build. Everything is committed and pushed.
