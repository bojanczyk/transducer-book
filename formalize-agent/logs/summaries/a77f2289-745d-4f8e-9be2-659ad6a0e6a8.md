# C4-fo-transductions — COMPLETE_WITH_ERRORS

task: a77f2289-745d-4f8e-9be2-659ad6a0e6a8
created: 2026-08-23 22:55:00.165772

## What was done

**Theorem C.4.16 (`Transducers.foRelabelling_iff_aperiodicBimachine`) is now proved outright.**

The statement is unchanged and now lives in `RequestProject/PartC/MSO.lean` (the file of Section C.4 results that are proved, which contains no `sorry`). Its proof rests on four files, all sorry-free:

* `RequestProject/PartC/FORev.lean` — every first-order definable language is defined by a first-order *sentence*; the characterisation of first-order definability by invariance under `k`-types; the reversal `tpRev` of a `k`-type and hence closure of first-order definability under string reversal; first-order definability of the languages `{u | δ*(q₀,u) = q}` and of their reverses, for an aperiodic transition function.
* `RequestProject/PartC/FOPos.lean` — compositionality at a position: truth of a first-order formula of quantifier rank ≤ `k`, evaluated at a position, depends only on the `k`-type of the prefix, the letter and the `k`-type of the suffix.
* `RequestProject/PartC/FORelabBimach.lean` — first-order relabelling ⇒ aperiodic bimachine: the prefix/suffix automata compute `k`-types (aperiodic by Lemma C.4.15), so the index chosen at each position, hence the output block, is a function of the two bimachine states and the letter; the block of the last gap is appended at the last position.
* `RequestProject/PartC/FOBimachRelab.lean` — aperiodic bimachine ⇒ first-order relabelling: the formula of an index `(q, a, s, last?)` combines the sentence given by Theorem C.4.11 for the prefix (relativised below the free variable), the letter test, the reverse-language sentence for the suffix (relativised above), and the "is last position" test. This session fixed the four remaining errors in this file (the `strTrans` step lemmas, the uniqueness argument, and the reduction of the structure projection).

`#print axioms` on `Transducers.foRelabelling_iff_aperiodicBimachine` and on both implications reports only `propext`, `Classical.choice`, `Quot.sound`.

## Status of the two requested results

* **C.4.16 — proved outright**, no hypotheses beyond the statement's own, no dependency containing `sorry`.
* **C.4.17 (`Transducers.foTransduction_iff_prime_composition`) — still open.** The faithful statement is kept in `RequestProject/PartC/MSOOpen.lean` with a `sorry` proof. The book itself gives no proof (it defers it to a future edition) and lists the missing ingredients: first-order variants of (1) computing a string representation of the configuration graph of a two-way transducer and (2) the main step of the decomposition of two-way transducers into primes. In this project those are the aperiodic counterparts of the dozen files behind Theorem C.4.8 and Theorem B.2.6, which do not exist yet; and the converse inclusion additionally needs closure of first-order transductions under composition, which in the mso case is obtained *through* Theorem C.4.8 and so does not transfer — it would have to be proved by backwards translation of formulas. This is documented in the theorem's docstring and in a new `THEOREMS.md` section *Theorem C.4.17 is still open*.

## Housekeeping

* `lake build` succeeds with no errors (only pre-existing `sorry` warnings elsewhere in the project). No new `axiom`, no `native_decide`, no weakened statements.
* `THEOREMS.md` updated: the status rows of C.4.16 and C.4.17, the four new files added to the file table, the descriptions of `PartC/MSO.lean` and `PartC/MSOOpen.lean`, two new prose sections (*The proof of Theorem C.4.16*, *Theorem C.4.17 is still open*), the `## Status` section, and the closing `#print axioms` list. `ARISTOTLE_SUMMARY.md` was not modified, and no LaTeX source was touched.
* All work is committed and pushed.

