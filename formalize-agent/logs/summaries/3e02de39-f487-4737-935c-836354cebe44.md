# hyp-polynomial-ideals — COMPLETE_WITH_ERRORS

task: 3e02de39-f487-4737-935c-836354cebe44
created: 2026-08-31 22:58:35.662527

## Outcome

The two hypotheses this target names — `SortedFromOmegaOutputs` and the factorisation statement — are discharged, and I verified that state end to end from the sources rather than taking it on trust. No hypothesis argument remains anywhere in `RequestProject/Exercises/Ideals.lean`.

**Proved outright (all with `#print axioms` = `propext`, `Classical.choice`, `Quot.sound`):**

* `Transducers.Exercises.SortedFromOmegaOutputs` — a rational function with `Ω(n^k)` outputs has `sortedFun k` among its rational pre- and post-compositions. It is now a theorem, resting on `exists_rational_sorted_of_omega`: the loop analysis of `Exercises/ChainWords.lean` (the words `p₀x₀^{c₀}p₁⋯p_k` produced by a chain of `k` loops, and injectivity in the exponent tuple), the rational bijection with the sorted word in `Exercises/SortedPattern.lean`, and the Uniformisation Lemma in `Exercises/IdealsOmega.lean`.
* `Transducers.Exercises.FactorThroughSortedOfOutputsPolyPos` — a rational function with `O(n^k)` outputs, `k ≥ 1`, is a rational pre- and post-composition of `sortedFun k`. This is the strongly-connected-component analysis: `RegGrowth.exists_pattern_cover` (`Exercises/PatternCover.lean`) shows that in an automaton with no ambiguous cycle and no chain of `k+1` loops every accepted word belongs to one of finitely many `k`-patterns; `Exercises/PatternCoverRat.lean` realises such a family rationally, and `Exercises/IdealsPoly.lean` applies it to the range and composes with a rational section.
* Exercise `exer:polynomial-ideals` (`polynomial_ideals`), Exercise `exer:all-ideals` (`all_ideals`) and Exercise `exer:decide-same-ideal` (`sameIdeal_iff`, with `sameIdeal_decidable`). The last two are unconditional and free of any extra hypothesis; their rows in `EXERCISES.md` and their alias docstrings in `RequestProject/Labels.lean` say so, and both aliases carry `assert_no_sorry`.

**The one divergence from the book, unchanged and documented:** the factorisation as the author states it is false at `k = 0` (over the empty alphabet a factorisation through `sortedFun 0` forces the function to be constant, whereas `O(n^0)` outputs means only a finite range). The faithful rendering is kept as `def FactorThroughSortedOfOutputsPoly` and refuted by `not_factorThroughSortedOfOutputsPoly`; correspondingly `polynomial_ideals` carries `1 ≤ k`, refuted at `k = 0` by `polynomial_ideals_false_at_zero`, with the faithful form preserved as a commented-out statement. `all_ideals` and `sameIdeal_iff` are unaffected, because where they invoke `polynomial_ideals` the ideal has a member of infinite range, whose level is at least `1`.

**Verification performed in this pass:** a full `lake build` of `transducer-lean/` succeeds (8414 jobs, no errors, no warnings); `tools/print_axioms.sh` reports only the three standard axioms for all 213 aliases of `RequestProject/Labels.lean`; `#print axioms` run directly on the seven declarations above gives the same; `RequestProject/` has no `sorry` outside block comments, no `axiom`, no `@[implemented_by]`, no `native_decide`; and `tools/gen_labels.py --check`, `tools/decl_files.py --check`, `tools/tex_numbering.py --check` all pass unchanged. A *Re-verification (independent pass)* section recording this was appended to `THEOREMS.md`, and the work is committed and pushed. No statement, proof or count needed to change.

**Still assumed, deliberately out of scope:** `Transducers.Exercises.EFSuccSeparation` (`exer:fo-suc`), `Transducers.Exercises.IteratesReduction` (`exer:rational-composition-finiteness-undecidable`), and the undecidability of the Post correspondence problem — all left exactly as they were.

