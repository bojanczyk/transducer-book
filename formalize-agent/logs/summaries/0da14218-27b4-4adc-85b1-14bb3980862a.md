# C4-first-order — COMPLETE

task: 0da14218-27b4-4adc-85b1-14bb3980862a
created: 2026-08-23 19:30:07.419572

Both target results of Section C.4 are now proved outright, with no `sorry` in any of their dependencies.

**Numbered results now proved**

* **Theorem C.4.11** — `Transducers.foDefinable_iff_aperiodic_dfa` (`RequestProject/PartC/MSO.lean`): a language is first-order definable iff it is recognised by an aperiodic dfa. Statement unchanged.
* **Lemma C.4.13** — `Transducers.tp_eq_iff_fo_equiv` (`RequestProject/PartC/MSO.lean`): two strings have the same k-type iff they satisfy the same first-order sentences of quantifier rank at most k. Statement unchanged.

`#print axioms` on each reports exactly `propext`, `Classical.choice`, `Quot.sound` — no `sorryAx`, no new axioms, no `native_decide`. Neither result is proved from an extra hypothesis: both are unconditional, in the form given in the book.

**How they are proved**

Lemma C.4.13 follows the Ehrenfeucht–Fraïssé argument. One direction is the compositionality of first-order logic (Claim C.4.14 — an internal step, formalised as `Transducers.sat_iff_of_kEquiv` in `PartC/FOComp.lean`, on top of relativisation of formulas in `PartC/FORel.lean` and the transfer of factorisations in `PartC/FOSeg.lean`). The other builds a Hintikka sentence of quantifier rank k separating two strings of different k-type (`PartC/FOHintikka.lean`, with the syntactic renaming/shifting machinery in `PartC/FORename.lean`), using the finiteness and composition properties of k-types from Lemma C.4.15.

Theorem C.4.11: the easy direction runs the finite, aperiodic automaton of k-types (`PartC/FOTypeDFA.lean`). The hard direction follows the book through the aperiodic Krohn–Rhodes Theorem A.2.8 already available in Part A: the Mealy machine attached to the dfa has the dfa's transition function, so aperiodicity is literally the hypothesis of `krohn_rhodes_flipFlop`; flip-flop machines are first-order definable (`PartC/FOFlipFlop.lean`, `PartC/FOMealy.lean`), and first-order definable Mealy machines are closed under composition by substitution of formulas (`PartC/FOSubstRel.lean`). The empty input is handled separately, since the "output ends with b" languages never contain it.

**Still open in Section C.4**: Theorems C.4.16 and C.4.17 remain as faithful statements with `sorry` in `PartC/MSOOpen.lean`; Theorem C.4.8 and Theorem C.3.2 are formalised in both directions but still route through the one open statement of Section C.2 (`boundedWidth_isRegular_step` in `PartC/SnakeReg.lean`), unchanged by this task.

**Repository state**: `lake build` succeeds with no errors. `THEOREMS.md` has been updated — the status rows for C.4.11, C.4.13 and C.4.14, the file table (the nine new files `PartC/FORel.lean`, `FOSeg.lean`, `FOComp.lean`, `FORename.lean`, `FOHintikka.lean`, `FOTypeDFA.lean`, `FOSubstRel.lean`, `FOFlipFlop.lean`, `FOMealy.lean`), and the `## Status` section, including the recorded axiom check. The header comments of `PartC/MSO.lean` and `PartC/MSOOpen.lean` were brought in line. The LaTeX sources and `ARISTOTLE_SUMMARY.md` were not touched. All work is committed and pushed.
