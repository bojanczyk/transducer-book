# C4-relabelling — COMPLETE

task: 415129dd-23b2-49e0-89e4-fd8e834a3812
created: 2026-08-23 11:13:06.120350

Both target results of Section C.4 are now proved outright, with no `sorry` anywhere in their dependencies and no new axioms.

**Theorem C.4.4** (`Transducers.rational_iff_msoRelabelling`, now in `RequestProject/PartC/MSO.lean`) — a string-to-string function is rational iff it is definable by an mso relabelling. Both directions go through bimachines (Theorem B.2.3) and through the doubly marked alphabet `Mark2 A = A × 2 × 2`:
* `PartC/MarkStr.lean` — doubly marked strings, the marking `markAt2 w x y`, its decomposition into prefix / marked infix / suffix, and the regular language `markedSat2 φ` of marked strings satisfying a formula (from Lemma C.4.2).
* `PartC/MarkLogic.lean` — the converse translation from an automaton over `Mark2 A` back to an mso formula with one free variable (via Theorem C.4.1).
* `PartC/MarkBimach.lean` — the bimachine precomputing, at each position, the state of every automaton of a finite family on the unmarked prefix and its state transformation on the unmarked suffix; the function `markFun` it computes, its rationality, and its length preservation in the letter-to-letter case.
* `PartC/MSORatRelab.lean` — the two inclusions. Claim C.4.5 (not a numbered result) is the internal step, formalised as `RatRelab.exists_form` for the index of a bimachine rather than for the transitions of an unambiguous transducer; this choice is documented in the file header and in `THEOREMS.md`.

**Lemma C.4.10** (`Transducers.mso_formulas_via_rational`, also in `PartC/MSO.lean`) — the letter-to-letter rational function precomputing a finite family of formulas with one or two free first-order variables:
* `PartC/MarkDelay.lean` — the delayed automaton over letters carrying states and state transformations (the last letter read is kept pending, since "is this the last position" is known only at the end of the string).
* `PartC/MSOPrecomp.lean` — the function `f` decorating each position by the pair (state before, state transformation after) of each automaton of the family, the set of letters `F_φ` for a one-variable formula, and the delayed regular language `L_φ` for a two-variable formula.

No numbered statement was weakened, generalised or deleted; both statements are byte-for-byte the ones that were previously `sorry`, moved unchanged from `PartC/MSOOpen.lean` (which keeps the still-open results) back to `PartC/MSO.lean`, so every name remains available from `RequestProject.PartC.MSO`. Both were checked with `#print axioms`: each depends only on `propext`, `Classical.choice`, `Quot.sound`; this is recorded in `THEOREMS.md`, whose status column, file list and `## Status` section have been updated.

Still open in Section C.4 (unchanged by this task, statements only): Theorems C.4.8, C.4.11, C.4.16, C.4.17 and Lemma C.4.13. `lake build` succeeds with no errors on the whole project, and none of the new files contains a `sorry`. All work is committed and pushed.
