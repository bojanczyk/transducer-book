# B-effectivity-conditional — COMPLETE_WITH_ERRORS

task: 0827f1f7-10f3-4b74-be12-52b127932c85
created: 2026-08-21 09:55:32.901000

I finished the four remaining results of Part B of the book, turning them into conditional theorems in the style already used for Theorem B.1.6, with all their mathematical content proved outright.

**Now proved (from two explicit effectivity hypotheses, and nothing else)**

* **Theorem B.3.3** `Transducers.weighted_equivalence_decidable` — equivalence of weighted automata over ℚ is decidable.
* **Theorem B.3.4** `Transducers.rationalFun_equivalence_decidable` — equivalence of rational functions is decidable.
* **Theorem B.3.7** `Transducers.weighted_zeroness_decidable` — zeroness is decidable for weighted automata over ℚ.
* **Theorem B.4.2** `Transducers.rationalFun_isMealy_decidable` — one can decide whether a rational function is computed by a Mealy machine (statement relativised to the strings over the alphabet of the code, as documented earlier; the degenerate original is kept as a comment).

Each takes the two hypotheses `EffectiveWeightedEvalEq` (a coded weighted automaton over ℚ can be evaluated effectively, in the form of an equality test) and `EffectiveWeightedBound` (a Schützenberger bound can be computed from the two codes), both in the new `RequestProject/PartB/Effective.lean` with docstrings saying precisely what they assert and why they are true in the ordinary mathematical sense but not derivable inside Mathlib, whose `Primrec`/`Computable` API has no arithmetic on ℤ or ℚ. The unconditional statements are kept as commented-out originals in `RequestProject/PartB/WeightedStatements.lean` with notes on the relationship. I ran `#print axioms` on all four (and on B.1.6 and B.4.3): each depends only on `propext`, `Classical.choice`, `Quot.sound`.

**What I completed in this session.** The scaffolding for these four results was in place but fifteen proofs were still missing, all of them on the path to Theorem B.4.2; I proved every one of them, so that nothing they rest on is assumed:

* `RequestProject/PartB/PrefixCodes.lean` — the two code constructions `dropCode` (computing `w ↦ dropLast (f w)`) and `shiftCode` (computing `w ↦ f (dropLast w)`) are primitive recursive; under the promise both describe total functions on the strings over their alphabet; and for a length-preserving coded function, prefix preservation is exactly the equality of the two coded relations.
* `RequestProject/PartB/CodeRat.lean` — the bridge between codes over the infinite alphabet ℕ and the machine-independent characterisations over finite alphabets: the finite alphabets and state space of a code, the nfa with output it describes over them (with the transfer of runs in both directions), the function `codeFun c` it computes, its rationality, the equivalence of `IsMealy (codeFun c)` with length preservation plus prefix preservation, and the equivalence of the property appearing in Theorem B.4.2 with `IsMealy (codeFun c)`.

**Status of the rest.** Part A and Part B are now complete: no file of either contains a `sorry`. The results still open are in Parts C and D (C.1.4, C.2.5, C.2.8–C.2.11, C.3.2, most of Section C.4, and D.1.1–D.2.4), which remain faithful statements with `sorry`.

`THEOREMS.md` records all of this: the status column of the four results, the new files, a new subsection explaining the two hypotheses and how the four proofs are organised, the note that B.4.2 is relativised, and an updated `## Status` section. The whole project builds cleanly (`lake build`, 8109 jobs, no errors), and everything is committed and pushed.
