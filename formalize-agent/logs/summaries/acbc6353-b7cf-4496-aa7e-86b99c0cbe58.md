# hyp-two-way-bound — COMPLETE

task: acbc6353-b7cf-4496-aa7e-86b99c0cbe58
created: 2026-09-01 23:58:36.667171

`Transducers.EffectiveTwoWayBound` is discharged. Theorem `thm:decidable-equivalence-regular` is now proved outright, and with it every numbered result of the book that this project formalises.

**Acceptance test.** `Transducers.EffectiveTwoWayBound` is taken as an argument by no declaration; `#check @Transducers.Book.«thm:decidable-equivalence-regular»` shows `DecidableUnderPromise (fun p => TwoWayCodeTotal p.1 ∧ TwoWayCodeTotal p.2) (fun p => twoWayCodeRel p.1 = twoWayCodeRel p.2)` with no hypotheses, and `#print axioms` on it reports only `propext`, `Classical.choice`, `Quot.sound`. The `def EffectiveTwoWayBound : Prop` is kept, unchanged, in `RequestProject/PartC/EffectiveReg.lean`; it is now what `Transducers.effectiveTwoWayBound` proves. No conclusion anywhere was weakened, and no new `def ... : Prop` is assumed.

**The route taken.** Not `EffectiveTwoWayWeighted` — the effective, code-to-code form of the book's reduction to weighted automata is still only *stated*, in `RequestProject/PartC/RegBoundGap.lean`, assumed nowhere and depended on by nothing. The bound is instead constructed directly, as an arithmetic formula, in nine new files:

* `RequestProject/Common/HankelRank.lean` — Schützenberger's rank criterion in elementary form (`HankelRank.zero_of_short`): if `f (u ++ v) = ∑ i : ι, g i u * h i v` over a `Fintype ι` and `f` vanishes on all strings of length ≤ `card ι`, then `f ≡ 0`.
* `RequestProject/PartC/RegPos.lean`, `RegBlock.lean`, `RegCross.lean`, `RegProfile.lean`, `RegVal.lean`, `RegHankel.lean` — the crossing-sequence decomposition of a run of a two-way transducer at a cut of the input: a positional description of runs, runs confined to one side of the cut, the alternating sequence of such runs that a full run decomposes into (of length at most `2·|Q|`), the pieces read off each side, the injective rational value `oval` of an output string, and the resulting `RegHankel.hankel_decomp`: `oval (f (u ++ v)) = ∑ ι, g ι u * h ι v`, indexed by the letter at the cut, the crossing sequence of states, and two indices into it.
* `RequestProject/PartC/RegShort.lean` — that index set has explicit size `RegHankel.idxBound`, so applying the criterion to the difference of the two values gives `RegHankel.twoWay_eq_of_short`: two two-way transducers computing total functions over a finite alphabet agree everywhere as soon as they agree on the inputs of length at most `idxBound a q₁ + idxBound a q₂`.
* `RequestProject/PartC/RegEffBound.lean` — a code lists at most two input letters and at most two states per table entry, so those numbers are read off its length; this gives the primitive recursive `RegDec.codeBound`, and the bookkeeping of `RegDec.exists_bound` (a code read over the finite alphabets occurring in it, plus blindness to the letters outside them) is repeated with the explicit bound in place of the abstract one, yielding `Transducers.effectiveTwoWayBound`.

`Transducers.RegDec.regular_equivalence_decidable_aux` and `Transducers.regular_equivalence_decidable` lost their `hBound` argument, conclusions unchanged.

**Status of the numbered results.** All 77 formalised results are now proved outright; none takes a hypothesis. Parts A, B, C and D are complete. Three theorem-like environments remain unformalised, as before (Definition `def:rational-recognisable-subsets`, Conjecture `conj:regular-via-weighted-automata`, and the withdrawn `nolabel:thm-fo-transduction-into-primes`). The three assumptions still in the project are all in *exercises* and were out of scope: `Exercises.EFSuccSeparation`, `Exercises.IteratesReduction`, `Exercises.EffectiveLengthPairsSemilinear`; `Exercises.CanonicalSuffixBimachineExists` remains taken as an argument but is refuted in the project.

**Verification.** `lake build` succeeds with no errors and no warnings (8472 jobs). `tools/print_axioms.sh` reports that all 213 label aliases depend only on `propext`, `Classical.choice`, `Quot.sound`. `tools/gen_labels.py --check`, `tools/decl_files.py --check` and `tools/tex_numbering.py --check` all pass. No `sorry` outside block comments, no `axiom`, no `@[implemented_by]`, no `native_decide`.

**Documentation.** `THEOREMS.md` updated (file index with the nine new files; the status column and index row for `thm:decidable-equivalence-regular`; the *Hypotheses that the Lean statement adds* list; the section formerly called *The conditional result of Part C*, rewritten as *how it became unconditional*; the counts table, now 77 outright and 0 conditional; and a new final `## Status` section that supersedes the earlier ones). `FORMALISATION.md`, `ARISTOTLE_SUMMARY.md`, `RequestProject/Labels.lean`, `RequestProject/PartC.lean` and the docstrings of `EffectiveReg.lean`, `RegBoundGap.lean`, `RegEqDec.lean` and `Statements.lean` were brought into line. `EXERCISES.md` was not touched. All work is committed.
