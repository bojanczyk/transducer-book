# C4-fo-transductions — COMPLETE

task: b4ebd3b0-70d6-43f3-a511-bd7ab76d91f3
created: 2026-08-24 02:55:17.474948

## What is now in the repository

`lake build` succeeds with no errors, and the only `sorry`s in `transducer-lean/RequestProject` are the pre-existing open ones (Part D statements, Part B weighted statements, Part C statements, the snake-lemma induction step `boundedWidth_isRegular_step`) plus the one new, named and documented open half of C.4.17 described below.

### Theorem C.4.16 — proved outright

`Transducers.foRelabelling_iff_aperiodicBimachine` (the first-order relabellings are exactly the functions computed by aperiodic bimachines) is proved, in `RequestProject/PartC/MSO.lean`, from `PartC/FORelabBimach.lean` and `PartC/FOBimachRelab.lean`. `#print axioms` reports only `propext`, `Classical.choice`, `Quot.sound`.

### Theorem C.4.17 — one inclusion proved outright, the other still open

`Transducers.foTransduction_iff_prime_composition` is stated faithfully and is now derived from two named results:

* **Proved outright**: `Transducers.isFOTransduction_of_compClosure` (`PartC/FOTransPrimeComp.lean`) — every composition of the primes is a first-order transduction. Axioms: `propext`, `Classical.choice`, `Quot.sound`. This required building, from scratch, the ingredients that in the mso case come for free via Theorem C.4.8:
  * closure of first-order transductions under composition, by backwards translation of formulas (`PartC/FOTransTr.lean`, `PartC/FOTransComp.lean`);
  * a first-order relabelling is a first-order transduction (`PartC/FORelabTrans.lean`);
  * map reverse is a first-order transduction (`PartC/FOTransRev.lean`);
  * map duplicate is a first-order transduction (`PartC/FOTransDup.lean`, completed this session);
  * supporting theory: block combinatorics of positions (`PartC/BlockPos.lean`), the corresponding first-order formulas (`PartC/BlockForm.lean`), generic tools for building output witnesses (`PartC/ITransBuild.lean`), and the prime family moved to `PartC/FOPrimeFam.lean`.
  Each of these five results was checked with `#print axioms` and depends only on `propext`, `Classical.choice`, `Quot.sound`.
* **Still open**: `Transducers.compClosure_of_isFOTransduction` (`PartC/MSOOpen.lean`) — every first-order transduction is a composition of primes. Its proof is `sorry`, documented in the docstring. This is the direction the book itself does not prove (it defers it to a future edition and only sketches the two first-order ingredients needed, which are aperiodic counterparts of the machinery behind Theorem C.4.8). Consequently `#print axioms Transducers.foTransduction_iff_prime_composition` still reports `sorryAx`.

Importantly, nothing in the newly proved material depends on Theorem C.4.8, which itself still rests on the unproved `boundedWidth_isRegular_step`; the easy inclusion was proved directly rather than through C.4.8.

### Other

Linter warnings introduced by the new files were fixed at their root (unused hypotheses removed from `revOrd_trans`, `le_of_sameBlk_of_not_sameBlk` and `phi_injOn`, unused simp arguments dropped). `THEOREMS.md` has been updated: the status rows for C.4.16 and C.4.17, the new file list entries, a rewritten section explaining exactly what of C.4.17 is proved and what remains, and the `## Status` section including the `#print axioms` results. All work is committed and pushed.

