# exercises-two-new — COMPLETE

task: 0c343c7c-b28f-4354-99a9-31a70dd87d48
created: 2026-09-07 06:04:05.084694

Both new Part B exercises are formalised and proved outright, the repository builds cleanly, and the three tool checks pass.

**Exercise `exer:duplication-not-rational` (`myhill-nerode.tex`)** — new file `transducer-lean/RequestProject/Exercises/DuplicateNotRational.lean`, written after `ReverseNotRational.lean` and following the author's solution: `not_boundedVarRel_duplicate` shows that for `i < j` the strings `falseⁱ` and `falseʲ` are inequivalent for the relation `∼` of Theorem `thm:machine-independent-rational-functions` (`Transducers.BoundedVarRel`) — extending on the left by `true^{K+1}` makes the two outputs diverge at position `K+1+i`, so their left distance exceeds `K` — hence that relation has infinite index and the function is not rational.

One naming point. The requested name `Transducers.Exercises.not_isRationalFun_duplicate` was already taken in the project: the same statement is the second item of Exercise `exer:non-rational`, proved in `Exercises/PartBC.lean` by the book's other argument (the range is the language of squares, which is not regular). I left that declaration exactly as it was and made it the first alias of the new label `exer:duplication-not-rational`; the proof by the relation `∼`, which is what this exercise asks for, is `Transducers.Exercises.not_isRationalFun_duplicate_boundedVar` and is the second alias (`exer:duplication-not-rational#2`). This is recorded in the docstrings, in `EXERCISES.md` and in `THEOREMS.md`.

**Exercise `exer:no-reverse-reversible` (`rational-functions.tex`)** — new file `transducer-lean/RequestProject/Exercises/NoReverseReversible.lean`. The shortened list of primes is `Transducers.Exercises.PrimeRatNoRevRevFam` (prime Mealy machines, right-to-left *flip-flop* Mealy machines, string homomorphisms, `w ↦ w#`), and `Transducers.Exercises.rational_primes_no_reverse_reversible` states that a string-to-string function is rational iff it is a composition of functions from that list. The construction is the author's: append the separator; run a left-to-right reversible Mealy machine whose states are the permutations of `Q`, labelling every position with the product of the permutations of the letters before it and the separator with the product over the whole input; broadcast that product leftwards with a right-to-left flip-flop machine; recover the output letter with a homomorphism, using cancellation in the group of permutations to get the transformation of the suffix after each position.

**The Section *Combinators* renumbering.** `tools/decl_files.py --check` and `tools/tex_numbering.py --check` both failed at the start of the run and now pass. The book has labelled a new claim in that section, Claim `claim:pairing-copairing` (C.5.8), which shifted the nine numbers after it: `LABELS.md` is renumbered, the claim has a row in the index of `THEOREMS.md` and an alias in `Labels.lean`, and it is proved — `Transducers.IsRegularTermFun.pair` and `Transducers.IsRegularTermFun.copair` of `PartC/CombFinite.lean`. Since in this project pairing and co-pairing are constructors of the term syntax and the book's diagonal, co-diagonal and functoriality combinators are the derived ones (the reverse of the book's order, same class of terms), that divergence is documented under *Divergences from the book* and in the docstring of `PartC/CombTerms.lean`. The `decl_files.py` failure had a separate cause: a docstring line of `terms_define_append_hash` began with the word "end-marker", which the script reads as closing a namespace; the docstring is reworded.

**Status of the results.** Both exercises are proved outright — no `sorry`, no `axiom`, no `native_decide`, no hypothesis of any kind, and no dependency containing a `sorry`. `tools/print_axioms.sh` reports that all 240 aliases, the five new ones included, depend only on `propext`, `Classical.choice`, `Quot.sound`. Nothing is left open. `lake build` completes with no errors and no warnings; `tools/gen_labels.py --check`, `tools/decl_files.py --check` and `tools/tex_numbering.py --check` all pass. `THEOREMS.md` (index row, divergence, counts — 116 environments, 90 results proved — and a new status section), `EXERCISES.md` (file list, two index rows and a status section, other chapters' entries untouched), `LABELS.md`, `RequestProject/Labels.lean`, `RequestProject/Exercises.lean` and the run summary are updated. No LaTeX source of the book was modified, and everything is committed and pushed.
