# C2-snake-step — COMPLETE

task: bfa0f826-ae5d-4511-a96e-ba77e78e1ba3
created: 2026-08-25 02:11:27.096964

## Result

The task's target is closed. `Transducers.boundedWidth_isRegular_step` — the induction step of the book's snake lemma in `RequestProject/PartC/SnakeReg.lean` — and everything it rested on are now proved, with no `sorry`, no new `axiom` and no `native_decide`.

### What was actually missing, and how it was closed

On inspection the induction step itself was already assembled; the single remaining gap underneath it was `Transducers.TwoWay.Chk.nonempty_chainData_of_good` in `RequestProject/PartC/SnakeChkRB.lean` ("every good input carries a chain of pieces"), the last piece of the book's stage 1. It is now proved, via four new files in `RequestProject/PartC/`:

* `SnakeChkPieceWin.lean` — `TwoWay.qAt` (the state of the run at a given time) and the four window-condition lemmas deriving `WinCond` membership from run data, with the two boundary states named as `qAt`.
* `SnakeChkCross.lean` — the structures `Chk.CrossOK` / `Chk.HaltOK` packaging one chain piece, and their existence lemmas.
* `SnakeChkSlot.lean` — the times `Chk.pcStart` / `Chk.pcEnd` delimiting a slot, and the existence of a piece for each excursion, progress and final slot.
* `SnakeChkSlotOK.lean` — `Chk.SlotOK` and `Chk.exists_slotOK`: every slot carries a chain piece on a window confined to its pair of blocks.

The design idea that made it go through: take the entry/exit state of each piece to be the *run's* state at the slot's start/end time and each cut to be the head position at those times. Consecutive slots share a time (`pcEnd i r = pcStart i (r+1)`), so the chain conditions (`ext_step`, `ext_pair`, `cut_step`, `cut_last`) become immediate.

### Status of the numbered results

Proved outright (`#print axioms` reports exactly `propext`, `Classical.choice`, `Quot.sound`):

* **Theorem C.2.9** — `Transducers.twoWay_iff_regular` and its hard half `Transducers.twoWay_isRegular`, through `Transducers.isRegularFun_of_isTwoWay`
* the snake-lemma induction step `Transducers.boundedWidth_isRegular_step`
* **Theorem C.3.2** — `Transducers.sst_iff_regular`
* **Theorem C.4.8** — `Transducers.msoTransduction_iff_regular`
* **Corollary C.2.8** — `Transducers.regularFun_isTwoWay` (already proved before this task)
* the new `Transducers.TwoWay.Chk.nonempty_chainData_of_good`

None of these is proved from an extra hypothesis, and no numbered statement was weakened, generalised away or deleted.

Still open, all outside the scope of this task and unchanged by it: the `sorry`-carrying statements in `RequestProject/PartB/WeightedStatements.lean`, `RequestProject/PartB/RationalStatements.lean`, `RequestProject/PartC/Statements.lean`, `RequestProject/PartC/MSOOpen.lean` and `RequestProject/PartD/Statements.lean`; and Part B's conditional results (B.1.6 from the PCP-undecidability hypothesis; B.3.3, B.3.4, B.3.7, B.4.2 from `EffectiveWeightedEvalEq`).

### On the accuracy of `THEOREMS.md`

You asked me to check its section *What is missing in Theorem C.2.9* against the sources. It was **accurate in its diagnosis but out of date in its location**. It named `Transducers.TwoWay.exists_regular_snakeLang` (`SnakeStage1.lean`) as the remaining `sorry`; in fact that statement was already proved in `SnakeChkMain.lean`, on top of a whole `SnakeChk*` family that the file table did not list at all, and the only remaining `sorry` was `nonempty_chainData_of_good`. Its closing prescription, however, was exactly right and is precisely what was done: the data of the pieces had to be given explicitly in terms of the run, the states involved being the run's states at the boundary times of the excursions.

### Housekeeping

`lake build` succeeds with no errors (8256 jobs). `THEOREMS.md` has been updated: the file table now lists the `SnakeChk*` family and the four new files, the status rows for C.2.9, C.3.2 and C.4.8 record them as proved, the section formerly titled *What is missing in Theorem C.2.9* has been rewritten as *How Theorem C.2.9 is proved*, and the `## Status` section and its axiom list have been brought in line. Stale "still open" remarks in the docstrings of `RequestProject/PartC/SnakeReg.lean` and `RequestProject/PartC/Statements.lean` were corrected too. Everything is committed and pushed.

