# encoding-snake-alphabet — COMPLETE_WITH_ERRORS

task: 4013eee5-3222-4769-8e60-80e2bc0e6659
created: 2026-08-28 04:06:29.153092

Lemma `lem:output-of-snake-graph-is-regular` is now stated over the book's own alphabet and proved, and the whole project builds cleanly.

**What is in place**

* The book's alphabet `C` of snake letters is a real object in Lean: `Transducers.SnakeLetter Q B` in `RequestProject/PartC/SnakeAlph.lean` — a letter is a bipartite graph on two copies of the state set `Q`, each vertex with at most one outgoing edge going to the other copy, edges labelled by `B + 1`. The same file defines the snake graph a string over `C` represents (`SnakeGraph.Edge`, `SnakeGraph.IsSnakePath` for the book's "all edges lie on a single directed path"), its output (`SnakeGraph.pathOut`, `SnakeGraph.SnakeOutIs`, well defined by `SnakeGraph.snakeOutIs_unique`), its width (`SnakeGraph.SnakeWidthLe`), and the function of the lemma, `SnakeGraph.snakeOut k`: the output of the snake graph `w` represents when it represents one of width at most `k`, and the empty string otherwise.
* The lemma in the book's phrasing is `Transducers.SnakeGraph.snakeOut_isRegular` (`RequestProject/PartC/SnakeAlphReg.lean`): for every `k`, `snakeOut k` is a regular function. It is proved by *reusing* `Transducers.boundedWidth_isRegular` and the `Snake*` development, not by repeating the induction on the width: the strings representing a snake graph of width at most `k` form a regular language (degree, width and single-source conditions in `SnakeAlphLoc/Char/LocLang.lean`, acyclicity by a left-to-right automaton carrying the reachability relation of the current column in `SnakeAlphCyc.lean`, with `representsSnake_iff` justifying that these four conditions are exactly the book's one-path condition), and on those strings the output is computed by the two-way transducer that walks along the snake (`SnakeAlphRun.lean`), whose halting run has width bounded by the number of states; the two are combined by the conditional of Lemma `lem:regular-closure-properties`.
* Both statements are kept. The label `lem:output-of-snake-graph-is-regular` in `RequestProject/Labels.lean` names the book's form, and the general form `Transducers.boundedWidth_isRegular` is kept as `#2`.

**Are they equivalent?** As Lean statements, no: the book's form is one instance of the general one, over the fixed alphabet `SnakeLetter Q B`, so it is the weaker of the two, and the derivation formalised goes general ⇒ book's form. Mathematically the two have the same content — the reachable part of a width-`k` run of a two-way transducer is a snake graph of width `k` whose slice encoding is produced letter by letter, so `widthOut M k` factors as a letter-to-letter map followed by `snakeOut k` — but that converse derivation is not formalised. `THEOREMS.md` says exactly this.

**Documentation.** The old divergence note ("stated for the width-`k` output function `TwoWay.widthOut` … rather than for an alphabet of snake letters") is gone from `THEOREMS.md`; it is replaced by an accurate one: the statement is the book's, over the alphabet of snake letters, except that it is proved for every `k : ℕ` rather than only `k ∈ {1, …, |Q|}` (more general — a column has at most `|Q|` vertices), and the book's extra letter for the empty input is not needed, since the empty string already represents the one-column graph with no edge and empty output. `THEOREMS.md` also carries the file list, the status row, the section *The alphabet of snake letters* describing the construction, and the `## Status` counts.

**Verification.** `lake build` of the whole project succeeds with no errors (8347 jobs); the `Snake*` files contain no `sorry`; `#print axioms` reports `[propext, Classical.choice, Quot.sound]` for both `Transducers.SnakeGraph.snakeOut_isRegular` and `Transducers.boundedWidth_isRegular`, and `RequestProject/Labels.lean`'s `assert_no_sorry` on the label passes as part of the build.

**Status of this numbered result:** Lemma `lem:output-of-snake-graph-is-regular` is proved outright, with no hypotheses beyond finiteness of the state set and output alphabet; nothing about it remains open. No other numbered result was touched. Work is committed and pushed; a short section recording this pass was prepended to `ARISTOTLE_SUMMARY.md`.
