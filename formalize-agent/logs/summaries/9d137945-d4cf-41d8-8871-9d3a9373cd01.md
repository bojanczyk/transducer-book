# C2-regular-equivalence — COMPLETE_WITH_ERRORS

task: 9d137945-d4cf-41d8-8871-9d3a9373cd01
created: 2026-08-22 01:31:44.821567

## What is now proved

**Lemma C.2.10 (`Transducers.regular_closure_properties`) — proved outright, sorry-free.**
Regular functions are closed under map lifting, under concatenation `w ↦ f w · g w`, and under conditionals over a regular language. `#print axioms` reports only `propext`, `Classical.choice`, `Quot.sound`.

**Claim C.2.11 (`Transducers.sum_of_regular`) — proved outright, sorry-free**, in a corrected form (see the discrepancy note below). `#print axioms` reports only `propext`, `Classical.choice`, `Quot.sound`.

**Theorem C.2.9 (`Transducers.twoWay_iff_regular`) — still open.** The right-to-left implication (regular ⊆ two-way, Corollary C.2.8) is proved; the theorem is now derived from that together with the left-to-right implication, which I isolated as a separate statement `Transducers.twoWay_isRegular` and which still contains the single remaining `sorry` of Section C.2. So C.2.9 must *not* be described as proved.

## Work done this session

* Finished `RequestProject/PartC/RegClosure.lean` (concatenation and conditionals), which was the last unfinished piece of C.2.10; added `RequestProject/PartC/RatSeq.lean` (sequential letter-by-letter rational transducers) that it uses.
* Restructured C.2.9 in `RequestProject/PartC/Statements.lean`: the easy half is now proved inside the theorem, and the hard half is a named, documented statement.
* Updated `THEOREMS.md`: status rows for C.2.9, C.2.10, C.2.11; new file-table rows for `RegularDef.lean`, `RatBuild.lean`, `RatTools.lean`, `RatSeq.lean`, `MapLiftAux.lean`, `MapLiftRat.lean`, `MapLiftPrime.lean`, `RegMapLift.lean`, `SumShape.lean`, `SumPrime.lean`, `SumReg.lean`, `RegSum.lean`, `RegClosure.lean`; new subsections *An error in Claim C.2.11* and *What is missing in Theorem C.2.9*; refreshed the `## Status` section and the `#print axioms` record.

## Discrepancy with the book (documented in the docstring and in THEOREMS.md)

Claim C.2.11 as printed is **false** on the empty input: `ε` uses only letters of `A₁` and, simultaneously, only letters of `A₂`, so the first two clauses conflict unless both `f₁ ε` and `f₂ ε` are empty. An explicit counterexample, `Transducers.not_sum_of_regular_nil`, is in `RequestProject/PartC/RegSum.lean`. The printed statement is kept, commented out, in `RequestProject/PartC/Statements.lean`; the corrected `sum_of_regular` restricts the two clauses to nonempty inputs and assumes the output alphabets are nonempty (needed for `⊥` to exist). This is harmless for the book's use of the claim, since in the proof of C.2.10 the blocks the sum is applied to always carry a marker.

## What remains for C.2.9

The book's route is `A* → C*` ("compute the snake graph", rational, and largely available here through `TwoWayVisit.lean`/`TwoWayAnnot.lean`) followed by `C* → B*` ("output of the snake graph"). The missing part is the second stage: the induction on the *width* of a snake graph, whose step splits a snake into looping and progressing parts along the record-breaking columns and recombines them with the three closure properties of C.2.10. Those closure properties, and Claim C.2.11 on which they rest, are now in place; the remaining gap is the combinatorics of the width induction.

## Repository state

`lake build` succeeds with no errors. No new `sorry` was introduced; the only `sorry`s left are the pre-existing ones (Theorem C.1.4, Theorem C.3.2, `PartC/MSO.lean`, `PartB/WeightedStatements.lean`, `PartD/Statements.lean`) plus the isolated `twoWay_isRegular`. All work is committed and pushed.
