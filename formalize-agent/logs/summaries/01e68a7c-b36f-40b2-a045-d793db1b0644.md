# external — COMPLETE

task: 01e68a7c-b36f-40b2-a045-d793db1b0644
created: 2026-08-25 05:13:01.889012

Theorem C.1.4 (decidability of equivalence for regular functions), whose statement on finite descriptions of two-way transducers was the last `sorry` of Part C, is now a **proved conditional result**, in exactly the style already used for Theorems B.3.3 and B.3.7. The whole project builds (`lake build`, no errors), Part C now contains no `sorry` in active code, and `#print axioms` for the new results reports only `propext`, `Classical.choice`, `Quot.sound`.

**What the statement now looks like** (`transducer-lean/RequestProject/PartC/Statements.lean`): `Transducers.regular_equivalence_decidable` takes two explicit effectivity hypotheses and is proved from them; the unconditional form is kept, commented out, with a note explaining the relationship, and the definitions of codes (`TwoWayCode`, `twoWayCodeAut`, `twoWayCodeRel`, `TwoWayCodeTotal`) moved unchanged into a supporting file, with a pointer left behind.

**The hypotheses** (`RequestProject/PartC/EffectiveReg.lean`), both true statements about ordinary computability that Mathlib's `Primrec`/`Computable` API cannot currently express because it has no arithmetic on the integers or the rationals:
* `EffectiveTwoWayEvalEq` — two coded two-way transducers can be compared effectively on a given input;
* `EffectiveTwoWayBound` — an equivalence bound can be computed from the two codes. Only the *computability* of the bound is assumed.

**What is proved, and not assumed:**
* `Transducers.RegDec.twoWayCodeRel_map` (`RegCodeSan.lean`) — a code is a finite table, so it cannot distinguish two letters that are both absent from it; renaming those letters does not change the computed relation. This is what makes the equivalence test a finite check: it suffices to compare the two codes on the strings over their letters plus one fresh letter.
* `Transducers.exists_twoWayCode_bound` (`RegCodeBound.lean`) — the *existence* of the equivalence bound, i.e. the whole mathematical content of the second hypothesis. A code describes a transducer over the infinite alphabet ℕ with infinitely many possible states, but only finitely many letters, output letters and states occur in it, so the code also describes a transducer over those finite sets whose runs correspond step by step to the runs of the coded transducer; its function is regular by Theorem C.2.9, and the bound then comes from the already-proved `Transducers.regularFun_eq_of_short`. Along the way, the determinism of two-way transducers (`TwoWay.computes_unique`) is proved.
* `RegEqDec.lean` — the decision procedure itself (test all short strings over the letters of the two codes and a fresh letter), its correctness and its computability, assembled as in Part B's `WeightedDec.lean`.

`THEOREMS.md` was updated: the status row for C.1.4, the file table with the four new files, a new subsection *The conditional result of Part C (C.1.4)* mirroring the one for Part B, and the overall status paragraph. `ARISTOTLE_SUMMARY.md` was not modified. All work is committed and pushed.
