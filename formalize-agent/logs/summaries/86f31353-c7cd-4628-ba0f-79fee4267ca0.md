# C4-buchi — COMPLETE

task: 86f31353-c7cd-4628-ba0f-79fee4267ca0
created: 2026-08-22 23:14:26.812556

All three target results of this task are now proved outright, and `lake build` succeeds with no errors.

**Proved outright (no `sorry` anywhere in their dependencies; `#print axioms` reports only `propext`, `Classical.choice`, `Quot.sound` for each):**

* **Lemma C.4.2** — `Transducers.mso_annotated_regular` (`RequestProject/PartC/MSO.lean`, proved in `RequestProject/PartC/MSOAnnot.lean`). The language of annotated strings over `A × 2^{k+l}` satisfying a formula is regular, by induction on the syntax: the atomic formulas `x_i ≤ x_j`, `a(x_i)`, `x_i ∈ X_j` are recognised by scanning automata, the Boolean connectives use closure of regular languages under Boolean operations, and each quantifier is a projection, realised as the image of a language under a letter-to-letter map (with the induction hypothesis applied at `k' = max k (i+1)` variables so that the quantified variable need not be among the free ones). The auxiliary language `AnnLang` is then identified with the language occurring in the statement of the lemma.

* **Theorem C.4.1** — `Transducers.regular_iff_msoDefinable` (proved in `RequestProject/PartC/MSOBuchi.lean`), the Büchi–Elgot–Trakhtenbrot theorem. From logic to automata: the formula is turned into a sentence by existentially quantifying all of its variables, and the language of a sentence is obtained from Lemma C.4.2 with `k = l = 0` by pulling back along `a ↦ (a, (), ())`; the empty string, on which an existential closure is false, is handled separately by Boolean operations. From automata to logic: a formula that guesses the run of a deterministic automaton, with one second-order variable per state, asserting that the sets partition the positions, are consistent with the initial state and with the transition function, and that the last position lies in an accepting set (again with a separate disjunct for the empty string).

* **Claim C.4.6** — `Transducers.msoRelabelling_annotation_regular` (proved in `RequestProject/PartC/MSORelab.lean`), as a corollary of Lemma C.4.2: strings over `Γ × 2` with exactly one marked position at which a fixed formula holds form a regular language (inverse image of an annotated language, all first-order variables placed at the marked position and all second-order variables empty); for each index the "bad" strings are the projection of such a language intersected with "the marked letter carries that index", and the language of the claim is the intersection, over the finitely many indices, of the complements.

**Statements were kept exactly as they stood** — no numbered statement was weakened, generalised away, or deleted, and no new `axiom`, `sorry` or `native_decide` was introduced. No discrepancy with the book was encountered for these three results, so nothing new had to be documented as a correction.

**Still open in Section C.4** (unchanged, statement only): C.4.4, C.4.8, C.4.10, C.4.11, C.4.13, C.4.16, C.4.17. Elsewhere the pre-existing open items are unchanged (the induction step of the snake lemma in Section C.2, on which C.2.9 and hence C.3.2 depend, and Part D).

**Files.** New in this run: `RequestProject/PartC/MSOBuchi.lean` and `RequestProject/PartC/MSORelab.lean`; completed: `RequestProject/PartC/MSOAnnot.lean`; extended: `RequestProject/PartC/MSOSyntax.lean` and `RequestProject/PartC/RegAut.lean` (universal quantification/implication abbreviations, existential closure over given valuations, and the language `{[]}`). `THEOREMS.md` has been updated: the status rows of C.4.1, C.4.2 and C.4.6, the table of files (including the earlier split of `MSO.lean` into `MSODef.lean` plus the statements file), and the `## Status` section, which now records that these three results are proved outright and depend only on the three permitted axioms. Everything is committed and pushed.
