# external — COMPLETE

task: 7099d7f0-ebf7-4d53-8998-00337a5e34a5
created: 2026-08-24 06:27:01.951669

Theorem C.4.17 has been removed from the formalised theorems.

What changed, in `transducer-lean`:

* `RequestProject/PartC/MSOOpen.lean` — the two Lean declarations that made up Theorem C.4.17, namely `Transducers.foTransduction_iff_prime_composition` (the theorem) and `Transducers.compClosure_of_isFOTransduction` (its unproved half, which carried the `sorry`), are no longer declarations. Following the project's convention of not deleting stated material, their text is retained in the file inside a block comment, with a note explaining that they were removed at your request. The file now declares nothing and contains no live `sorry`; it is kept for its pointer comments, and `PartC/MSO.lean` still imports it.
* The result that stood on its own is untouched and still proved: `Transducers.isFOTransduction_of_compClosure` in `RequestProject/PartC/FOTransPrimeComp.lean` (every composition of first-order relabellings, map reverse and map duplicate is a first-order transduction), together with all its supporting files (closure of first-order transductions under composition, the three primes, and the block-combinatorics machinery).
* Documentation brought in line: the status row and file table entries for C.4.17 in `THEOREMS.md`, the section now titled *Theorem C.4.17: what was removed and what remains*, the summary paragraphs and the closing `#print axioms` note (no statement depends on `sorryAx` through C.4.17 any more), plus the header comments of `PartC/MSO.lean`, `PartC/MSOOpen.lean`, `PartC/FOPrimeFam.lean` and `PartC/FOTransPrimeComp.lean`. `ARISTOTLE_SUMMARY.md` and the LaTeX sources were not modified.

Verification: a full `lake build` of the project succeeds with no errors, and `#check` confirms `Transducers.foTransduction_iff_prime_composition` no longer exists while `Transducers.isFOTransduction_of_compClosure` still does. Section C.4 now has no `sorry` at all; the remaining `sorry`s elsewhere in the project (Part D statements, Part B weighted/rational statements, some Part C statements, and the snake-lemma induction step) are the pre-existing ones and are unchanged. All work is committed and pushed.
