/-
Part B: Rational relations, rational functions, weighted automata and the
machine independent characterisations, from *Transducers* (M. Bojańczyk).

This file collects the contents of the directory `RequestProject/PartB`.  The
numbered results of Sections B.1-B.2 are stated in
`RequestProject/PartB/RationalStatements.lean` and those of Sections B.3-B.4 in
`RequestProject/PartB/WeightedStatements.lean`; the other files contain the
constructions used in their proofs.
-/
import RequestProject.PartB.LabAut
import RequestProject.PartB.Atomize
import RequestProject.PartB.OutLang
import RequestProject.PartB.EpsElim
import RequestProject.PartB.RatComp
import RequestProject.PartB.RatCont
import RequestProject.PartB.HomComplement
import RequestProject.PartB.MealyChar
import RequestProject.PartB.Typing
import RequestProject.PartB.LenNormalForm
import RequestProject.PartB.SeqChar
import RequestProject.PartB.RationalStatements
import RequestProject.PartB.WeightedStatements
